@Test public void getAfter_ReturnsHooks(){
  Element element=features.get(1).getElements()[0];
  Hook[] after=element.getAfter();
  assertThat(after).hasSize(1);
  assertThat(after[0].getResult().getDuration()).isEqualTo(64700000L);
}
