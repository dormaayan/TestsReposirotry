@Test public void compareTo_OnDifferentTagName_ReturnsNoneZero(){
  TagObject tag1=new TagObject("one");
  TagObject tag2=new TagObject("two");
  int result=comparator.compare(tag1,tag2);
  assertThat(result).isNotEqualTo(0);
}
