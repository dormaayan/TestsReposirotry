@Test public void compareTo_OnSameLocation_ReturnsZero(){
  TagObject tag1=new TagObject("one");
  TagObject tag2=new TagObject("one");
  int result=comparator.compare(tag1,tag2);
  assertThat(result).isEqualTo(0);
}
