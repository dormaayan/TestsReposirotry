@Test public void getCells_ReturnsCells(){
  Row[] rows=features.get(0).getElements()[0].getSteps()[2].getRows();
  String[] cells=rows[0].getCells();
  assertThat(rows).hasSize(5);
  assertThat(cells).containsExactly("Müller","Deutschland");
}
