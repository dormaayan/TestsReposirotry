@Test public void testInternalException(){
  try {
    throw new InternalException(cause);
  }
 catch (  EmpireException e) {
    assertEquals(e.getErrorParams()[0],"RuntimeException");
    assertEquals(e.getErrorParams()[1],ROOT_EXCEPTION_MSG);
  }
}
