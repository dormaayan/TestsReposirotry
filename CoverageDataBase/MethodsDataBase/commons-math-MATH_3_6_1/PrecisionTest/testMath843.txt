@Test public void testMath843(){
  final double afterEpsilon=FastMath.nextAfter(Precision.EPSILON,Double.POSITIVE_INFINITY);
  Assert.assertTrue(1 + Precision.EPSILON == 1);
  Assert.assertFalse(1 + afterEpsilon == 1);
}
