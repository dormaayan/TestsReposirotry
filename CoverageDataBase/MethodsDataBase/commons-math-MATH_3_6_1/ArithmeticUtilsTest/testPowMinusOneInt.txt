@Test public void testPowMinusOneInt(){
  final int base=-1;
  for (int i=0; i < 100; i++) {
    final int pow=ArithmeticUtils.pow(base,i);
    Assert.assertEquals("i: " + i,i % 2 == 0 ? 1 : -1,pow);
  }
}
