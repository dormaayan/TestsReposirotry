@Test public void testGammaNegativeDouble(){
  double previousGamma=Gamma.gamma(-18.5);
  for (double x=-19.5; x > -25; x-=1.0) {
    double gamma=Gamma.gamma(x);
    Assert.assertEquals((int)FastMath.signum(previousGamma),-(int)FastMath.signum(gamma));
    previousGamma=gamma;
  }
}
