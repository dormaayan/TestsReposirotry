@Test public void growMaxSize() throws Exception {
  cache.close();
  createNewCacheWithSize(10);
  set("a","a","aaa");
  set("b","bb","bbbb");
  cache.setMaxSize(20);
  set("c","c","c");
  assertEquals(12,cache.size());
}
