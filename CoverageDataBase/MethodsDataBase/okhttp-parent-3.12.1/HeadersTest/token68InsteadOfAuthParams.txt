@Test public void token68InsteadOfAuthParams(){
  Headers headers=new Headers.Builder().add("WWW-Authenticate","Other abc==").build();
  assertEquals(singletonList(new Challenge("Other",singletonMap(((String)null),"abc=="))),HttpHeaders.parseChallenges(headers,"WWW-Authenticate"));
}
