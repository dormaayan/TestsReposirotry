@Test(expected=IOException.class) public void testCopyToDirectoryWithIterableSourceDoesNotExist() throws IOException {
  FileUtils.copyToDirectory(Collections.singleton(new File(getTestDirectory(),"doesNotExists")),getTestDirectory());
}
