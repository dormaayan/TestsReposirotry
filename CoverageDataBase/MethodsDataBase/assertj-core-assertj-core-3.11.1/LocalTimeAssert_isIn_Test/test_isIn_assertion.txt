@Test public void test_isIn_assertion(){
  assertThat(REFERENCE).isIn(REFERENCE.toString(),REFERENCE.plusHours(1).toString());
  assertThatThrownBy(() -> assertThat(REFERENCE).isIn(REFERENCE.plusHours(1).toString(),REFERENCE.plusHours(2).toString())).isInstanceOf(AssertionError.class);
}
