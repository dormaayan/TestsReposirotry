@Test public void should_work_with_optional_chained_with_map(){
  Optional<String> optional=Optional.of("Gandalf");
  softly.then(optional).contains("Gandalf").map(String::length).contains(7);
  softly.assertAll();
}
