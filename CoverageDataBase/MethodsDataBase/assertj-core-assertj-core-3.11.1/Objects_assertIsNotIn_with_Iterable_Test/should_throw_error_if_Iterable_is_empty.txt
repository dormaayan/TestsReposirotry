@Test public void should_throw_error_if_Iterable_is_empty(){
  assertThatIllegalArgumentException().isThrownBy(() -> objects.assertIsNotIn(someInfo(),"Luke",emptyList())).withMessage(iterableIsEmpty());
}
