/** 
 * Test that the delegate method is called.
 */
@Test public void withAssertions_assertThat_comparable_Test(){
  assertThat(new Comparable<String>(){
    @Override public int compareTo(    final String o){
      return 0;
    }
  }
).isNotNull();
}
