@Test public void should_fail_if_optional_is_empty(){
  Optional<Object> actual=Optional.empty();
  Throwable thrown=catchThrowable(() -> {
    assertThat(actual).containsInstanceOf(Object.class);
  }
);
  assertThat(thrown).isInstanceOf(AssertionError.class).hasMessage(shouldBePresent(actual).create());
}
