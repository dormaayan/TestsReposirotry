@Test public void should_fail_if_dates_as_string_array_parameter_is_empty(){
  assertThatIllegalArgumentException().isThrownBy(() -> assertThat(Instant.now()).isNotIn(new String[0])).withMessage("The given Instant array should not be empty");
}
