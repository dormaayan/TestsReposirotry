@Test public void testValidateFunctionWithTwoAndThreeArguments() throws Exception {
  Expression exp=new ExpressionBuilder("gamma(x, beta(y, h), z)").variables("x","y","z","h").functions(gamma,beta).build();
  ValidationResult result=exp.validate(false);
  Assert.assertTrue(result.isValid());
}
