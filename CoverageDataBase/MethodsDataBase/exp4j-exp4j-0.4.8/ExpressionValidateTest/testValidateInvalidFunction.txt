@Test public void testValidateInvalidFunction() throws Exception {
  Expression exp=new ExpressionBuilder("sin()").build();
  ValidationResult result=exp.validate(false);
  Assert.assertFalse(result.isValid());
}
