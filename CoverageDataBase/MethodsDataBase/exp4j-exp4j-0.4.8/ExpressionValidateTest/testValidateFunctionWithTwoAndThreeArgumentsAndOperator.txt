@Test public void testValidateFunctionWithTwoAndThreeArgumentsAndOperator() throws Exception {
  Expression exp=new ExpressionBuilder("gamma(x, beta(y, h), z + 1)").variables("x","y","z","h").functions(gamma,beta).build();
  ValidationResult result=exp.validate(false);
  Assert.assertTrue(result.isValid());
}
