@Test public void testValidateInvalidFunctionWithManyArguments() throws Exception {
  Expression exp=new ExpressionBuilder("beta(1, 2, 3)").functions(beta).build();
  ValidationResult result=exp.validate(false);
  Assert.assertFalse(result.isValid());
}
