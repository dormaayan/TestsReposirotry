@Test public void testValidateFunctionWithTwoArguments() throws Exception {
  Expression exp=new ExpressionBuilder("beta(x, y)").variables("x","y").functions(beta).build();
  ValidationResult result=exp.validate(false);
  Assert.assertTrue(result.isValid());
}
