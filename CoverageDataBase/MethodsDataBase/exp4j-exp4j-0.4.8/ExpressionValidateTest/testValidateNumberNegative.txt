@Test public void testValidateNumberNegative() throws Exception {
  Expression exp=new ExpressionBuilder("-1").build();
  ValidationResult result=exp.validate(false);
  Assert.assertTrue(result.isValid());
}
