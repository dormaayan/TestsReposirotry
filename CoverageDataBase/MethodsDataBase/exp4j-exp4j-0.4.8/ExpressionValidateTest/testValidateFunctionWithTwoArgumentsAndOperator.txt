@Test public void testValidateFunctionWithTwoArgumentsAndOperator() throws Exception {
  Expression exp=new ExpressionBuilder("beta(x, y + 1)").variables("x","y").functions(beta).build();
  ValidationResult result=exp.validate(false);
  Assert.assertTrue(result.isValid());
}
