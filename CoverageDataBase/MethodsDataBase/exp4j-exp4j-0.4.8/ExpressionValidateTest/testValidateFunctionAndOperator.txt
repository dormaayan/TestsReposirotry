@Test public void testValidateFunctionAndOperator() throws Exception {
  Expression exp=new ExpressionBuilder("sin(x + 1 + 2)").variable("x").build();
  ValidationResult result=exp.validate(false);
  Assert.assertTrue(result.isValid());
}
