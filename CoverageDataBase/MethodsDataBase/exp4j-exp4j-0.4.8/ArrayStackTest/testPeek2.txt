@Test public void testPeek2(){
  ArrayStack stack=new ArrayStack(5);
  stack.push(-1);
  double old=-1;
  for (int i=0; i < 5; i++) {
    assertEquals(old,stack.peek(),0d);
    stack.push(i);
    old=i;
    assertEquals(old,stack.peek(),0d);
  }
}
