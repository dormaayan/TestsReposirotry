@Test public void testPeek(){
  ArrayStack stack=new ArrayStack(5);
  for (int i=0; i < 5; i++) {
    stack.push(i);
  }
  assertEquals(4d,stack.peek(),0d);
  assertEquals(4d,stack.peek(),0d);
  assertEquals(4d,stack.peek(),0d);
}
