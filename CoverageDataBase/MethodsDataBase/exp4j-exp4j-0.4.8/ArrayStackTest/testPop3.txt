@Test public void testPop3(){
  ArrayStack stack=new ArrayStack(5);
  for (int i=0; i < 5; i++) {
    stack.push(i);
    assertEquals(1,stack.size());
    assertEquals(i,stack.pop(),0d);
  }
  assertEquals(0,stack.size());
  assertTrue(stack.isEmpty());
}
