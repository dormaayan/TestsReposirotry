@Test(expected=IllegalArgumentException.class) public void testFunctionNameEmpty() throws Exception {
  Function f=new Function(""){
    @Override public double apply(    double... args){
      return 0;
    }
  }
;
}
