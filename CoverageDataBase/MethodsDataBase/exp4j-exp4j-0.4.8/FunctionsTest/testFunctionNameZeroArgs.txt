@Test public void testFunctionNameZeroArgs() throws Exception {
  Function f=new Function("foo",0){
    @Override public double apply(    double... args){
      return 0;
    }
  }
;
  assertEquals(0f,f.apply(),0f);
}
