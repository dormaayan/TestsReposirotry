@Test(expected=IllegalArgumentException.class) public void testFunctionNameNull() throws Exception {
  Function f=new Function(null){
    @Override public double apply(    double... args){
      return 0;
    }
  }
;
}
