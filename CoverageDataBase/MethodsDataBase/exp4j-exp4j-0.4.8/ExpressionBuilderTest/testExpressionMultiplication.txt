@Test public void testExpressionMultiplication() throws Exception {
  String expr="2*-2";
  double expected=-4d;
  Expression e=new ExpressionBuilder(expr).build();
  assertEquals(expected,e.evaluate(),0d);
}
