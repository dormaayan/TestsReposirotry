/** 
 * Verify that security disabling feature works properly. <p/> 1. try to hit url with invalid certificate and evaluate that exception is thrown
 * @throws Exception
 */
@Test(expected=IOException.class) public void testUnsafeFail() throws Exception {
  String url=WEBSITE_WITH_INVALID_CERTIFICATE;
  Jsoup.connect(url).execute();
}
