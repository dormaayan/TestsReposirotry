@Test public void followsTempRedirect() throws IOException {
  Connection con=Jsoup.connect("http://direct.infohound.net/tools/302.pl");
  Document doc=con.get();
  assertTrue(doc.title().contains("jsoup"));
}
