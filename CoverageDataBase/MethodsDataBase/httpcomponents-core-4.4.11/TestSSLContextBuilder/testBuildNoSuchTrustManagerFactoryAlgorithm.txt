@Test public void testBuildNoSuchTrustManagerFactoryAlgorithm() throws Exception {
  thrown.expect(NoSuchAlgorithmException.class);
  final URL resource1=getResource("/test-keypasswd.keystore");
  final String storePassword="nopassword";
  SSLContextBuilder.create().setTrustManagerFactoryAlgorithm(" BAD ").loadTrustMaterial(resource1,storePassword.toCharArray()).build();
}
