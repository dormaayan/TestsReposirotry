@Test public void testSetPoolableBoolean() throws Exception {
  try {
    delegate.setPoolable(Boolean.TRUE);
  }
 catch (  final SQLException e) {
  }
  verify(obj,times(1)).setPoolable(Boolean.TRUE);
}
