@Test public void testExecuteLargeUpdateStringInteger() throws Exception {
  try {
    delegate.executeLargeUpdate("foo",1);
  }
 catch (  final SQLException e) {
  }
  verify(obj,times(1)).executeLargeUpdate("foo",1);
}
