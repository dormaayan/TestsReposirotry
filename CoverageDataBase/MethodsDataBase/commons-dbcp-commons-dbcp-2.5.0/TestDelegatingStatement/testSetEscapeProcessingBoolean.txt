@Test public void testSetEscapeProcessingBoolean() throws Exception {
  try {
    delegate.setEscapeProcessing(Boolean.TRUE);
  }
 catch (  final SQLException e) {
  }
  verify(obj,times(1)).setEscapeProcessing(Boolean.TRUE);
}
