@Test public void testUpdateByteStringByte() throws Exception {
  try {
    delegate.updateByte("foo",(byte)1);
  }
 catch (  final SQLException e) {
  }
  verify(rs,times(1)).updateByte("foo",(byte)1);
}
