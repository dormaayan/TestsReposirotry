@Test public void test_serial_eager_gsc_hand_coded(){
  Verify.assertListsEqual(Interval.oneToBy(1_000_000,10_000).flatCollect(each -> Interval.fromTo(each,each + 9_997)).toList(),this.serial_eager_gsc_hand_coded());
}
