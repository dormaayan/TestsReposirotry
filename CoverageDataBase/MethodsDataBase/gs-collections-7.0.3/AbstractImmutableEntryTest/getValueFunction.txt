@Test public void getValueFunction(){
  ImmutableEntry<String,Integer> entry=new ImmutableEntry<>("foo",2);
  Assert.assertEquals(Integer.valueOf(2),AbstractImmutableEntry.<Integer>getValueFunction().valueOf(entry));
}
