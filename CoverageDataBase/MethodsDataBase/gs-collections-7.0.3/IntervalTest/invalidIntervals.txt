@Test public void invalidIntervals(){
  try {
    Interval.fromToBy(5,1,2);
    Assert.fail();
  }
 catch (  IllegalArgumentException ignored) {
  }
  try {
    Interval.fromToBy(5,1,0);
    Assert.fail();
  }
 catch (  IllegalArgumentException ignored) {
  }
  try {
    Interval.fromToBy(-5,1,-1);
    Assert.fail();
  }
 catch (  IllegalArgumentException ignored) {
  }
}
