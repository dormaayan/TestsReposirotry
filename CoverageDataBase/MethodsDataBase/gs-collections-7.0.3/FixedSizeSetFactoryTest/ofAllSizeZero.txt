@Test public void ofAllSizeZero(){
  MutableSet<Integer> set=Sets.fixedSize.ofAll(FastList.<Integer>newList());
  Assert.assertEquals(UnifiedSet.<Integer>newSetWith(),set);
  Verify.assertInstanceOf(FixedSizeSet.class,set);
}
