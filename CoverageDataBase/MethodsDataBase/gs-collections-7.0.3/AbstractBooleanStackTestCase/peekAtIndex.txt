@Test public void peekAtIndex(){
  int size=this.classUnderTest().size();
  for (int i=0; i < size; i++) {
    boolean sizeEven=(this.classUnderTest().size() & 1) == 0;
    boolean iEven=(i & 1) == 0;
    Assert.assertEquals(sizeEven != iEven,this.classUnderTest().peekAt(i));
  }
}
