@Test public void forEachFromToInReverse(){
  MutableList<Integer> result=Lists.mutable.of();
  this.newWith(1,2,3,4).forEach(3,2,result::add);
  Assert.assertEquals(FastList.newListWith(4,3),result);
}
