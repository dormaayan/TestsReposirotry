@Test public void testDuplicateAdd() throws Exception {
  Predicate p=Constant.TRUE;
  And q=new And(p,p);
  assertTrue(q.test());
  for (int i=0; i < 10; i++) {
    q.and(p);
    assertTrue(q.test());
  }
}
