@Test public void testDuplicateAdd() throws Exception {
  Predicate p=Constant.TRUE;
  Or q=new Or(p,p);
  assertTrue(q.test());
  for (int i=0; i < 10; i++) {
    q.or(p);
    assertTrue(q.test());
  }
}
