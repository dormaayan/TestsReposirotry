@Test public void testMark_unsupported() throws IOException {
  JimfsInputStream in=newInputStream(1,2,3);
  assertThat(in.markSupported()).isFalse();
  in.mark(1);
  try {
    in.reset();
    fail();
  }
 catch (  IOException expected) {
  }
}
