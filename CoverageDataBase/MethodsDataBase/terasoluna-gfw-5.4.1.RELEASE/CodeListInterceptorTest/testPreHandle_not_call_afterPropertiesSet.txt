/** 
 * [preHandle] Case of not call afterPropertiesSet. <p> [Expected Result] <ol> <li>do nothing.(don't occur error.)</li> </ol> </p>
 * @throws Exception
 */
@Test public void testPreHandle_not_call_afterPropertiesSet() throws Exception {
  boolean actualReturnValue=testTarget.preHandle(mockRequest,mockResponse,null);
  assertThat(mockRequest.getAttributeNames().hasMoreElements(),is(false));
  assertThat(actualReturnValue,is(true));
}
