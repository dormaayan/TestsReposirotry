/** 
 * [afterPropertiesSet] Case of applicationContext is null. <p> [Expected Result] <ol> <li>target is one.(don't occur error.)</li> </ol> </p>
 * @throws Exception
 */
@Test public void testAfterPropertiesSet_applicationContext_is_null() throws Exception {
  testTarget.setApplicationContext(null);
  try {
    testTarget.afterPropertiesSet();
  }
 catch (  IllegalArgumentException e) {
    assertThat("applicationContext is null.",is(e.getMessage()));
  }
}
