/** 
 * specify operator. expected valid if input left value less than right value.
 * @throws Throwable
 */
@Test public void testSpecifyOperatorLessThan() throws Throwable {
{
    form.setLeft(101);
    form.setRight(100);
    violations=validator.validate(form,LessThan.class);
    assertThat(violations.size(),is(1));
    assertThat(violations.iterator().next().getMessage(),is(String.format(MESSAGE_VALIDATION_ERROR,"left","right")));
  }
{
    form.setLeft(100);
    form.setRight(100);
    violations=validator.validate(form,LessThan.class);
    assertThat(violations.size(),is(1));
    assertThat(violations.iterator().next().getMessage(),is(String.format(MESSAGE_VALIDATION_ERROR,"left","right")));
  }
{
    form.setLeft(99);
    form.setRight(100);
    violations=validator.validate(form,LessThan.class);
    assertThat(violations.size(),is(0));
  }
}
