@Test public void testEmptyPairs(){
  expectedException.expect(IllegalArgumentException.class);
  expectedException.expectMessage("pairs must not be empty");
  new FullHalfPairs(Collections.<FullHalfPair>emptySet(),null);
}
