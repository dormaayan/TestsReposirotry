/** 
 * abnormal case 2. <pre> - page is null </pre>
 */
@Test public void testDoStartTagInternal09() throws Exception {
  Page<String> page=null;
  tag.setPage(page);
  int ret=tag.doStartTagInternal();
  assertThat(ret,is(TagSupport.EVAL_BODY_INCLUDE));
  String expected="";
  assertThat(getOutput(),is(expected));
}
