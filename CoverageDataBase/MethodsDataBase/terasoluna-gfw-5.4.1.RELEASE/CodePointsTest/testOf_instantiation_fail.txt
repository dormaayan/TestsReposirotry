@Test public void testOf_instantiation_fail(){
  expectedException.expect(IllegalArgumentException.class);
  expectedException.expectMessage("exception occurred while initializing");
  CodePoints.of(AbstractCodePoints.class);
}
