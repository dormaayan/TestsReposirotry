@Test public void testGetAllNotAllowedCodePoint_includingNotAllowedSurrogatePair(){
  String testStr=SURROGATE_PAIR_CHAR_2000B + "あいうあ";
  Set<Integer> result=new CodePoints("あいうえお").allExcludedCodePoints(testStr);
  assertThat(result.size(),is(1));
  assertThat(result.contains(0x2000B),is(true));
}
