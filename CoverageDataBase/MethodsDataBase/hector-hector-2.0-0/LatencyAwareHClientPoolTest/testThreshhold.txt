@Test public void testThreshhold(){
  for (int i=50; i < 150; i++) {
    clientPool.add(i);
  }
  double score1=clientPool.score();
  clientPool.clear();
  System.out.println(score1);
  for (int i=450; i < 550; i++) {
    clientPool.add(i);
  }
  double score2=clientPool.score();
  clientPool.clear();
  System.out.println(score2);
  System.out.println((score2 - score1) / score2);
  assertTrue("Error The Badness threshold value is not taken into account!",(score2 - score1) / score2 > DYNAMIC_BADNESS_THRESHOLD);
}
