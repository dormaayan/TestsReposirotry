/** 
 * Validate serialization roundtrip for a collection of objects (defined by subclasses).
 */
@Test public void testRoundTrip(){
  for (  T object : getTestData()) {
    Assert.assertEquals(object,getSerializer().fromByteBuffer(getSerializer().toByteBuffer(object)));
  }
}
