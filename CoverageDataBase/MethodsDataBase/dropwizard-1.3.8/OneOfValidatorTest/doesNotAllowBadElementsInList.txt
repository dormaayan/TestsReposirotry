@Test public void doesNotAllowBadElementsInList(){
  final Example example=new Example();
  example.basicList=ImmutableList.of("four");
  assertThat(format(validator.validate(example))).containsOnly("basicList[0].<collection element> must be one of [one, two, three]");
}
