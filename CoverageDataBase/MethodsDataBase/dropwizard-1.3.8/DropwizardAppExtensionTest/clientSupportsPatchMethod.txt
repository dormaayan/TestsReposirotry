@Test public void clientSupportsPatchMethod(){
  assertThat(EXTENSION.client().target("http://localhost:" + EXTENSION.getLocalPort() + "/echoPatch").request().method("PATCH",Entity.text("Patch is working"),String.class),is("Patch is working"));
}
