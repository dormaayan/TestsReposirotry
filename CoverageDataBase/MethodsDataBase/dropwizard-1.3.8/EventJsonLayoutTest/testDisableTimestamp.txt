@Test public void testDisableTimestamp(){
  includes.remove(EventAttribute.TIMESTAMP);
  eventJsonLayout.setIncludes(includes);
  assertThat(eventJsonLayout.toJsonMap(event)).containsOnly(entry("thread","main"),entry("level","INFO"),entry("logger",logger),entry("message",message),entry("mdc",mdc));
}
