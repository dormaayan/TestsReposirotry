@Test public void testAddNewField(){
  Map<String,Object> map=new EventJsonLayout(jsonFormatter,timestampFormatter,throwableProxyConverter,includes,ImmutableMap.of(),ImmutableMap.of("serviceName","userService","serviceBuild",207),ImmutableSet.of(),false).toJsonMap(event);
  assertThat(map).containsOnly(entry("timestamp",timestamp),entry("thread","main"),entry("level","INFO"),entry("logger",logger),entry("message",message),entry("mdc",mdc),entry("serviceName","userService"),entry("serviceBuild",207));
}
