@Test public void testReplaceFieldName(){
  Map<String,Object> map=new EventJsonLayout(jsonFormatter,timestampFormatter,throwableProxyConverter,includes,ImmutableMap.of("timestamp","@timestamp","message","@message"),ImmutableMap.of(),ImmutableSet.of(),false).toJsonMap(event);
  assertThat(map).containsOnly(entry("@timestamp",timestamp),entry("thread","main"),entry("level","INFO"),entry("logger",logger),entry("@message",message),entry("mdc",mdc));
}
