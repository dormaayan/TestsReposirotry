@Test public void testReplaceStringFieldName(){
  assertThat(new MapBuilder(timestampFormatter,ImmutableMap.of("message","@message"),ImmutableMap.of(),size).add("message",true,message).build()).containsOnly(entry("@message",message));
}
