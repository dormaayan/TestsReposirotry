@Test @SuppressWarnings("unchecked") public void unhandledExceptionsCustomCommand() throws Exception {
  doThrow(new BadAppException()).when(command).run(any(Bootstrap.class),any(Namespace.class),any(Configuration.class));
  assertThat(cli.run("custom")).isFalse();
  assertThat(stdOut.toString()).isEqualTo(String.format("I did not expect this!%n"));
  assertThat(stdErr.toString()).isEmpty();
}
