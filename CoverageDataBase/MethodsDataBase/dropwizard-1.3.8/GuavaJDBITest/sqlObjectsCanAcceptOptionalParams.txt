@Test public void sqlObjectsCanAcceptOptionalParams() throws Exception {
  final GuavaPersonDAO dao=dbi.open(GuavaPersonDAO.class);
  assertThat(dao.findByName(Optional.of("Coda Hale"))).isEqualTo("Coda Hale");
}
