@Test public void configuresLevelForMultipleLoggers() throws Exception {
  ImmutableMultimap<String,String> parameters=ImmutableMultimap.of("logger","logger.one","logger","logger.two","level","INFO");
  task.execute(parameters,output);
  assertThat(logger1.getLevel()).isEqualTo(Level.INFO);
  assertThat(logger2.getLevel()).isEqualTo(Level.INFO);
  assertThat(stringWriter.toString()).isEqualTo(String.format("Configured logging level for logger.one to INFO%nConfigured logging level for logger.two to INFO%n"));
}
