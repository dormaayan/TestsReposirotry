static class LocalConnFactory implements NIOConnFactory<String,IOSession> {
  @Override public IOSession create(  final String route,  final IOSession session) throws IOException {
    return session;
  }
}
