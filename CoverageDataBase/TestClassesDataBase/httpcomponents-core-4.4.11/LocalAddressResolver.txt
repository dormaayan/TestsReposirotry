static class LocalAddressResolver implements SocketAddressResolver<String> {
  @Override public SocketAddress resolveLocalAddress(  final String route){
    return null;
  }
  @Override public SocketAddress resolveRemoteAddress(  final String route){
    return InetSocketAddress.createUnresolved(route,80);
  }
}
