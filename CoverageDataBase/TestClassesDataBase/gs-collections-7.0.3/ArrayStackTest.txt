@RunWith(Java8Runner.class) public class ArrayStackTest implements MutableStackTestCase {
  @SafeVarargs @Override public final <T>MutableStack<T> newWith(  T... elements){
    return ArrayStack.newStackFromTopToBottom(elements);
  }
}
