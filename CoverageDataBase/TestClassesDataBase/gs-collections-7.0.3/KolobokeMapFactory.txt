private static final class KolobokeMapFactory extends SizedMapFactory implements Function0<HashObjObjMap<Integer,String>> {
  private KolobokeMapFactory(  int size){
    super(size,0.0f);
  }
  @Override public HashObjObjMap<Integer,String> value(){
    return this.fill(HashObjObjMaps.<Integer,String>newMutableMap());
  }
}
