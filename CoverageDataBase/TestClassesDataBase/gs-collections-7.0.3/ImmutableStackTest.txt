@RunWith(Java8Runner.class) public class ImmutableStackTest implements ImmutableStackTestCase {
  @SafeVarargs @Override public final <T>ImmutableStack<T> newWith(  T... elements){
    return ArrayStack.newStackFromTopToBottom(elements).toImmutable();
  }
}
