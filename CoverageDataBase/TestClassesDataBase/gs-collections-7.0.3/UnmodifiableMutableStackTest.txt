@RunWith(Java8Runner.class) public class UnmodifiableMutableStackTest implements UnmodifiableMutableStackTestCase {
  @SafeVarargs @Override public final <T>MutableStack<T> newWith(  T... elements){
    return ArrayStack.newStackFromTopToBottom(elements).asUnmodifiable();
  }
}
