@RunWith(Java8Runner.class) public class HashBagTest implements MutableBagTestCase {
  @SafeVarargs @Override public final <T>MutableBag<T> newWith(  T... elements){
    return HashBag.newBagWith(elements);
  }
}
