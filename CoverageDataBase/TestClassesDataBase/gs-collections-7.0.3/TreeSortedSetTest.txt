@RunWith(Java8Runner.class) public class TreeSortedSetTest implements MutableSortedSetTestCase {
  @SafeVarargs @Override public final <T>MutableSortedSet<T> newWith(  T... elements){
    MutableSortedSet<T> result=TreeSortedSet.newSet(Comparators.reverseNaturalOrder());
    IterableTestCase.addAllTo(elements,result);
    return result;
  }
}
