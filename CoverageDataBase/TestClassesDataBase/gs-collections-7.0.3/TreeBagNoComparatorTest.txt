@RunWith(Java8Runner.class) public class TreeBagNoComparatorTest implements MutableSortedBagNoComparatorTestCase {
  @SafeVarargs @Override public final <T>MutableSortedBag<T> newWith(  T... elements){
    return TreeBag.newBagWith(elements);
  }
}
