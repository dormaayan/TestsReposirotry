@RunWith(Java8Runner.class) public class ImmutableSortedBagImplTest implements ImmutableSortedBagTestCase {
  @SafeVarargs @Override public final <T>ImmutableSortedBag<T> newWith(  T... elements){
    MutableSortedBag<T> result=SortedBags.mutable.with(Comparators.reverseNaturalOrder());
    IterableTestCase.addAllTo(elements,result);
    return result.toImmutable();
  }
}
