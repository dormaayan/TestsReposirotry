@RunWith(Java8Runner.class) public class FlatCollectIterableTestNoIteratorTest implements LazyNoIteratorTestCase {
  @Override public <T>LazyIterable<T> newWith(  T... elements){
    return new FlatCollectIterable<>(new FastListNoIterator<T>().with(elements),each -> FastList.newListWith(each));
  }
}
