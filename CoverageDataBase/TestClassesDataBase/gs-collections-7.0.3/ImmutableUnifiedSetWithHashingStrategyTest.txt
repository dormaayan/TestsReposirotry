@RunWith(Java8Runner.class) public class ImmutableUnifiedSetWithHashingStrategyTest implements ImmutableSetTestCase {
  @SafeVarargs @Override public final <T>ImmutableSet<T> newWith(  T... elements){
    MutableSet<T> result=UnifiedSetWithHashingStrategy.newSet(HashingStrategies.defaultStrategy());
    IterableTestCase.addAllTo(elements,result);
    return result.toImmutable();
  }
}
