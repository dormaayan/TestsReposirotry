@RunWith(Java8Runner.class) public class UnifiedSetWithHashingStrategyTest implements MutableSetTestCase {
  @SafeVarargs @Override public final <T>MutableSet<T> newWith(  T... elements){
    MutableSet<T> result=UnifiedSetWithHashingStrategy.newSet(HashingStrategies.defaultStrategy());
    IterableTestCase.addAllTo(elements,result);
    return result;
  }
}
