@RunWith(Java8Runner.class) public class RejectIterableTestNoIteratorTest implements LazyNoIteratorTestCase {
  @Override public <T>LazyIterable<T> newWith(  T... elements){
    return new RejectIterable<>(new FastListNoIterator<T>().with(elements),ignored -> false);
  }
}
