@RunWith(Java8Runner.class) public class TreeBagTest implements MutableSortedBagTestCase {
  @SafeVarargs @Override public final <T>MutableSortedBag<T> newWith(  T... elements){
    return TreeBag.newBagWith(Comparators.reverseNaturalOrder(),elements);
  }
}
