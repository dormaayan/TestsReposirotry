@RunWith(Java8Runner.class) public class ImmutableBagTest implements ImmutableBagTestCase {
  @SafeVarargs @Override public final <T>ImmutableBag<T> newWith(  T... elements){
    return HashBag.newBagWith(elements).toImmutable();
  }
}
