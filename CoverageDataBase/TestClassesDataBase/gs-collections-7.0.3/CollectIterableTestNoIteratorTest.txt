@RunWith(Java8Runner.class) public class CollectIterableTestNoIteratorTest implements LazyNoIteratorTestCase {
  @Override public <T>LazyIterable<T> newWith(  T... elements){
    return new CollectIterable<>(new FastListNoIterator<T>().with(elements),Functions.identity());
  }
}
