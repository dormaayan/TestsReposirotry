@RunWith(Java8Runner.class) public class ImmutableListTest implements ImmutableListTestCase {
  @SafeVarargs @Override public final <T>ImmutableList<T> newWith(  T... elements){
    return FastList.newListWith(elements).toImmutable();
  }
}
