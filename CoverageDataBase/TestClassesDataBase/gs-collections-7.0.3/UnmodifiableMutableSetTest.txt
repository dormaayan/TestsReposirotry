@RunWith(Java8Runner.class) public class UnmodifiableMutableSetTest implements UnmodifiableMutableSetTestCase {
  @SafeVarargs @Override public final <T>MutableSet<T> newWith(  T... elements){
    MutableSet<T> result=UnifiedSet.newSet();
    IterableTestCase.addAllTo(elements,result);
    return result.asUnmodifiable();
  }
}
