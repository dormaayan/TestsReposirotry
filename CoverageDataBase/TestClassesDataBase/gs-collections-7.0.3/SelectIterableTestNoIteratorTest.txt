@RunWith(Java8Runner.class) public class SelectIterableTestNoIteratorTest implements LazyNoIteratorTestCase {
  @Override public <T>LazyIterable<T> newWith(  T... elements){
    return new SelectIterable<>(new FastListNoIterator<T>().with(elements),ignored -> true);
  }
}
