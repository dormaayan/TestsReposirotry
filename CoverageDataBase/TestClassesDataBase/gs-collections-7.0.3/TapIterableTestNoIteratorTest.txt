@RunWith(Java8Runner.class) public class TapIterableTestNoIteratorTest implements LazyNoIteratorTestCase {
  @Override public <T>LazyIterable<T> newWith(  T... elements){
    return new TapIterable<>(new FastListNoIterator<T>().with(elements),ignored -> {
    }
);
  }
}
