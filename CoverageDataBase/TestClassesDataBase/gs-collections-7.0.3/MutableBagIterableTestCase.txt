public interface MutableBagIterableTestCase extends MutableCollectionTestCase {
  @Override <T>MutableBagIterable<T> newWith(  T... elements);
  @Test default void MutableBag_addOccurrences_throws(){
    Verify.assertThrows(IllegalArgumentException.class,() -> this.newWith(1,2,2,3,3,3).addOccurrences(4,-1));
  }
  @Test default void MutableBag_removeOccurrences_throws(){
    Verify.assertThrows(IllegalArgumentException.class,() -> this.newWith(1,2,2,3,3,3).removeOccurrences(4,-1));
  }
}
