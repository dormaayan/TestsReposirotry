@RunWith(Java8Runner.class) public class UnmodifiableMutableListTest implements UnmodifiableMutableListTestCase {
  @SafeVarargs @Override public final <T>MutableList<T> newWith(  T... elements){
    return FastList.newListWith(elements).asUnmodifiable();
  }
}
