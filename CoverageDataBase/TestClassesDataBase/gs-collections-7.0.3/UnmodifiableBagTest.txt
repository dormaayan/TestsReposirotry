@RunWith(Java8Runner.class) public class UnmodifiableBagTest implements UnmodifiableBagTestCase {
  @SafeVarargs @Override public final <T>MutableBag<T> newWith(  T... elements){
    return HashBag.newBagWith(elements).asUnmodifiable();
  }
}
