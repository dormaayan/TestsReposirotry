@RunWith(Java8Runner.class) public class SetAdapterTest implements MutableSetTestCase {
  @SafeVarargs @Override public final <T>MutableSet<T> newWith(  T... elements){
    MutableSet<T> result=SetAdapter.adapt(new HashSet<>());
    IterableTestCase.addAllTo(elements,result);
    return result;
  }
}
