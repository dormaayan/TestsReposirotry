@RunWith(Java8Runner.class) public class FastListTest implements MutableListTestCase {
  @SafeVarargs @Override public final <T>MutableList<T> newWith(  T... elements){
    return FastList.newListWith(elements);
  }
}
