@RunWith(Java8Runner.class) public class ListAdapterTest implements MutableListTestCase {
  @SafeVarargs @Override public final <T>MutableList<T> newWith(  T... elements){
    return ListAdapter.adapt(new ArrayList<>(Arrays.asList(elements)));
  }
}
