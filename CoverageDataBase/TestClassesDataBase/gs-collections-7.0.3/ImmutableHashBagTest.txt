@RunWith(Java8Runner.class) public class ImmutableHashBagTest implements ImmutableBagTestCase {
  @SafeVarargs @Override public final <T>ImmutableBag<T> newWith(  T... elements){
    return new ImmutableHashBag<>(HashBag.newBagWith(elements));
  }
}
