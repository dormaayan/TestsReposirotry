@RunWith(Java8Runner.class) public class SelectInstancesOfIterableTestNoIteratorTest implements LazyNoIteratorTestCase {
  @Override public <T>LazyIterable<T> newWith(  T... elements){
    return (LazyIterable<T>)new SelectInstancesOfIterable<>(new FastListNoIterator<T>().with(elements),Object.class);
  }
}
