private static class HeaderMapEmitter implements HpackDecoder.HeaderEmitter {
  HeaderMap map=new HeaderMap();
  @Override public void emitHeader(  HttpString name,  String value,  boolean neverIndex){
    map.add(name,value);
  }
}
