public class Issue654 extends v4Test {
  @Test public void makeSureMapResolverIsAlwaysPresent() throws Exception {
    Context ctx=Context.newBuilder($("foo","bar")).resolver(JavaBeanValueResolver.INSTANCE).build();
    assertEquals("bar",ctx.get("foo"));
  }
  @Test public void ignoreMapResolverWhenItIsProvided() throws Exception {
    Context ctx=Context.newBuilder($("foo","bar")).resolver(MapValueResolver.INSTANCE).build();
    assertEquals("bar",ctx.get("foo"));
  }
}
