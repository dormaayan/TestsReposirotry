/** 
 * Tests for <code> {@link BooleanAssert#usingComparator(java.util.Comparator)}</code>.
 * @author Joel Costigliola
 */
public class BooleanAssert_usingComparator_Test extends BooleanAssertBaseTest {
  @Mock private Comparator<Boolean> comparator;
  @BeforeEach public void before(){
    initMocks(this);
  }
  @Override @Test @SuppressWarnings("deprecation") public void should_have_internal_effects(){
    assertThatExceptionOfType(UnsupportedOperationException.class).isThrownBy(() -> assertions.usingComparator(comparator));
  }
  @Override @Test public void should_return_this(){
  }
  @Override protected BooleanAssert invoke_api_method(){
    return null;
  }
  @Override protected void verify_internal_effects(){
  }
}
