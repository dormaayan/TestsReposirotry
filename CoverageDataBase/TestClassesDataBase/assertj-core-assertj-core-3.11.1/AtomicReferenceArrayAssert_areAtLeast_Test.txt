public class AtomicReferenceArrayAssert_areAtLeast_Test extends AtomicReferenceArrayAssertBaseTest {
  private Condition<Object> condition;
  @BeforeEach public void before(){
    condition=new TestCondition<>();
  }
  @Override protected AtomicReferenceArrayAssert<Object> invoke_api_method(){
    return assertions.areAtLeast(2,condition);
  }
  @Override protected void verify_internal_effects(){
    verify(arrays).assertAreAtLeast(info(),internalArray(),2,condition);
  }
}
