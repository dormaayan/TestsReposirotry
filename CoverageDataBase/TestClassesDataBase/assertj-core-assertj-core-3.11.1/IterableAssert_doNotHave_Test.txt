/** 
 * Tests for <code> {@link AbstractIterableAssert#doNotHave(org.assertj.core.api.Condition)}</code>.
 * @author Nicolas François
 */
public class IterableAssert_doNotHave_Test extends IterableAssertBaseTest {
  private static Condition<Object> condition;
  @BeforeAll public static void beforeOnce(){
    condition=new TestCondition<>();
  }
  @Override protected ConcreteIterableAssert<Object> invoke_api_method(){
    return assertions.doNotHave(condition);
  }
  @Override protected void verify_internal_effects(){
    verify(iterables).assertDoNotHave(getInfo(assertions),getActual(assertions),condition);
  }
}
