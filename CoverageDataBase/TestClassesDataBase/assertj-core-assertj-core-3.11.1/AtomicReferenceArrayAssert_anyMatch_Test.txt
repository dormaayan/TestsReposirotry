public class AtomicReferenceArrayAssert_anyMatch_Test extends AtomicReferenceArrayAssertBaseTest {
  private Predicate<Object> predicate;
  @BeforeEach public void beforeOnce(){
    predicate=o -> o != null;
  }
  @Override protected AtomicReferenceArrayAssert<Object> invoke_api_method(){
    return assertions.anyMatch(predicate);
  }
  @Override protected void verify_internal_effects(){
    verify(iterables).assertAnyMatch(info(),newArrayList(internalArray()),predicate,PredicateDescription.GIVEN);
  }
}
