/** 
 * Tests for <code> {@link org.assertj.core.api.AbstractIterableAssert#have(org.assertj.core.api.Condition)}</code>.
 * @author Nicolas François
 */
public class IterableAssert_have_Test extends IterableAssertBaseTest {
  private static Condition<Object> condition;
  @BeforeAll public static void beforeOnce(){
    condition=new TestCondition<>();
  }
  @Override protected ConcreteIterableAssert<Object> invoke_api_method(){
    return assertions.have(condition);
  }
  @Override protected void verify_internal_effects(){
    verify(iterables).assertHave(getInfo(assertions),getActual(assertions),condition);
  }
}
