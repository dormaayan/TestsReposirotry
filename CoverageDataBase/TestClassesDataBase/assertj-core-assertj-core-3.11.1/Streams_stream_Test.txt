public class Streams_stream_Test {
  @Test public void should_build_stream_from_iterable(){
    Iterable<String> iterable=list("a","b","c");
    Stream<String> stream=stream(iterable);
    assertThat(stream).containsExactly("a","b","c");
  }
}
