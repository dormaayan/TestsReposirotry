@Test public void messageQueueTest(){
  MessageQueue msgQueue=new MessageQueue();
  msgQueue.submitMsg(new Message("MessageQueue Test"));
  assertEquals(msgQueue.retrieveMsg().getMsg(),"MessageQueue Test");
}
