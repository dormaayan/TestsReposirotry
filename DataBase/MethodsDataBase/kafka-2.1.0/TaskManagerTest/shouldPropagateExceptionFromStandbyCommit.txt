@Test public void shouldPropagateExceptionFromStandbyCommit(){
  EasyMock.expect(standby.commit()).andThrow(new RuntimeException(""));
  replay();
  try {
    taskManager.commitAll();
    fail("should have thrown exception");
  }
 catch (  final Exception e) {
  }
  verify(standby);
}
