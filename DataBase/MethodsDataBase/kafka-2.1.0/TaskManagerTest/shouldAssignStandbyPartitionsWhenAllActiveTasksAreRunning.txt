@Test public void shouldAssignStandbyPartitionsWhenAllActiveTasksAreRunning(){
  mockAssignStandbyPartitions(1L);
  replay();
  assertTrue(taskManager.updateNewAndRestoringTasks());
  verify(restoreConsumer);
}
