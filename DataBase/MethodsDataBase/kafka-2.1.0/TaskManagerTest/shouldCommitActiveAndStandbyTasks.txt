@Test public void shouldCommitActiveAndStandbyTasks(){
  EasyMock.expect(active.commit()).andReturn(1);
  EasyMock.expect(standby.commit()).andReturn(2);
  replay();
  assertThat(taskManager.commitAll(),equalTo(3));
  verify(active,standby);
}
