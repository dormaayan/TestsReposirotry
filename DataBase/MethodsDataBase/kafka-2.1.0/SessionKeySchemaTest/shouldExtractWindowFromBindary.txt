@Test public void shouldExtractWindowFromBindary(){
  final byte[] serialized=SessionKeySchema.toBinary(windowedKey,serde.serializer(),"dummy");
  assertEquals(window,SessionKeySchema.extractWindow(serialized));
}
