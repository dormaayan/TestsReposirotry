@Test public void shouldExtractEndTimeFromBinary(){
  final byte[] serialized=SessionKeySchema.toBinary(windowedKey,serde.serializer(),"dummy");
  assertEquals(endTime,SessionKeySchema.extractEndTimestamp(serialized));
}
