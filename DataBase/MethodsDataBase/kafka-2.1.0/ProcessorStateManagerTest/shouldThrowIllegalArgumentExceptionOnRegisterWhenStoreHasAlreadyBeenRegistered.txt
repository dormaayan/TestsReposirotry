@Test public void shouldThrowIllegalArgumentExceptionOnRegisterWhenStoreHasAlreadyBeenRegistered() throws IOException {
  final ProcessorStateManager stateManager=new ProcessorStateManager(taskId,noPartitions,false,stateDirectory,Collections.emptyMap(),changelogReader,false,logContext);
  stateManager.register(mockKeyValueStore,null);
  try {
    stateManager.register(mockKeyValueStore,null);
    fail("should have thrown illegal argument exception when store with same name already registered");
  }
 catch (  final IllegalArgumentException e) {
  }
}
