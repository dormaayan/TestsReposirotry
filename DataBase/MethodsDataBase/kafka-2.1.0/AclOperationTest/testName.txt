@Test public void testName() throws Exception {
  for (  AclOperationTestInfo info : INFOS) {
    assertEquals("AclOperation.fromString(" + info.name + ") was supposed to be "+ info.operation,info.operation,AclOperation.fromString(info.name));
  }
  assertEquals(AclOperation.UNKNOWN,AclOperation.fromString("something"));
}
