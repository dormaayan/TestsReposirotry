@Test public void shouldLogChangesOnPutAll(){
  store.putAll(Arrays.asList(KeyValue.pair(hi,there),KeyValue.pair(hello,world)));
  assertThat((byte[])sent.get(hi),equalTo(there));
  assertThat((byte[])sent.get(hello),equalTo(world));
}
