@Test(expected=SchemaBuilderException.class) public void testMapBuilderInvalidDefault(){
  Map<Byte,String> defMap=Collections.singletonMap((byte)5,"foo");
  SchemaBuilder.map(Schema.INT8_SCHEMA,Schema.INT8_SCHEMA).defaultValue(defMap).build();
}
