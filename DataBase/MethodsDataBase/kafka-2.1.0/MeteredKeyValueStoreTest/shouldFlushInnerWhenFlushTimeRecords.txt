@Test public void shouldFlushInnerWhenFlushTimeRecords(){
  inner.flush();
  EasyMock.expectLastCall().once();
  init();
  metered.flush();
  final KafkaMetric metric=metric("flush-rate");
  assertTrue((Double)metric.metricValue() > 0);
  EasyMock.verify(inner);
}
