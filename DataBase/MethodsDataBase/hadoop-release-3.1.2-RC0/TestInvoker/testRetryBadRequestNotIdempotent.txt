/** 
 * Repeatedly retry until eventually a bad request succeeds.
 */
@Test(expected=AWSBadRequestException.class) public void testRetryBadRequestNotIdempotent() throws Throwable {
  invoker.retry("test",null,false,() -> {
    throw BAD_REQUEST;
  }
);
}
