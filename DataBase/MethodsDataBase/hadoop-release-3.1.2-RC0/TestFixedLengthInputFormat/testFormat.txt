/** 
 * 20 random tests of various record, file, and split sizes.  All tests have uncompressed file as input.
 */
@Test(timeout=500000) public void testFormat() throws Exception {
  runRandomTests(null);
}
