/** 
 * First put a node in maintenance, then put a different node in decommission. Make sure decommission process take maintenance replica into account.
 */
@Test(timeout=360000) public void testDecommissionDifferentNodeAfterMaintenances() throws Exception {
  testDecommissionDifferentNodeAfterMaintenance(2);
  testDecommissionDifferentNodeAfterMaintenance(3);
  testDecommissionDifferentNodeAfterMaintenance(4);
}
