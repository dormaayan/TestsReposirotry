/** 
 * Test  {@link DFSUtil#getNamenodeNameServiceId(Configuration)} to ensurenameserviceId for namenode is determined based on matching the address with local node's address
 */
@Test public void getNameNodeNameServiceId(){
  Configuration conf=setupAddress(DFS_NAMENODE_RPC_ADDRESS_KEY);
  assertEquals("nn1",DFSUtil.getNamenodeNameServiceId(conf));
}
