/** 
 * Test  {@link DFSUtil#getBackupNameServiceId(Configuration)} to ensurenameserviceId for backup node is determined based on matching the address with local node's address
 */
@Test public void getBackupNameServiceId(){
  Configuration conf=setupAddress(DFS_NAMENODE_BACKUP_ADDRESS_KEY);
  assertEquals("nn1",DFSUtil.getBackupNameServiceId(conf));
}
