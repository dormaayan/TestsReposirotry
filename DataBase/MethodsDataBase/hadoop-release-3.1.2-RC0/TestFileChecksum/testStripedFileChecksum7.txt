@Test(timeout=90000) public void testStripedFileChecksum7() throws Exception {
  int length=-1;
  prepareTestFiles(fileSize,new String[]{stripedFile1,stripedFile2});
  testStripedFileChecksum(length,fileSize);
}
