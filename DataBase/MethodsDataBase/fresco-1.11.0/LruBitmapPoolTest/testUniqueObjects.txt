@Test public void testUniqueObjects(){
  Bitmap one=Bitmap.createBitmap(4,4,Bitmap.Config.RGB_565);
  mPool.release(one);
  mPool.release(one);
  mPool.release(one);
  assertEquals(1,((LruBucketsPoolBackend)mPool.mStrategy).valueCount());
}
