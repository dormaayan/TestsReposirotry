@Test public void testReadWithByteArray() throws IOException {
  byte[] readBuffer=new byte[5];
  assertEquals(5,mPooledByteArrayBufferedInputStream.read(readBuffer));
  assertFilledWithConsecutiveBytes(readBuffer,0,5,0);
}
