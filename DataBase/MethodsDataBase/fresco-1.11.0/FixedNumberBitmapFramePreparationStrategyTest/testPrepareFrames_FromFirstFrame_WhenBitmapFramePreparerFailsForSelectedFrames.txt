@Test public void testPrepareFrames_FromFirstFrame_WhenBitmapFramePreparerFailsForSelectedFrames() throws Exception {
  when(mBitmapFramePreparer.prepareFrame(eq(mBitmapFrameCache),eq(mAnimationBackend),eq(2))).thenReturn(false);
  when(mBitmapFramePreparer.prepareFrame(eq(mBitmapFrameCache),eq(mAnimationBackend),eq(3))).thenReturn(false);
  mBitmapFramePreparationStrategy.prepareFrames(mBitmapFramePreparer,mBitmapFrameCache,mAnimationBackend,0);
  verifyPrepareCalledForFramesInOrder(1,2);
}
