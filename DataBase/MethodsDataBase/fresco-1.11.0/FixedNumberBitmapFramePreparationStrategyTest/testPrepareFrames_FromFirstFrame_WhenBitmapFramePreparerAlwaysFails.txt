@Test public void testPrepareFrames_FromFirstFrame_WhenBitmapFramePreparerAlwaysFails() throws Exception {
  when(mBitmapFramePreparer.prepareFrame(eq(mBitmapFrameCache),eq(mAnimationBackend),anyInt())).thenReturn(false);
  mBitmapFramePreparationStrategy.prepareFrames(mBitmapFramePreparer,mBitmapFrameCache,mAnimationBackend,0);
  verifyPrepareCalledForFramesInOrder(1);
}
