@Test public void testOnCancellationDoesNotThrow(){
  doThrow(new RuntimeException()).when(mDelegatedConsumer).onCancellation();
  mBaseConsumer.onCancellation();
  verify(mDelegatedConsumer).onCancellation();
}
