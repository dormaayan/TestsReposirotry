@Test public void testStatusHasFlag(){
  assertThat(BaseConsumer.statusHasFlag(Consumer.IS_PLACEHOLDER | Consumer.IS_LAST,Consumer.IS_PLACEHOLDER)).isTrue();
  assertThat(BaseConsumer.statusHasFlag(Consumer.DO_NOT_CACHE_ENCODED | Consumer.IS_LAST,Consumer.IS_PLACEHOLDER)).isFalse();
}
