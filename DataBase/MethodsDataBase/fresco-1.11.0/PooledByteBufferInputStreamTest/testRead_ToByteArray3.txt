@Test public void testRead_ToByteArray3(){
  byte[] buf=new byte[BYTES.length - 1];
  assertEquals(buf.length,mStream.read(buf));
  assertEquals(buf.length,mStream.mOffset);
  assertArrayEquals(BYTES,buf,buf.length);
}
