@Test public void testWithLargeEnoughImageWhenNoResizeOptions(){
  for (int rotation=0; rotation < 360; rotation+=90) {
    assertTrue(ThumbnailSizeChecker.isImageBigEnough(mockImage(BIG_ENOUGH_SIZE_FOR_NO_RESIZE_OPTIONS,BIG_ENOUGH_SIZE_FOR_NO_RESIZE_OPTIONS,rotation),null));
  }
}
