@Test public void testImageWithInsufficientWidthWhenNoResizeOptions(){
  for (int rotation=0; rotation < 360; rotation+=90) {
    EncodedImage mockImage=mockImage(BIG_ENOUGH_SIZE_FOR_NO_RESIZE_OPTIONS - 1,BIG_ENOUGH_SIZE_FOR_NO_RESIZE_OPTIONS,rotation);
    assertFalse(ThumbnailSizeChecker.isImageBigEnough(mockImage,null));
  }
}
