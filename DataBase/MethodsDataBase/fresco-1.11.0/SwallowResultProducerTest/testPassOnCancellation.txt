@Test public void testPassOnCancellation(){
  setupInputProducerCancellation();
  mSwallowResultProducer.produceResults(mConsumer,mProducerContext);
  verify(mConsumer).onCancellation();
  verifyNoMoreInteractions(mConsumer);
}
