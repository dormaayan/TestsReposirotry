@Test public void testPassOnFailure(){
  setupInputProducerFailure();
  mSwallowResultProducer.produceResults(mConsumer,mProducerContext);
  verify(mConsumer).onFailure(mException);
  verifyNoMoreInteractions(mConsumer);
}
