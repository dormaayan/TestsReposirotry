@Test public void test_F_F_a_C(){
  testFailure(null,1);
  mInternalConsumer.onFailure(mock(Throwable.class));
  verifyFailed(null,mException);
  testSubscribe(ON_FAILURE);
  testClose(mException);
}
