@Test public void test_L_F_a_C(){
  testNewResult(mResultRef1,LAST,1);
  mInternalConsumer.onFailure(mException);
  verifyWithResult(mResultRef1,LAST);
  testSubscribe(ON_NEW_RESULT);
  testClose(FINISHED,2);
}
