@Test public void testDoesNotCancelAfterStarted(){
  mStatefulRunnable.mState.set(StatefulRunnable.STATE_STARTED);
  mStatefulRunnable.cancel();
  verify(mStatefulRunnable,never()).onCancellation();
}
