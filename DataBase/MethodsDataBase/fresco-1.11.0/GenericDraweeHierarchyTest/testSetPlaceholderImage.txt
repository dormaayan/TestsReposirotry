@Test public void testSetPlaceholderImage() throws Exception {
  final GenericDraweeHierarchy dh=mBuilder.setPlaceholderImage(mPlaceholderImage,ScaleType.FIT_XY).build();
  testSetDrawable(dh,1,new SetDrawableCallback(){
    @Override public void setDrawable(    Drawable drawable){
      dh.setPlaceholderImage(drawable);
    }
    @Override public void setDrawable(    Drawable drawable,    ScaleType scaleType){
      dh.setPlaceholderImage(drawable,scaleType);
    }
  }
);
}
