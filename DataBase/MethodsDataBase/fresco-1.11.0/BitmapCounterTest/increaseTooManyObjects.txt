@Test() public void increaseTooManyObjects(){
  for (int i=0; i < MAX_COUNT; ++i) {
    mBitmapCounter.increase(bitmapForSize(1));
  }
  assertFalse(mBitmapCounter.increase(bitmapForSize(1)));
  assertState(MAX_COUNT,MAX_COUNT);
}
