@Test(expected=IllegalArgumentException.class) public void testDecreaseTooMany(){
  assertTrue(mBitmapCounter.increase(bitmapForSize(2)));
  mBitmapCounter.decrease(bitmapForSize(1));
  mBitmapCounter.decrease(bitmapForSize(1));
}
