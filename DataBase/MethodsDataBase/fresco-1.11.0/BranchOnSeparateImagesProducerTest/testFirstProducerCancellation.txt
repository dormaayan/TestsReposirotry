@Test public void testFirstProducerCancellation(){
  mFirstProducerConsumer.onCancellation();
  verify(mConsumer).onCancellation();
  verify(mInputProducer2,never()).produceResults(any(Consumer.class),eq(mProducerContext));
}
