@Test public void testInvalidCipherOpenSSL() throws Exception {
  Assume.assumeTrue(OpenSsl.isAvailable());
  try {
    testInvalidCipher(SslProvider.OPENSSL);
    if (!OpenSsl.versionString().contains("1.1.1")) {
      fail();
    }
  }
 catch (  SSLException expected) {
  }
}
