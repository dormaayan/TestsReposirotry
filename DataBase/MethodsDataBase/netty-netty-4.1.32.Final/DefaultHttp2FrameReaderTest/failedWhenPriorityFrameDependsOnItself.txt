@Test(expected=Http2Exception.class) public void failedWhenPriorityFrameDependsOnItself() throws Http2Exception {
  ByteBuf input=Unpooled.buffer();
  try {
    writePriorityFrame(input,1,1,10);
    frameReader.readFrame(ctx,input,listener);
  }
  finally {
    input.release();
  }
}
