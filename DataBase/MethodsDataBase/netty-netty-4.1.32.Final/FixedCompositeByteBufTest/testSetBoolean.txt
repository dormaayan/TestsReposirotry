@Test(expected=ReadOnlyBufferException.class) public void testSetBoolean(){
  ByteBuf buf=newBuffer(wrappedBuffer(new byte[8]));
  try {
    buf.setBoolean(0,true);
  }
  finally {
    buf.release();
  }
}
