@Test(expected=ReadOnlyBufferException.class) public void testSetChar() throws IOException {
  ByteBuf buf=newBuffer(wrappedBuffer(new byte[8]));
  try {
    buf.setChar(0,'b');
  }
  finally {
    buf.release();
  }
}
