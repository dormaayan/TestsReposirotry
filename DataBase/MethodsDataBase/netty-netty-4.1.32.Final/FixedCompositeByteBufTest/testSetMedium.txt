@Test(expected=ReadOnlyBufferException.class) public void testSetMedium() throws IOException {
  ByteBuf buf=newBuffer(wrappedBuffer(new byte[8]));
  try {
    buf.setMedium(0,1);
  }
  finally {
    buf.release();
  }
}
