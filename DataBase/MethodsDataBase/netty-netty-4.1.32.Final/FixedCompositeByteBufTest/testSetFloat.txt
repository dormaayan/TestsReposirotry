@Test(expected=ReadOnlyBufferException.class) public void testSetFloat() throws IOException {
  ByteBuf buf=newBuffer(wrappedBuffer(new byte[8]));
  try {
    buf.setFloat(0,1);
  }
  finally {
    buf.release();
  }
}
