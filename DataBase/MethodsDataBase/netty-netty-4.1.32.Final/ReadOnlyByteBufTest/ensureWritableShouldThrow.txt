@Test(expected=ReadOnlyBufferException.class) public void ensureWritableShouldThrow(){
  ByteBuf buf=buffer(1);
  ByteBuf readOnly=buf.asReadOnly();
  try {
    readOnly.ensureWritable(1);
    fail();
  }
  finally {
    buf.release();
  }
}
