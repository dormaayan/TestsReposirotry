@Test public void shouldIndicateNotWritableAnyNumber(){
  ByteBuf buf=buffer(allocate(8).asReadOnlyBuffer()).clear();
  try {
    Assert.assertFalse(buf.isWritable(1));
  }
  finally {
    buf.release();
  }
}
