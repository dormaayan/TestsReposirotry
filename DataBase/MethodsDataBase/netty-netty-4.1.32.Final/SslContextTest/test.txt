@Test(expected=CertificateException.class) public void test() throws CertificateException {
  SslContext.toX509Certificates(new File(getClass().getResource("ec_params_unsupported.pem").getFile()));
}
