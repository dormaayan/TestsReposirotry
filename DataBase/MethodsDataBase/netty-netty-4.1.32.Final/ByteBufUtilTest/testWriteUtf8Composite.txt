@Test public void testWriteUtf8Composite(){
  String utf8="Some UTF-8 like äÄ∏ŒŒ";
  ByteBuf buf=Unpooled.buffer(16);
  buf.writeBytes(utf8.getBytes(CharsetUtil.UTF_8));
  ByteBuf buf2=Unpooled.compositeBuffer().addComponent(Unpooled.buffer(8)).addComponent(Unpooled.buffer(24));
  buf2.writeByte(1);
  ByteBufUtil.writeUtf8(buf2,utf8);
  assertEquals(buf,buf2.skipBytes(1));
  buf.release();
  buf2.release();
}
