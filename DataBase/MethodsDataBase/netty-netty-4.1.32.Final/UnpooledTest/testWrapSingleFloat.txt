@Test public void testWrapSingleFloat(){
  ByteBuf buffer=copyFloat(42);
  assertEquals(4,buffer.capacity());
  assertEquals(42,buffer.readFloat(),0.01);
  assertFalse(buffer.isReadable());
  buffer.release();
}
