/** 
 * Check that removing a marker that isn't assigned to the plot returns false.
 */
@Test public void testRemoveDomainMarker(){
  XYPlot plot=new XYPlot();
  assertFalse(plot.removeDomainMarker(new ValueMarker(0.5)));
}
