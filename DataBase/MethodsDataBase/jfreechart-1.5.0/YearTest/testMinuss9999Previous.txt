/** 
 * Set up a year equal to 1900.  Request the previous year, it should be null.
 */
@Test public void testMinuss9999Previous(){
  Year current=new Year(-9999);
  Year previous=(Year)current.previous();
  assertNull(previous);
}
