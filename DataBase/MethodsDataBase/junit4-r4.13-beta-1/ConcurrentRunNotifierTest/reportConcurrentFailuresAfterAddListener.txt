/** 
 * Verifies that listeners added while tests are run concurrently are notified about test failures.
 */
@Test public void reportConcurrentFailuresAfterAddListener() throws Exception {
  new AbstractConcurrentFailuresTest(){
    @Override protected void addListener(    ExaminedListener listener){
      fNotifier.addListener(listener);
    }
  }
.test();
}
