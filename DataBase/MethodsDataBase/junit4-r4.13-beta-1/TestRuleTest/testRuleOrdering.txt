@Test public void testRuleOrdering(){
  ruleLog.setLength(0);
  Result result=JUnitCore.runClasses(TestRuleOrdering.class);
  assertTrue(result.wasSuccessful());
  assertEquals(" outer.begin inner.begin foo inner.end outer.end",ruleLog.toString());
}
