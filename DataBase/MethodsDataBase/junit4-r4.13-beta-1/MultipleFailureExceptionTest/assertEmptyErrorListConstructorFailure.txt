@Test public void assertEmptyErrorListConstructorFailure(){
  try {
    new MultipleFailureException(Collections.<Throwable>emptyList());
    fail();
  }
 catch (  IllegalArgumentException expected) {
    assertThat(expected.getMessage(),containsString("List of Throwables must not be empty"));
  }
}
