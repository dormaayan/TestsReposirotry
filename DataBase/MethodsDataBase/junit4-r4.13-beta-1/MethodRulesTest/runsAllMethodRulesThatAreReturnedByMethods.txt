/** 
 * If there are multiple public methods annotated with @Rule returning a  {@link MethodRule}then all the rules returned should be run. <p>This case has been added with  <a href="https://github.com/junit-team/junit4/issues/589">Issue #589</a> -  Support @Rule for methods works only for TestRule but not for MethodRule
 */
@Test public void runsAllMethodRulesThatAreReturnedByMethods(){
  runCount=0;
  assertThat(testResult(HasMultipleMethodsReturningMethodRule.class),isSuccessful());
  assertEquals(2,runCount);
}
