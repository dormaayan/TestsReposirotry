@Test public void compare_DifferentMajorVersions(){
  JavaVersion first=jv(1,7,0,0);
  JavaVersion second=jv(1,6,0,0);
  assertThat(first,greaterThan(second));
  assertThat(second,lessThan(first));
}
