@Test public void compare_SecondVersionUnknown(){
  JavaVersion first=jv(1,7,0,60);
  JavaVersion second=jv(1,5,0,60);
  assertThat(first,greaterThan(second));
  assertThat(second,lessThan(first));
}
