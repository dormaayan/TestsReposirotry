@Test public void compare_DifferentMaintenanceVersions(){
  JavaVersion first=jv(1,7,5,0);
  JavaVersion second=jv(1,7,1,0);
  assertThat(first,greaterThan(second));
  assertThat(second,lessThan(first));
}
