@Test public void singleWaitState_WaitUntilSecondsPath(){
  final StateMachine stateMachine=stateMachine().startAt("InitialState").state("InitialState",waitState().waitFor(secondsPath("$.seconds")).transition(end())).build();
  assertStateMachineMatches("SingleWaitStateWithSecondsPath.json",stateMachine);
}
