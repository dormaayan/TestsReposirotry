@Test public void singleFailState(){
  final StateMachine stateMachine=stateMachine().startAt("InitialState").state("InitialState",failState().comment("My fail state").cause("InternalError").error("java.lang.Exception")).build();
  assertStateMachineMatches("SingleFailState.json",stateMachine);
}
