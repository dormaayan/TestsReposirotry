@Test(expected=ValidationException.class) public void waitForTimestamp_NullDate_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",waitState().waitFor(timestamp(null)).transition(end())).build();
}
