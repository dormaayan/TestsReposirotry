@Test(expected=ValidationException.class) public void zeroTimeoutSecondsInTaskState_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).timeoutSeconds(0).resource("arn")).build();
}
