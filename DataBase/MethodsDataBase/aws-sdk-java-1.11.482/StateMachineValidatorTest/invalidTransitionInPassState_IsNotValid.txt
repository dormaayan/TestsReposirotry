@Test(expected=ValidationException.class) public void invalidTransitionInPassState_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",passState().transition(next("NoSuchState"))).build();
}
