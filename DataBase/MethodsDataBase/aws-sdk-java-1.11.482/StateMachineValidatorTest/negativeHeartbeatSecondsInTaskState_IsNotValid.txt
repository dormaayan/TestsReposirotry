@Test(expected=ValidationException.class) public void negativeHeartbeatSecondsInTaskState_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).heartbeatSeconds(-1).resource("arn")).build();
}
