@Test public void retrierInTaskState_MaxAttemptsZero_IsValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).retrier(retrier().maxAttempts(0).retryOnAllErrors()).resource("arn")).build();
}
