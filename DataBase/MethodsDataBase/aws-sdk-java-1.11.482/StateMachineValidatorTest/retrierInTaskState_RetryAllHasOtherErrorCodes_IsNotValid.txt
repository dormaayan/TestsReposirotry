@Test(expected=ValidationException.class) public void retrierInTaskState_RetryAllHasOtherErrorCodes_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).retrier(retrier().errorEquals("Foo","Bar",ErrorCodes.ALL)).resource("arn")).build();
}
