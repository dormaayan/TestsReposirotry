@Test(expected=ValidationException.class) public void waitForSecondsPath_EmptyPath_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",waitState().waitFor(secondsPath("")).transition(end())).build();
}
