@Test(expected=ValidationException.class) public void invalidTransitionInTaskState_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(next("NoSuchState")).resource("arn")).build();
}
