@Test(expected=ValidationException.class) public void retrierInTaskState_IntervalSecondsNegative_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).retrier(retrier().intervalSeconds(-1).retryOnAllErrors()).resource("arn")).build();
}
