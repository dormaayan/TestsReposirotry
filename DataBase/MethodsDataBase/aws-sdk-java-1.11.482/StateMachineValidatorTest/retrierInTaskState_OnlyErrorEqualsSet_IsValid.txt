@Test public void retrierInTaskState_OnlyErrorEqualsSet_IsValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).retrier(retrier().retryOnAllErrors()).resource("arn")).build();
}
