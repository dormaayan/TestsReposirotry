@Test public void choiceStateWithNoDefaultTransition_IsValid(){
  stateMachine().startAt("Initial").state("Initial",choiceState().choice(choice().condition(eq("$.foo","bar")).transition(next("Terminal")))).state("Terminal",succeedState()).build();
}
