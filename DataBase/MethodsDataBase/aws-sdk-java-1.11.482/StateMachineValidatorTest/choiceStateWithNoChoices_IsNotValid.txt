@Test(expected=ValidationException.class) public void choiceStateWithNoChoices_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",choiceState().defaultStateName("Terminal")).state("Terminal",succeedState()).build();
}
