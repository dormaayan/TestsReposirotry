@Test(expected=ValidationException.class) public void retrierInTaskState_IntervalSecondsZero_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).retrier(retrier().intervalSeconds(0).retryOnAllErrors()).resource("arn")).build();
}
