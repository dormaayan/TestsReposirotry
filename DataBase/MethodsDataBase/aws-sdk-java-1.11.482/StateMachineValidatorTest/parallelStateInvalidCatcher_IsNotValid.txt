@Test(expected=ValidationException.class) public void parallelStateInvalidCatcher_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",parallelState().branch(branch().startAt("InitialBranchState").state("InitialBranchState",succeedState())).transition(end()).catcher(catcher().transition(next("NoSuchState")))).build();
}
