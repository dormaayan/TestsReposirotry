@Test(expected=ValidationException.class) public void retrierInTaskState_BackoffRateNegative_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).retrier(retrier().backoffRate(-1.0).retryOnAllErrors()).resource("arn")).build();
}
