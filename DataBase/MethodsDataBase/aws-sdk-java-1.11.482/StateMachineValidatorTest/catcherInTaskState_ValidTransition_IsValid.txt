@Test public void catcherInTaskState_ValidTransition_IsValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).catcher(catcher().transition(next("Terminal")).catchAll()).resource("arn")).state("Terminal",succeedState()).build();
}
