@Test(expected=ValidationException.class) public void zeroHeartbeatSecondsInTaskState_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).heartbeatSeconds(0).resource("arn")).build();
}
