@Test(expected=ValidationException.class) public void choiceStateWithInvalidChoiceTransition_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",choiceState().choice(choice().condition(eq("$.foo","bar")).transition(next("NoSuchState"))).defaultStateName("Terminal")).state("Terminal",succeedState()).build();
}
