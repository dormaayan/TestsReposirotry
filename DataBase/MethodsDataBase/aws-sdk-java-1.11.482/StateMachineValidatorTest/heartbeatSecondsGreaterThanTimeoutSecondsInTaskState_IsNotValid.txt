@Test(expected=ValidationException.class) public void heartbeatSecondsGreaterThanTimeoutSecondsInTaskState_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).heartbeatSeconds(60).timeoutSeconds(30).resource("arn")).build();
}
