@Test public void retrierInTaskState_BackoffRateGreaterThanOne_IsValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).retrier(retrier().backoffRate(1.5).retryOnAllErrors()).resource("arn")).build();
}
