@Test(expected=ValidationException.class) public void waitForSeconds_NegativeSeconds_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",waitState().waitFor(seconds(-1)).transition(end())).build();
}
