@Test(expected=ValidationException.class) public void retrierInTaskState_MaxAttemptsNegative_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).retrier(retrier().maxAttempts(-1).retryOnAllErrors()).resource("arn")).build();
}
