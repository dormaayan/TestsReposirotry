@Test(expected=ValidationException.class) public void waitForTimestampPath_MissingPath_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",waitState().waitFor(timestampPath(null)).transition(end())).build();
}
