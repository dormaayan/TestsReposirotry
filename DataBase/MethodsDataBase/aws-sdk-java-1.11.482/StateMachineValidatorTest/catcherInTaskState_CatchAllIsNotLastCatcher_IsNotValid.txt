@Test(expected=ValidationException.class) public void catcherInTaskState_CatchAllIsNotLastCatcher_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).catcher(catcher().transition(next("Terminal")).catchAll()).catcher(catcher().transition(next("Terminal")).errorEquals("Foo","Bar")).resource("arn")).state("Terminal",succeedState()).build();
}
