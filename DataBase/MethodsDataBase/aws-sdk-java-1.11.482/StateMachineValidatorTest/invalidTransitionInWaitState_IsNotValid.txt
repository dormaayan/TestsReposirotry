@Test(expected=ValidationException.class) public void invalidTransitionInWaitState_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",waitState().waitFor(seconds(10)).transition(next("NoSuchState"))).build();
}
