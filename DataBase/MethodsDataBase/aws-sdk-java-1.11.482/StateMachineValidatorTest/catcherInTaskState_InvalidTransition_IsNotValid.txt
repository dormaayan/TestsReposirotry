@Test(expected=ValidationException.class) public void catcherInTaskState_InvalidTransition_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).catcher(catcher().transition(next("NoSuchState")).catchAll()).resource("arn")).build();
}
