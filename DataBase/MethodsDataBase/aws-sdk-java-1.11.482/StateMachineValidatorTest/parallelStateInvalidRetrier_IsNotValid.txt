@Test(expected=ValidationException.class) public void parallelStateInvalidRetrier_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",parallelState().branch(branch().startAt("InitialBranchState").state("InitialBranchState",succeedState())).transition(end()).retrier(retrier().intervalSeconds(-1))).build();
}
