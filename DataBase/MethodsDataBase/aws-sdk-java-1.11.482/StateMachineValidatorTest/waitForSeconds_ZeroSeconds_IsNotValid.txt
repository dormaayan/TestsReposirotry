@Test(expected=ValidationException.class) public void waitForSeconds_ZeroSeconds_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",waitState().waitFor(seconds(0)).transition(end())).build();
}
