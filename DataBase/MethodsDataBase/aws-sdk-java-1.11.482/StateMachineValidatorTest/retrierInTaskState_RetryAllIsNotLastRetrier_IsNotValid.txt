@Test(expected=ValidationException.class) public void retrierInTaskState_RetryAllIsNotLastRetrier_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).retrier(retrier().retryOnAllErrors()).retrier(retrier().errorEquals("Foo","Bar")).resource("arn")).build();
}
