@Test public void retrierInTaskState_IntervalSecondsPositive_IsValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).retrier(retrier().intervalSeconds(10).retryOnAllErrors()).resource("arn")).build();
}
