@Test(expected=ValidationException.class) public void catcherInTaskState_CatchAllHasOtherErrorCodes_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",taskState().transition(end()).catcher(catcher().transition(next("Terminal")).errorEquals("Foo","Bar",ErrorCodes.ALL)).resource("arn")).state("Terminal",succeedState()).build();
}
