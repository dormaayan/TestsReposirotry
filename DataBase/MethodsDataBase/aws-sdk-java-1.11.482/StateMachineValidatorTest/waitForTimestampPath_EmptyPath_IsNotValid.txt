@Test(expected=ValidationException.class) public void waitForTimestampPath_EmptyPath_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",waitState().waitFor(timestampPath("")).transition(end())).build();
}
