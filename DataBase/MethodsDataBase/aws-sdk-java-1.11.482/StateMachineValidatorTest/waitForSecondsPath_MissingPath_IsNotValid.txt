@Test(expected=ValidationException.class) public void waitForSecondsPath_MissingPath_IsNotValid(){
  stateMachine().startAt("Initial").state("Initial",waitState().waitFor(secondsPath(null)).transition(end())).build();
}
