@Test public void choiceStateWithExplicitNullPaths_HasExplicitJsonNullInJson(){
  final StateMachine stateMachine=stateMachine().startAt("InitialState").state("InitialState",choiceState().inputPath(null).outputPath(null).choice(choice().condition(eq("$.foo","val")).transition(next("EndState")))).state("EndState",succeedState()).build();
  assertStateMachineMatches("ChoiceStateWithExplicitNullPaths.json",stateMachine);
}
