@Test public void succeedStateWithNonNullPaths_HasCorrectPathsInJson(){
  final StateMachine stateMachine=stateMachine().startAt("InitialState").state("InitialState",succeedState().inputPath("$.input").outputPath("$.output")).build();
  assertStateMachineMatches("SucceedStateWithNonNullPaths.json",stateMachine);
}
