@Test public void catcherWithExplicitNullPaths_HasExplicitJsonNullInJson(){
  final StateMachine stateMachine=stateMachine().startAt("InitialState").state("InitialState",taskState().resource("resource-arn").catcher(catcher().catchAll().resultPath(null).transition(next("EndState"))).transition(end())).state("EndState",succeedState()).build();
  assertStateMachineMatches("CatcherWithExplicitNullPaths.json",stateMachine);
}
