@Test public void waitStateWithNoPathsProvided_DoesNotHavePathFieldsInJson(){
  final StateMachine stateMachine=stateMachine().startAt("InitialState").state("InitialState",waitState().waitFor(seconds(4)).transition(end())).build();
  assertStateMachineMatches("WaitStateWithNoExplicitPaths.json",stateMachine);
}
