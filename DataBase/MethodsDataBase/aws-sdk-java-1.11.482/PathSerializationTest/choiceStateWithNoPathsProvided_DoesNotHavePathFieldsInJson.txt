@Test public void choiceStateWithNoPathsProvided_DoesNotHavePathFieldsInJson(){
  final StateMachine stateMachine=stateMachine().startAt("InitialState").state("InitialState",choiceState().choice(choice().condition(eq("$.foo","val")).transition(next("EndState")))).state("EndState",succeedState()).build();
  assertStateMachineMatches("ChoiceStateWithNoExplicitPaths.json",stateMachine);
}
