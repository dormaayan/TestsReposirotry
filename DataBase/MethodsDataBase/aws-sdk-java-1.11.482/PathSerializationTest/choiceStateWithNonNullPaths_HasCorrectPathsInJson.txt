@Test public void choiceStateWithNonNullPaths_HasCorrectPathsInJson(){
  final StateMachine stateMachine=stateMachine().startAt("InitialState").state("InitialState",choiceState().inputPath("$.input").outputPath("$.output").choice(choice().condition(eq("$.foo","val")).transition(next("EndState")))).state("EndState",succeedState()).build();
  assertStateMachineMatches("ChoiceStateWithNonNullPaths.json",stateMachine);
}
