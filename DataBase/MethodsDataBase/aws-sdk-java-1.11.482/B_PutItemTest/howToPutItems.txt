@Test public void howToPutItems(){
  Table table=dynamo.getTable(TABLE_NAME);
  Item item=newItem();
  int intAttrVal=item.getInt("intAttr");
  for (int i=1; i <= 10; i++) {
    PutItemOutcome result=table.putItem(item.withInt(RANGE_KEY_NAME,i).withInt("intAttr",intAttrVal + i));
    System.out.println(result);
  }
}
