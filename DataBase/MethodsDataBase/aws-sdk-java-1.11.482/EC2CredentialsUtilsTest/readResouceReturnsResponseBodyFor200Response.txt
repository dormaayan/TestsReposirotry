/** 
 * When server returns with status code 200, the test successfully returns the body from the response.
 */
@Test public void readResouceReturnsResponseBodyFor200Response() throws IOException {
  generateStub(200,SUCCESS_BODY);
  assertEquals(SUCCESS_BODY,ec2CredentialsUtils.readResource(endpoint));
}
