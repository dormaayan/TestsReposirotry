@Test public void pathAnyMatcherFailureState() throws Exception {
  JsonNode expectedResult=mapper.readTree("\"terminated\"");
  Assert.assertFalse("Expected result not equals final result",new AcceptorPathMatcher().pathAny(expectedResult,mapper.readTree("[\"running\", \"running\", \"running\", \"running\"]")));
}
