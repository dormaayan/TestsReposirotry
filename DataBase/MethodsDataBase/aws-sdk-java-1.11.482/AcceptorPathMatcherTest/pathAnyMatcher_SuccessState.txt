@Test public void pathAnyMatcher_SuccessState() throws Exception {
  JsonNode expectedResult=mapper.readTree("\"terminated\"");
  Assert.assertTrue("Expected result not equals final result",new AcceptorPathMatcher().pathAny(expectedResult,mapper.readTree("[\"running\", \"terminated\", \"running\", \"running\"]")));
}
