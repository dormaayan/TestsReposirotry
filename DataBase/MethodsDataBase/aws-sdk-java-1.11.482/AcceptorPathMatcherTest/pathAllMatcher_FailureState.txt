@Test public void pathAllMatcher_FailureState() throws Exception {
  JsonNode expectedResult=mapper.readTree("\"running\"");
  Assert.assertFalse("Expected result not equals final result",new AcceptorPathMatcher().pathAll(expectedResult,mapper.readTree("[\"running\", \"terminated\", \"running\", \"running\"]")));
}
