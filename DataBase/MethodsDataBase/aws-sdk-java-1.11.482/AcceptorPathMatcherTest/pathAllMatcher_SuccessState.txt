@Test public void pathAllMatcher_SuccessState() throws Exception {
  JsonNode expectedResult=mapper.readTree("\"running\"");
  Assert.assertTrue("Expected result not equals final result",new AcceptorPathMatcher().pathAll(expectedResult,mapper.readTree("[\"running\", \"running\", \"running\", \"running\"]")));
}
