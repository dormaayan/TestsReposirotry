@Test public void defaultParams(){
  CredentialsEndpointRetryParameters params=CredentialsEndpointRetryParameters.builder().build();
  assertNull(params.getStatusCode());
  assertNull(params.getException());
}
