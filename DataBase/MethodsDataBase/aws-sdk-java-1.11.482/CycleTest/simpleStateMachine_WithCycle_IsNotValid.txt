@Test(expected=ValidationException.class) public void simpleStateMachine_WithCycle_IsNotValid(){
  assertCycle(stateMachine().startAt("Initial").state("Initial",passState().transition(next("Next"))).state("Next",passState().transition(next("Initial"))));
}
