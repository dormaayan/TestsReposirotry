@Test public void testEmptySexp() throws IOException {
  IonParser parser=parse("()");
  assertEquals(JsonToken.START_ARRAY,parser.nextToken());
  assertEquals(JsonToken.END_ARRAY,parser.nextToken());
  assertNull(parser.nextToken());
}
