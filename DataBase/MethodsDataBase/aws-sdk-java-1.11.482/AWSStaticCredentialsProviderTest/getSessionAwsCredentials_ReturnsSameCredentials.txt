@Test public void getSessionAwsCredentials_ReturnsSameCredentials() throws Exception {
  final BasicSessionCredentials credentials=new BasicSessionCredentials("akid","skid","token");
  final AWSCredentials actualCredentials=new AWSStaticCredentialsProvider(credentials).getCredentials();
  assertEquals(credentials,actualCredentials);
}
