@Test public void getBasicAwsCredentials_ReturnsSameCredentials() throws Exception {
  final BasicAWSCredentials credentials=new BasicAWSCredentials("akid","skid");
  final AWSCredentials actualCredentials=new AWSStaticCredentialsProvider(credentials).getCredentials();
  assertEquals(credentials,actualCredentials);
}
