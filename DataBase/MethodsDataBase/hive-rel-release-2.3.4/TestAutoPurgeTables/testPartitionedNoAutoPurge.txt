/** 
 * Tests when auto.purge is set to false on a partitioned table, older data is moved to Trash when insert overwrite table .. is run
 * @throws Exception
 */
@Test public void testPartitionedNoAutoPurge() throws Exception {
  LOG.info("Running " + name.getMethodName());
  testUtil("false",false,true,false,name.getMethodName());
}
