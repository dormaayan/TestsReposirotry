/** 
 * Tests when auto.purge is set to false, older data is moved to Trash when insert overwrite table .. is run
 * @throws Exception
 */
@Test public void testNoAutoPurge() throws Exception {
  LOG.info("Running " + name.getMethodName());
  testUtil("false",false,false,false,name.getMethodName());
}
