/** 
 * Tests when auto.purge is set to false on a external table, older data is moved to Trash when insert overwrite table .. is run
 * @throws Exception
 */
@Test public void testExternalNoAutoPurge() throws Exception {
  LOG.info("Running " + name.getMethodName());
  testUtil("false",true,false,false,name.getMethodName());
}
