@Test public void testMultiplySpecial(){
  Random r=new Random(1050);
  for (  BigDecimal bigDecimal : specialBigDecimals) {
    for (    BigDecimal bigDecimal2 : specialBigDecimals) {
      doTestMultiply(r,bigDecimal,bigDecimal2);
    }
  }
}
