@Test public void testSQL11ReservedKeyWords_PRIMARY(){
  try {
    parse("CREATE TABLE PRIMARY (col STRING)");
    Assert.assertFalse("Expected ParseException",true);
  }
 catch (  ParseException ex) {
    Assert.assertEquals("Failure didn't match.","line 1:13 cannot recognize input near 'PRIMARY' '(' 'col' in table name",ex.getMessage());
  }
}
