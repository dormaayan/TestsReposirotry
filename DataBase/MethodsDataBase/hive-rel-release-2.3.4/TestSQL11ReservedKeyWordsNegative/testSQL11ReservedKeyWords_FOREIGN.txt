@Test public void testSQL11ReservedKeyWords_FOREIGN(){
  try {
    parse("CREATE TABLE FOREIGN (col STRING)");
    Assert.assertFalse("Expected ParseException",true);
  }
 catch (  ParseException ex) {
    Assert.assertEquals("Failure didn't match.","line 1:13 cannot recognize input near 'FOREIGN' '(' 'col' in table name",ex.getMessage());
  }
}
