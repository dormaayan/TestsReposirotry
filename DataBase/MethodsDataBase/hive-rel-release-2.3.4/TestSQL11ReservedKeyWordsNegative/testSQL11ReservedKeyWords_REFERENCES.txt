@Test public void testSQL11ReservedKeyWords_REFERENCES(){
  try {
    parse("CREATE TABLE REFERENCES (col STRING)");
    Assert.assertFalse("Expected ParseException",true);
  }
 catch (  ParseException ex) {
    Assert.assertEquals("Failure didn't match.","line 1:13 cannot recognize input near 'REFERENCES' '(' 'col' in table name",ex.getMessage());
  }
}
