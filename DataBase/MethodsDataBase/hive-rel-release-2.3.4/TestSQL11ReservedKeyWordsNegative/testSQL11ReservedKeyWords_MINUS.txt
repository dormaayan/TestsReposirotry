@Test public void testSQL11ReservedKeyWords_MINUS(){
  try {
    parse("CREATE TABLE MINUS (col STRING)");
    Assert.assertFalse("Expected ParseException",true);
  }
 catch (  ParseException ex) {
    Assert.assertEquals("Failure didn't match.","line 1:13 cannot recognize input near 'MINUS' '(' 'col' in table name",ex.getMessage());
  }
}
