@Test public void testSQL11ReservedKeyWords_CONSTRAINT(){
  try {
    parse("CREATE TABLE CONSTRAINT (col STRING)");
    Assert.assertFalse("Expected ParseException",true);
  }
 catch (  ParseException ex) {
    Assert.assertEquals("Failure didn't match.","line 1:13 cannot recognize input near 'CONSTRAINT' '(' 'col' in table name",ex.getMessage());
  }
}
