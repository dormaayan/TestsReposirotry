/** 
 * Test if beeline prompts for a password when optional password option is at the beginning of arguments
 */
@Test public void testPromptPasswordOptionAsFirst() throws Throwable {
  List<String> argList=new ArrayList<>();
  argList.add("-p");
  argList.addAll(getBaseArgs(miniHS2.getBaseJdbcURL()));
  argList.add("-n");
  argList.add("hive");
  connectBeelineWithUserPrompt(argList,"hivepassword");
}
