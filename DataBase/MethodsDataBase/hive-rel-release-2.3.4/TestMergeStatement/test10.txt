@Test public void test10() throws ParseException {
  ASTNode ast=parse("MERGE INTO target USING source ON target.pk = source.pk " + "WHEN MATCHED AND a = 1 THEN DELETE " + "WHEN MATCHED THEN UPDATE set a = b "+ "WHEN NOT MATCHED AND d < e THEN INSERT VALUES(1,2)");
}
