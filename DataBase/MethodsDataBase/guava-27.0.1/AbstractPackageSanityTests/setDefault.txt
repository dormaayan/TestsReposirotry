/** 
 * Sets the default value for  {@code type}, when dummy value for a parameter of the same type needs to be created in order to invoke a method or constructor. The default value isn't used in testing  {@link Object#equals} because more than one sample instances are needed for testinginequality.
 */
protected final <T>void setDefault(Class<T> type,T value){
  tester.setDefault(type,value);
}
