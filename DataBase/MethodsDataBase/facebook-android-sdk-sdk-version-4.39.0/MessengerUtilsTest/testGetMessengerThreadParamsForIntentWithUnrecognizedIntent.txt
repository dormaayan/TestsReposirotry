@Test public void testGetMessengerThreadParamsForIntentWithUnrecognizedIntent() throws Exception {
  Intent intent=new Intent();
  assertNull(MessengerUtils.getMessengerThreadParamsForIntent(intent));
}
