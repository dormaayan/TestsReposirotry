@Test public void testTryAuthorizeWithChromeDevPackage(){
  mockTryAuthorize();
  mockCustomTabsAllowed(true);
  mockCustomTabRedirectActivity(true);
  mockChromeCustomTabsSupported(true,DEV_PACKAGE);
  assertTrue(handler.tryAuthorize(request));
}
