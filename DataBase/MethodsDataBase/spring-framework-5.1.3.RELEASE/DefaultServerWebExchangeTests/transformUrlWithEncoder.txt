@Test public void transformUrlWithEncoder(){
  ServerWebExchange exchange=createExchange();
  exchange.addUrlTransformer(s -> s + "?nonce=123");
  assertEquals("/foo?nonce=123",exchange.transformUrl("/foo"));
}
