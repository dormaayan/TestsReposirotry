@Test public void ctorWithEmptyScriptSourceLocator(){
  try {
    new BshScriptFactory("",Messenger.class);
    fail("Must have thrown exception by this point.");
  }
 catch (  IllegalArgumentException expected) {
  }
}
