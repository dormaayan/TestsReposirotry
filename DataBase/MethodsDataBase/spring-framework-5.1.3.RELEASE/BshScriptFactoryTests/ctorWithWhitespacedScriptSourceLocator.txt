@Test public void ctorWithWhitespacedScriptSourceLocator(){
  try {
    new BshScriptFactory("\n   ",Messenger.class);
    fail("Must have thrown exception by this point.");
  }
 catch (  IllegalArgumentException expected) {
  }
}
