@Test public void httpHeaderTimestamp(){
  long timestamp=new Date().getTime();
  request.addHeader(HttpHeaders.IF_MODIFIED_SINCE,timestamp);
  assertEquals(timestamp,request.getDateHeader(HttpHeaders.IF_MODIFIED_SINCE));
}
