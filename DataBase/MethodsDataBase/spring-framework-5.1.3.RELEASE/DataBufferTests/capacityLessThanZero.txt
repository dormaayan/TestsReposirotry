@Test public void capacityLessThanZero(){
  DataBuffer buffer=createDataBuffer(1);
  try {
    buffer.capacity(-1);
    fail("IllegalArgumentException expected");
  }
 catch (  IllegalArgumentException ignored) {
  }
 finally {
    release(buffer);
  }
}
