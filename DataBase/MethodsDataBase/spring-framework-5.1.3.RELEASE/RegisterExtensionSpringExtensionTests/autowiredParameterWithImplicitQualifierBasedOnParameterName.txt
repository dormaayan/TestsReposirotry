/** 
 * NOTE: Test code must be compiled with "-g" (debug symbols) or "-parameters" in order for the parameter name to be used as the qualifier; otherwise, use {@code @Qualifier("wally")}.
 */
@Test void autowiredParameterWithImplicitQualifierBasedOnParameterName(@Autowired Person wally){
  assertNotNull(wally,"Wally should have been @Autowired by Spring");
  assertEquals("Wally",wally.getName(),"Person's name");
}
