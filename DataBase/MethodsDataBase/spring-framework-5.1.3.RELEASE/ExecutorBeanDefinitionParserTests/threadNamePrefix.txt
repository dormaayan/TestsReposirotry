@Test public void threadNamePrefix(){
  CustomizableThreadCreator executor=this.context.getBean("default",CustomizableThreadCreator.class);
  assertEquals("default-",executor.getThreadNamePrefix());
}
