@Test public void doesNotHaveEmptyPathForIndefinatePathWithResults(){
  String expression="$.familyMembers[?(@.name == 'Bart')]";
  exception.expect(AssertionError.class);
  exception.expectMessage("Expected no values at JSON path \"" + expression + "\" "+ "but found: [{\"name\":\"Bart\"}]");
  new JsonPathExpectationsHelper(expression).doesNotHaveJsonPath(SIMPSONS);
}
