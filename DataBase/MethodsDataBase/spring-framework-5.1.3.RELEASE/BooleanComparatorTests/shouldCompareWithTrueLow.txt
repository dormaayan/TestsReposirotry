@Test public void shouldCompareWithTrueLow(){
  Comparator<Boolean> c=new BooleanComparator(true);
  assertThat(c.compare(true,false),is(-1));
  assertThat(c.compare(Boolean.TRUE,Boolean.TRUE),is(0));
}
