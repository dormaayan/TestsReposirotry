@Ignore("TODO(jwilson): reject parameters that use invalid characters") @Test public void doubleQuoteInToken(){
  Headers headers=new Headers.Builder().add("WWW-Authenticate","Digest,,,, Basic ,,,realm=my\"realm").build();
  assertEquals(Arrays.asList(new Challenge("Digest",Collections.<String,String>emptyMap())),HttpHeaders.parseChallenges(headers,"WWW-Authenticate"));
}
