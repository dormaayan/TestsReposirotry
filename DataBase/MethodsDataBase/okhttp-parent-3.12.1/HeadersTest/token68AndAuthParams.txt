@Test public void token68AndAuthParams(){
  Headers headers=new Headers.Builder().add("WWW-Authenticate","Other abc==, realm=myrealm").build();
  assertEquals(Arrays.asList(new Challenge("Other",singletonMap((String)null,"abc=="))),HttpHeaders.parseChallenges(headers,"WWW-Authenticate"));
}
