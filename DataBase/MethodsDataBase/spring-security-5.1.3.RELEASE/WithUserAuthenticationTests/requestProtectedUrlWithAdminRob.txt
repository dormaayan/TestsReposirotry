@Test @WithAdminRob public void requestProtectedUrlWithAdminRob() throws Exception {
  mvc.perform(get("/")).andExpect(status().isNotFound()).andExpect(authenticated().withUsername("rob").withRoles("ADMIN"));
}
