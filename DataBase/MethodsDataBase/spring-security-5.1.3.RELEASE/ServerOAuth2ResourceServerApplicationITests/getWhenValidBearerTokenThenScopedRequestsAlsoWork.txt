@Test public void getWhenValidBearerTokenThenScopedRequestsAlsoWork(){
  this.rest.get().uri("/message").headers(this.messageReadToken).exchange().expectStatus().isOk().expectBody(String.class).isEqualTo("secret message");
}
