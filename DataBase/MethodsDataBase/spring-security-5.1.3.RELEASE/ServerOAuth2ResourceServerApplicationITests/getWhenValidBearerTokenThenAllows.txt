@Test public void getWhenValidBearerTokenThenAllows(){
  this.rest.get().uri("/").headers(this.noScopesToken).exchange().expectStatus().isOk().expectBody(String.class).isEqualTo("Hello, subject!");
}
