@Test public void publisherWhenPermitAllThenAopDoesNotSubscribe(){
  when(this.delegate.publisherFindById(1L)).thenReturn(result);
  this.delegate.publisherFindById(1L);
  result.assertNoSubscribers();
}
