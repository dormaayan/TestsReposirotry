@Test public void authenticatedUserIsSentToOriginalPage(){
  final SecurePage securePage=SecurePage.to(this.driver,this.port).loginForm().username("rod").password("koala").submit();
  securePage.assertAt();
}
