@Test public void getWhenMultipleRequestThenCached(){
  when(this.matcher.matches(any())).thenReturn(true);
  this.source.get(this.selector).block();
  this.source.get(this.selector).block();
  assertThat(this.server.getRequestCount()).isEqualTo(1);
}
