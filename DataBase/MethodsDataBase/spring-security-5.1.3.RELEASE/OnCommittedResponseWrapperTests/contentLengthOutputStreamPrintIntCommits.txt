@Test public void contentLengthOutputStreamPrintIntCommits() throws Exception {
  int i=1234;
  response.setContentLength(String.valueOf(i).length());
  response.getOutputStream().print(i);
  assertThat(committed).isTrue();
}
