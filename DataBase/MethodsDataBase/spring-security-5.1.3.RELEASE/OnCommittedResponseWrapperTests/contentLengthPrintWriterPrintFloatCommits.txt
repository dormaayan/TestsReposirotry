@Test public void contentLengthPrintWriterPrintFloatCommits() throws Exception {
  float f=12345;
  response.setContentLength(String.valueOf(f).length());
  response.getWriter().print(f);
  assertThat(committed).isTrue();
}
