@Test public void contentLengthPrintWriterPrintIntCommits() throws Exception {
  int i=1234;
  response.setContentLength(String.valueOf(i).length());
  response.getWriter().print(i);
  assertThat(committed).isTrue();
}
