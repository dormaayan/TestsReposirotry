@Test public void contentLengthOutputStreamPrintLongCommits() throws Exception {
  long l=12345;
  response.setContentLength(String.valueOf(l).length());
  response.getOutputStream().print(l);
  assertThat(committed).isTrue();
}
