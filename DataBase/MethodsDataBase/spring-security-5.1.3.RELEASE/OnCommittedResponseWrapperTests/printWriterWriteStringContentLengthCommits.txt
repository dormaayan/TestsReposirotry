@Test public void printWriterWriteStringContentLengthCommits() throws IOException {
  String body="something";
  response.getWriter().write(body);
  response.setContentLength(body.length());
  assertThat(committed).isTrue();
}
