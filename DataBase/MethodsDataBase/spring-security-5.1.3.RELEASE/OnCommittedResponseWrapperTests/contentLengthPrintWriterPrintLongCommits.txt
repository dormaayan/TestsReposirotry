@Test public void contentLengthPrintWriterPrintLongCommits() throws Exception {
  long l=12345;
  response.setContentLength(String.valueOf(l).length());
  response.getWriter().print(l);
  assertThat(committed).isTrue();
}
