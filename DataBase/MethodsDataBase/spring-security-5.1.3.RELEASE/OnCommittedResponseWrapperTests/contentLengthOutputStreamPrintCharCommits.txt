@Test public void contentLengthOutputStreamPrintCharCommits() throws Exception {
  char c=1;
  response.setContentLength(1);
  response.getOutputStream().print(c);
  assertThat(committed).isTrue();
}
