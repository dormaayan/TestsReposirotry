@Test public void contentLengthPrintWriterPrintlnLongCommits() throws Exception {
  long l=12345678;
  response.setContentLength(String.valueOf(l).length());
  response.getWriter().println(l);
  assertThat(committed).isTrue();
}
