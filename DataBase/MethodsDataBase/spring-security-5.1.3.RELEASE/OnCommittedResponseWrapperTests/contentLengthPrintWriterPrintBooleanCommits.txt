@Test public void contentLengthPrintWriterPrintBooleanCommits() throws Exception {
  boolean b=true;
  response.setContentLength(1);
  response.getWriter().print(b);
  assertThat(committed).isTrue();
}
