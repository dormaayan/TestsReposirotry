@Test public void contentLengthPrintWriterAppendCharSequenceIntIntCommits() throws Exception {
  String x="abcdef";
  int start=1;
  int end=3;
  response.setContentLength(end - start);
  response.getWriter().append(x,start,end);
  assertThat(committed).isTrue();
}
