@Test public void contentLengthPrintWriterPrintlnFloatCommits() throws Exception {
  float f=1234;
  response.setContentLength(String.valueOf(f).length());
  response.getWriter().println(f);
  assertThat(committed).isTrue();
}
