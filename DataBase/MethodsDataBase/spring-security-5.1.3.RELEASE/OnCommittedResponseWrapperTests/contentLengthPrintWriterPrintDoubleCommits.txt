@Test public void contentLengthPrintWriterPrintDoubleCommits() throws Exception {
  double x=1.2345;
  response.setContentLength(String.valueOf(x).length());
  response.getWriter().print(x);
  assertThat(committed).isTrue();
}
