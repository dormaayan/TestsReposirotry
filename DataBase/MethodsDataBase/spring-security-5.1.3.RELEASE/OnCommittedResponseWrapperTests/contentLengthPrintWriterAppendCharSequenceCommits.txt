@Test public void contentLengthPrintWriterAppendCharSequenceCommits() throws Exception {
  String x="a";
  response.setContentLength(String.valueOf(x).length());
  response.getWriter().append(x);
  assertThat(committed).isTrue();
}
