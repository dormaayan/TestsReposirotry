@Test public void contentLengthOutputStreamPrintBooleanCommits() throws Exception {
  boolean b=true;
  response.setContentLength(1);
  response.getOutputStream().print(b);
  assertThat(committed).isTrue();
}
