@Test public void contentLengthPrintWriterWriteIntMultiDigitCommits() throws Exception {
  int expected=10000;
  response.setContentLength(String.valueOf(expected).length());
  response.getWriter().write(expected);
  assertThat(committed).isTrue();
}
