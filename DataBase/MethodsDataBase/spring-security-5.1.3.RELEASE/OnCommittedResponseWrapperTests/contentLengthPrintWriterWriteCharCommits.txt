@Test public void contentLengthPrintWriterWriteCharCommits() throws Exception {
  char[] buff=new char[4];
  response.setContentLength(buff.length);
  response.getWriter().write(buff);
  assertThat(committed).isTrue();
}
