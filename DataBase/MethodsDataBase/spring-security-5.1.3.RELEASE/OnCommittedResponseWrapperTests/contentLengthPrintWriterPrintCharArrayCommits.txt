@Test public void contentLengthPrintWriterPrintCharArrayCommits() throws Exception {
  char[] x=new char[10];
  response.setContentLength(x.length);
  response.getWriter().print(x);
  assertThat(committed).isTrue();
}
