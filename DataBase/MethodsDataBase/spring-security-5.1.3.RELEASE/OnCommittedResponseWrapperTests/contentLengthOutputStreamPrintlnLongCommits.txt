@Test public void contentLengthOutputStreamPrintlnLongCommits() throws Exception {
  long l=12345678;
  response.setContentLength(String.valueOf(l).length());
  response.getOutputStream().println(l);
  assertThat(committed).isTrue();
}
