@Test public void contentLengthPrintWriterWriteStringIntIntCommits() throws Exception {
  String s="";
  int off=2;
  int len=3;
  response.setContentLength(3);
  response.getWriter().write(s,off,len);
  assertThat(committed).isTrue();
}
