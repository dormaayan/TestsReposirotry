@Test public void contentLengthPrintWriterPrintlnCharArrayCommits() throws Exception {
  char[] x=new char[20];
  response.setContentLength(x.length);
  response.getWriter().println(x);
  assertThat(committed).isTrue();
}
