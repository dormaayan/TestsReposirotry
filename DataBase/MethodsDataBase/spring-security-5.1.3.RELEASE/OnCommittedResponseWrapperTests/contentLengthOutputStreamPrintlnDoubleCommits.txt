@Test public void contentLengthOutputStreamPrintlnDoubleCommits() throws Exception {
  double x=1;
  response.setContentLength(String.valueOf(x).length());
  response.getOutputStream().println(x);
  assertThat(committed).isTrue();
}
