@Test public void contentLengthOutputStreamWriteIntCommits() throws Exception {
  int expected=1;
  response.setContentLength(String.valueOf(expected).length());
  response.getOutputStream().write(expected);
  assertThat(committed).isTrue();
}
