@Test public void contentLengthPrintWriterWriteCharIntIntCommits() throws Exception {
  char[] buff=new char[0];
  int off=2;
  int len=3;
  response.setContentLength(3);
  response.getWriter().write(buff,off,len);
  assertThat(committed).isTrue();
}
