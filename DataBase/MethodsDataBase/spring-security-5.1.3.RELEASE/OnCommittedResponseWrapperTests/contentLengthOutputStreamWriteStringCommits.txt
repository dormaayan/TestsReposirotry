@Test public void contentLengthOutputStreamWriteStringCommits() throws IOException {
  String body="something";
  response.setContentLength(body.length());
  response.getOutputStream().print(body);
  assertThat(committed).isTrue();
}
