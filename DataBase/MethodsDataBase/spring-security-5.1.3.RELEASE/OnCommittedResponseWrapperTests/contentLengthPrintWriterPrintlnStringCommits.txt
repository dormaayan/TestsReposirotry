@Test public void contentLengthPrintWriterPrintlnStringCommits() throws Exception {
  String x="1";
  response.setContentLength(String.valueOf(x).length());
  response.getWriter().println(x);
  assertThat(committed).isTrue();
}
