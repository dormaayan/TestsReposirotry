@Test public void contentLengthOutputStreamPrintStringCommits() throws Exception {
  String x="12345";
  response.setContentLength(x.length());
  response.getOutputStream().print(x);
  assertThat(committed).isTrue();
}
