@Test public void authenticatedUserIsSentToOriginalPage(){
  final HomePage homePage=HomePage.to(this.driver,this.port).loginForm().username("user").password("password").submit();
  homePage.assertAt().andTheUserNameIsDisplayed();
}
