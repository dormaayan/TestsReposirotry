@Test public void noOverlapForComputedAndProvidedChecksums(){
  try {
    UpdateCenter.verifyChecksums(new MockDownloadJob(EMPTY_SHA1,EMPTY_SHA256,null),buildEntryWithExpectedChecksums(null,null,EMPTY_SHA512),new File("example"));
    fail();
  }
 catch (  Exception e) {
    assertTrue(e.getMessage().equals("Unable to confirm integrity of downloaded file, refusing installation"));
  }
}
