/** 
 * Requesting a loginError page directly should result in a redirect, on a non-secured Hudson.
 */
@Test public void loginErrorRedirect1() throws Exception {
  verifyNotError(j.createWebClient());
}
