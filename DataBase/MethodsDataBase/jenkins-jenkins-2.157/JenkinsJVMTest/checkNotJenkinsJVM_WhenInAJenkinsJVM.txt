@Test(expected=IllegalStateException.class) public void checkNotJenkinsJVM_WhenInAJenkinsJVM(){
  JenkinsJVM.setJenkinsJVM(true);
  try {
    JenkinsJVM.checkNotJenkinsJVM();
  }
  finally {
    JenkinsJVM.setJenkinsJVM(false);
  }
}
