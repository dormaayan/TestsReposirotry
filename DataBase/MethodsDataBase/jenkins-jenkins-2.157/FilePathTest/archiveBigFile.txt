/** 
 * Checks that big files (greater than 8GB) can be archived and then unpacked. This test is disabled by default due the impact on RAM. The actual file size limit is 8589934591 bytes.
 * @throws Exception test failure
 */
@Issue("JENKINS-10629") @Ignore @Test public void archiveBigFile() throws Exception {
  final long largeFileSize=9000000000L;
  final String filePrefix="JENKINS-10629";
  checkTarUntarRoundTrip(filePrefix,largeFileSize);
}
