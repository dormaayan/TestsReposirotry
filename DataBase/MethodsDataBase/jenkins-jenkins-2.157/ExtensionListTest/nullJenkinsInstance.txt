@Test @WithoutJenkins public void nullJenkinsInstance() throws Exception {
  ExtensionList<Animal> list=ExtensionList.lookup(Animal.class);
  assertEquals(0,list.size());
  assertFalse(list.iterator().hasNext());
}
