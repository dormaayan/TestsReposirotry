@Test public void testKeyForCaseInsensitive(){
  IdStrategy idStrategy=IdStrategy.CASE_INSENSITIVE;
  assertThat(idStrategy.keyFor("user1"),is("user1"));
  assertThat(idStrategy.keyFor("User1"),is("user1"));
  assertThat(idStrategy.keyFor("USER1"),is("user1"));
}
