@Test public void testKeyForCaseSensitive(){
  IdStrategy idStrategy=new IdStrategy.CaseSensitive();
  assertThat(idStrategy.keyFor("user1"),is("user1"));
  assertThat(idStrategy.keyFor("User1"),is("User1"));
  assertThat(idStrategy.keyFor("USER1"),is("USER1"));
}
