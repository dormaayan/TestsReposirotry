@Test public void isSundayAndPreviousRunIsPreviousSunday7() throws Exception {
  final Calendar cal=Calendar.getInstance(locale);
  cal.set(2011,0,16,0,0,0);
  final String cronStr="0 1 * * 7";
  final CronTab cron=new CronTab(cronStr);
  final Calendar actual=cron.floor(cal);
  final Calendar expected=Calendar.getInstance();
  expected.set(2011,0,9,1,0,0);
  compare(expected,actual);
}
