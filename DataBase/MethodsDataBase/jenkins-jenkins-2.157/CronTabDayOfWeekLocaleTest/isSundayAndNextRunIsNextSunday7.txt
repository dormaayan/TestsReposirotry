@Test @Issue("JENKINS-12357") public void isSundayAndNextRunIsNextSunday7() throws Exception {
  final Calendar cal=Calendar.getInstance(locale);
  cal.set(2011,0,16,1,0,0);
  final String cronStr="0 0 * * 7";
  final CronTab cron=new CronTab(cronStr);
  final Calendar actual=cron.ceil(cal);
  final Calendar expected=Calendar.getInstance();
  expected.set(2011,0,23,0,0,0);
  compare(expected,actual);
}
