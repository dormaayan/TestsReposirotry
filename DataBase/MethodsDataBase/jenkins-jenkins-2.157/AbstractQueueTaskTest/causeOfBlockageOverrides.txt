@Issue("JENKINS-47517") @Test public void causeOfBlockageOverrides(){
  Queue.Task t=new LegacyTask();
  assertFalse(t.isBuildBlocked());
  assertNull(t.getWhyBlocked());
  assertNull(t.getCauseOfBlockage());
}
