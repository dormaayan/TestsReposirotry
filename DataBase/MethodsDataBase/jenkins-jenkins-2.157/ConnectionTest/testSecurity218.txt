@Test public void testSecurity218() throws Exception {
  c1.writeObject(new Security218());
  try {
    c2.readObject();
    fail();
  }
 catch (  SecurityException e) {
    assertTrue(e.getMessage().contains(Security218.class.getName()));
  }
}
