@Test @LocalData public void corruptConfigFile(){
  String fredUserId="fred";
  User fred=User.getById(fredUserId,true);
  assertThat(fred,notNullValue());
  assertThat(fred.getFullName(),is("fred"));
}
