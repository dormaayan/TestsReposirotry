@Test public void testThreadCount() throws InterruptedException {
  assertThat(containsJerseyThread(),equalTo(true));
  client.shutdown();
  Thread.sleep(5);
  assertThat(containsJerseyThread(),equalTo(false));
}
