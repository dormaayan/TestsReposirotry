/** 
 * 20 random tests of various record, file, and split sizes.  All tests have compressed file as input.
 */
@Test(timeout=500000) public void testFormatCompressedIn() throws Exception {
  runRandomTests(new GzipCodec());
}
