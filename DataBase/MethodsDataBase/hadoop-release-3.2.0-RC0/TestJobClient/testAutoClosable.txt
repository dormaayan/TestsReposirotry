/** 
 * Asks the compiler to check if JobClient is AutoClosable.
 */
@Test(timeout=10000) public void testAutoClosable() throws IOException {
  Configuration conf=new Configuration();
  try (JobClient jobClient=new JobClient(conf)){
  }
 }
