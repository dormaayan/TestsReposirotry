/** 
 * This test verifies that exceptions in the {@link LaunchableService#execute()} method are relayed if an instance ofan exit exceptions, and forwarded if not.
 */
@Test public void testServiceLaunchException() throws Throwable {
  assertLaunchOutcome(EXIT_OTHER_FAILURE,SLE_TEXT,NAME,ARG_THROW_SLE);
}
