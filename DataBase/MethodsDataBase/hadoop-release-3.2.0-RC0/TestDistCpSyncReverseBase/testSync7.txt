/** 
 * Test a case where rename a dir, then create a new dir with the same name and sub dir.
 */
@Test public void testSync7() throws Exception {
  if (isSrcNotSameAsTgt) {
    initData7(source);
  }
  initData7(target);
  enableAndCreateFirstSnapshot();
  int numDeletedAndModified=changeData7(target);
  createSecondSnapshotAtTarget();
  testAndVerify(numDeletedAndModified);
}
