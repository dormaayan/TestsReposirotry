@Documented("Delete node index.") @Test public void shouldReturn204WhenRemovingNodeIndexes(){
  final String indexName=indexes.newInstance();
  helper.createNodeIndex(indexName);
  gen().expectedStatus(204).delete(functionalTestHelper.indexNodeUri(indexName));
}
