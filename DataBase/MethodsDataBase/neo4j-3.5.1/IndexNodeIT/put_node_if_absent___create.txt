@Documented("Backward Compatibility Test (using old syntax ?unique)\n" + "Put node if absent - Create.\n" + "\n"+ "Add a node to an index unless a node already exists for the given index data. In\n"+ "this case, a new node is created since nothing existing is found in the index.") @Test public void put_node_if_absent___create(){
  final String index=indexes.newInstance();
  String key="name";
  String value="Mattias";
  helper.createNodeIndex(index);
  String uri=functionalTestHelper.nodeIndexUri() + index + "?unique";
  gen().expectedStatus(201).payloadType(MediaType.APPLICATION_JSON_TYPE).payload("{\"key\": \"" + key + "\", \"value\": \""+ value+ "\", "+ "\"uri\":\""+ functionalTestHelper.nodeUri(helper.createNode())+ "\"}").post(uri);
}
