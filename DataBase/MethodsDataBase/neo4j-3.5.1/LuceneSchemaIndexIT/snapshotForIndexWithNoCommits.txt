@Test void snapshotForIndexWithNoCommits() throws Exception {
  try (LuceneIndexAccessor indexAccessor=createDefaultIndexAccessor();ResourceIterator<File> snapshotIterator=indexAccessor.snapshotFiles()){
    assertThat(asUniqueSetOfNames(snapshotIterator),equalTo(emptySet()));
  }
 }
