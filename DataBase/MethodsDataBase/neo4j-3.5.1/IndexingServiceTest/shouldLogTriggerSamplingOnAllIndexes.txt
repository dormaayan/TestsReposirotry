@Test public void shouldLogTriggerSamplingOnAllIndexes() throws Exception {
  IndexingService indexingService=newIndexingServiceWithMockedDependencies(populator,accessor,withData());
  IndexSamplingMode mode=TRIGGER_REBUILD_ALL;
  indexingService.triggerIndexSampling(mode);
  internalLogProvider.assertAtLeastOnce(logMatch.info("Manual trigger for sampling all indexes [" + mode + "]"));
}
