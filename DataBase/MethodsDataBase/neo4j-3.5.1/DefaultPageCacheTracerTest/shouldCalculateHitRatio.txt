@Test void shouldCalculateHitRatio(){
  assertThat("hitRation",tracer.hitRatio(),closeTo(0d,0.0001));
  tracer.hits(3);
  tracer.faults(7);
  assertThat("hitRation",tracer.hitRatio(),closeTo(3.0 / 10,0.0001));
}
