@Test public void shouldIgnoreRemovingNonExistentNodeProperty(){
  BatchInserter inserter=globalInserter;
  long id=inserter.createNode(Collections.emptyMap());
  inserter.removeNodeProperty(id,"non-existent");
}
