/** 
 * Find node by query from an automatic index. <p/> See Find node by query for the actual query syntax.
 */
@Test @Graph(nodes={@NODE(name="I",setNameProperty=true)},autoIndexNodes=true) public void shouldRetrieveFromAutoIndexByQuery(){
  data.get();
  assertSize(1,gen.get().expectedStatus(200).get(nodeAutoIndexUri() + "?query=name:I").entity());
}
