/** 
 * Add a property for autoindexing on nodes.
 */
@Test public void addAutoIndexingPropertyForNodes(){
  gen.get().expectedStatus(204).payload("myProperty1").post(autoIndexURI("node") + "/properties");
}
