/** 
 * Remove a property for autoindexing on nodes.
 */
@Test public void removeAutoIndexingPropertyForNodes(){
  gen.get().expectedStatus(204).delete(autoIndexURI("node") + "/properties/myProperty1");
}
