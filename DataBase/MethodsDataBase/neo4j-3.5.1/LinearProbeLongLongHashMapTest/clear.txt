@Test void clear(){
  map.clear();
  assertEquals(0,map.size());
  map.putAll(newWithKeysValues(0,10,1,11,2,12));
  assertEquals(3,map.size());
  map.clear();
  assertEquals(0,map.size());
  map.clear();
  assertEquals(0,map.size());
}
