@Test public void plannerInfoShouldBeInSmallCase(){
  CompilerInfo compilerInfo=new CompilerInfo("PLANNER","RUNTIME",emptyList());
  assertThat(compilerInfo.planner(),is("planner"));
  assertThat(compilerInfo.runtime(),is("runtime"));
}
