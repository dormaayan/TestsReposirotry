@Test public void testLocalTimeTypeSmallNano(){
  LocalTime time=LocalTimeValue.localTime(0,0,0,37).asObjectCopy();
  String key="dt";
  node1.setProperty(key,time);
  newTransaction();
  Object property=node1.getProperty(key);
  assertEquals(time,property);
}
