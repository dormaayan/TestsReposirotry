@Test public void shouldNotBeAbleToWriteAfterClose() throws Throwable {
  out.beginMessage();
  out.writeLong(1);
  out.writeLong(2);
  out.writeLong(3);
  out.messageSucceeded();
  out.flush();
  out.close();
  try {
    out.writeShort((short)42);
    fail("Should have thrown IOException");
  }
 catch (  IOException ignore) {
  }
}
