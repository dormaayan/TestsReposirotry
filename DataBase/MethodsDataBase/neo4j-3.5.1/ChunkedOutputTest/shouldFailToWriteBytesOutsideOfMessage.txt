@Test public void shouldFailToWriteBytesOutsideOfMessage() throws Exception {
  try {
    out.writeBytes(ByteBuffer.wrap(new byte[10]));
    fail("Exception expected");
  }
 catch (  IllegalStateException ignore) {
  }
}
