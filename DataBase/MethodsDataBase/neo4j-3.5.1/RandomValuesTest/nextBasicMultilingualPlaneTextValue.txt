@Test public void nextBasicMultilingualPlaneTextValue(){
  for (int i=0; i < ITERATIONS; i++) {
    TextValue value=randomValues.nextBasicMultilingualPlaneTextValue();
    assertThat(value.length(),equalTo(value.stringValue().length()));
  }
}
