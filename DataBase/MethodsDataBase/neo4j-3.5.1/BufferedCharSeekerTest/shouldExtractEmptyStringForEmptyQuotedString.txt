@Test public void shouldExtractEmptyStringForEmptyQuotedString() throws Exception {
  seeker=seeker("\"\",,\"\"");
  assertNextValue(seeker,mark,COMMA,"");
  assertNextValueNotExtracted(seeker,mark,COMMA);
  assertNextValue(seeker,mark,COMMA,"");
  assertFalse(seeker.seek(mark,COMMA));
}
