@Test public void shouldNegotiateProtocolV2WhenClientSupportsBothV1AndV2() throws Exception {
  connection.connect(address).send(util.acceptedVersions(2,1,0,0)).send(util.chunk(new InitMessage(USER_AGENT,emptyMap())));
  assertThat(connection,eventuallyReceives(new byte[]{0,0,0,2}));
}
