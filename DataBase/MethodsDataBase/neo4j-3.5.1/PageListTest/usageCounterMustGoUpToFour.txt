@Test public void usageCounterMustGoUpToFour(){
  pageList.incrementUsage(pageRef);
  pageList.incrementUsage(pageRef);
  pageList.incrementUsage(pageRef);
  pageList.incrementUsage(pageRef);
  assertFalse(pageList.decrementUsage(pageRef));
  assertFalse(pageList.decrementUsage(pageRef));
  assertFalse(pageList.decrementUsage(pageRef));
  assertTrue(pageList.decrementUsage(pageRef));
}
