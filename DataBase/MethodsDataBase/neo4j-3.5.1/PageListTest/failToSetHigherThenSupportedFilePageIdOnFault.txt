@Test(expected=IllegalArgumentException.class) public void failToSetHigherThenSupportedFilePageIdOnFault() throws IOException {
  pageList.unlockExclusive(pageRef);
  short swapperId=2;
  doFault(swapperId,Long.MAX_VALUE);
}
