@Test(expected=IllegalStateException.class) public void disallowWriteLockedPageToExplicitlyLowerModifiedFlag(){
  pageList.unlockExclusive(pageRef);
  assertTrue(pageList.tryWriteLock(pageRef));
  pageList.explicitlyMarkPageUnmodifiedUnderExclusiveLock(pageRef);
}
