@Test public void concurrentWriteLocksMustFailExclusiveLocks(){
  pageList.unlockExclusive(pageRef);
  pageList.tryWriteLock(pageRef);
  pageList.tryWriteLock(pageRef);
  pageList.unlockWrite(pageRef);
  assertFalse(pageList.tryExclusiveLock(pageRef));
}
