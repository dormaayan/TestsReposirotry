@Test public void tryEvictMustWhenPageIsNotLoadedMustNotLeavePageLocked() throws Exception {
  pageList.unlockExclusive(pageRef);
  pageList.tryEvict(pageRef,EvictionRunEvent.NULL);
  assertFalse(pageList.isExclusivelyLocked(pageRef));
}
