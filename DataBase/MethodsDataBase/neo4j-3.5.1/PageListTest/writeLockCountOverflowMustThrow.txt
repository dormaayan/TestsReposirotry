@Test(expected=IllegalMonitorStateException.class,timeout=TIMEOUT) public void writeLockCountOverflowMustThrow(){
  pageList.unlockExclusive(pageRef);
  for (; ; ) {
    assertTrue(pageList.tryWriteLock(pageRef));
  }
}
