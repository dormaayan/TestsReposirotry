@Test(timeout=TIMEOUT) public void writeLocksMustNotBlockOtherWriteLocks(){
  pageList.unlockExclusive(pageRef);
  assertTrue(pageList.tryWriteLock(pageRef));
  assertTrue(pageList.tryWriteLock(pageRef));
}
