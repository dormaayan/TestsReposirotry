@Test public void uncontendedFlushLockMustBeAvailableAfterExclusiveLock(){
  pageList.unlockExclusive(pageRef);
  pageList.tryExclusiveLock(pageRef);
  pageList.unlockExclusive(pageRef);
  assertTrue(pageList.tryFlushLock(pageRef) != 0);
}
