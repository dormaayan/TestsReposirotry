@Test public void mustExposePageCount(){
  int pageCount;
  long victimPage=VictimPageReference.getVictimPage(pageSize,GlobalMemoryTracker.INSTANCE);
  pageCount=3;
  assertThat(new PageList(pageCount,pageSize,mman,swappers,victimPage,ALIGNMENT).getPageCount(),is(pageCount));
  pageCount=42;
  assertThat(new PageList(pageCount,pageSize,mman,swappers,victimPage,ALIGNMENT).getPageCount(),is(pageCount));
}
