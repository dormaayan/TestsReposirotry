@Test public void uncontendedFlushLockMustBeAvailableAfterWriteLock(){
  pageList.unlockExclusive(pageRef);
  pageList.tryWriteLock(pageRef);
  pageList.unlockWrite(pageRef);
  assertTrue(pageList.tryFlushLock(pageRef) != 0);
}
