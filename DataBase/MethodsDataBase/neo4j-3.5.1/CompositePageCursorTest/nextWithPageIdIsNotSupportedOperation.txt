@Test void nextWithPageIdIsNotSupportedOperation(){
  assertThrows(UnsupportedOperationException.class,() -> {
    PageCursor pageCursor=CompositePageCursor.compose(first,PAGE_SIZE,second,PAGE_SIZE);
    pageCursor.next(12);
  }
);
}
