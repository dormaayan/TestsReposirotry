@Test public void shouldHandleSadPaths(){
  assertNull(decode(""));
  assertNull(decode("Basic"));
  assertNull(decode("Basic not valid value"));
  assertNull(decode("Basic " + Base64.getEncoder().encodeToString("".getBytes())));
}
