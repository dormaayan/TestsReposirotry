@Test public void testNumRange_InclusiveLowerExclusiveUpper(){
  RangePredicate<?> p=IndexQuery.range(propId,11,true,13,false);
  assertFalse(test(p,10));
  assertTrue(test(p,11));
  assertTrue(test(p,12));
  assertFalse(test(p,13));
}
