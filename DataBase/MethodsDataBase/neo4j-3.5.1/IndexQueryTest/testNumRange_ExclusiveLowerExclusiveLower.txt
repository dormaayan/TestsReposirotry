@Test public void testNumRange_ExclusiveLowerExclusiveLower(){
  RangePredicate<?> p=IndexQuery.range(propId,11,false,13,false);
  assertFalse(test(p,11));
  assertTrue(test(p,12));
  assertFalse(test(p,13));
}
