@Test public void testStringRange_UpperUnbounded(){
  RangePredicate<?> p=IndexQuery.range(propId,"bbb",false,null,false);
  assertFalse(test(p,"bbb"));
  assertTrue(test(p,"bbba"));
  assertTrue(test(p,"xxxxx"));
}
