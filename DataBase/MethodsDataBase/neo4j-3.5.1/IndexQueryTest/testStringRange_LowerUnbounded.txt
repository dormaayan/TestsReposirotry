@Test public void testStringRange_LowerUnbounded(){
  RangePredicate<?> p=IndexQuery.range(propId,null,false,"bee",false);
  assertTrue(test(p,""));
  assertTrue(test(p,"bed"));
  assertFalse(test(p,"bee"));
}
