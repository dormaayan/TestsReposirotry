@Test public void testNumRange_ExclusiveLowerInclusiveUpper(){
  RangePredicate<?> p=IndexQuery.range(propId,11,false,13,true);
  assertFalse(test(p,11));
  assertTrue(test(p,12));
  assertTrue(test(p,13));
  assertFalse(test(p,14));
}
