@Test(expected=InvalidRecordException.class) public void readingNonExistingRecordMustThrow(){
  try (NeoStores neoStores=storeFixture()){
    S store=getStore(neoStores);
    getHeavy(store,ID + 1);
  }
 }
