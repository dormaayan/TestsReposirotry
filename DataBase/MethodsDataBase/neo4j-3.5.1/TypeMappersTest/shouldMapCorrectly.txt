@Test public void shouldMapCorrectly() throws Throwable {
  TypeChecker mapper=new TypeMappers().checkerFor(javaClass);
  Object converted=mapper.typeCheck(javaValue);
  assertEquals(expectedNeoValue,converted);
}
