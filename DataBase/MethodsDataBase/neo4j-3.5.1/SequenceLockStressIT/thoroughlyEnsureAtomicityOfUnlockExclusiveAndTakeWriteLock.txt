@Test void thoroughlyEnsureAtomicityOfUnlockExclusiveAndTakeWriteLock() throws Exception {
  for (int i=0; i < 30000; i++) {
    unlockExclusiveAndTakeWriteLockMustBeAtomic();
    OffHeapPageLock.unlockWrite(lockAddr);
  }
}
