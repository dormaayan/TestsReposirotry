@Test void deleteRecursivelyMustDeleteGivenFile() throws Exception {
  fsa.mkdirs(path);
  File file=new File(path,"file");
  fsa.create(file).close();
  fsa.deleteRecursively(file);
  assertFalse(fsa.fileExists(file));
}
