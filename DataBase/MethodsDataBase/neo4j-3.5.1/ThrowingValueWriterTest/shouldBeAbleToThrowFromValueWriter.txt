@Test void shouldBeAbleToThrowFromValueWriter(){
  Value value=Values.of("This is a value");
  ValueWriter<TestException> writer=throwing(TestException::new);
  assertThrows(TestException.class,() -> value.writeTo(writer));
}
