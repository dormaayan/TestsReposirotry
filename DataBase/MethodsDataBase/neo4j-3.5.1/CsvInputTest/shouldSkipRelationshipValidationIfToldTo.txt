@Test public void shouldSkipRelationshipValidationIfToldTo() throws Exception {
  Iterable<DataFactory> data=datas(CsvInputTest.data(":START_ID,:END_ID,:TYPE\n" + ",,"));
  Input input=new CsvInput(datas(),defaultFormatNodeFileHeader(),data,defaultFormatRelationshipFileHeader(),IdType.INTEGER,config(COMMAS),silentBadCollector(0));
  try (InputIterator relationships=input.relationships().iterator()){
    readNext(relationships);
    assertNull(visitor.startId());
    assertNull(visitor.endId());
    assertNull(visitor.stringType);
  }
 }
