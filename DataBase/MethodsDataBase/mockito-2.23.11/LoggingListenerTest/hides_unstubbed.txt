@Test public void hides_unstubbed(){
  LoggingListener listener=new LoggingListener(false);
  listener.foundUnstubbed(new InvocationBuilder().toInvocationMatcher());
  assertEquals("",listener.getStubbingInfo());
}
