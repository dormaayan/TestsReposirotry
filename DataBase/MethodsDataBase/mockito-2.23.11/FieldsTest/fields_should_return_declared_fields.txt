@Test public void fields_should_return_declared_fields() throws Exception {
  assertThat(Fields.declaredFieldsOf(new HierarchyOfClasses()).filter(syntheticField()).names()).containsOnly("b","static_b");
}
