@Test public void should_stub() throws Exception {
  doReturn("foo").when(mock).simpleMethod();
  doReturn("bar").when(mock).simpleMethod();
  Assertions.assertThat(mock.simpleMethod()).isEqualTo("bar");
}
