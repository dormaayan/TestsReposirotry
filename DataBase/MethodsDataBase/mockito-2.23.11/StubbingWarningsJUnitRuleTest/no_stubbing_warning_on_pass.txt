@Test public void no_stubbing_warning_on_pass() throws Throwable {
  rule.expectSuccess(new Runnable(){
    public void run(){
      assertTrue(logger.isEmpty());
    }
  }
);
  declareStubbingWithArg(mock,"a");
  useStubbingWithArg(mock,"a");
}
