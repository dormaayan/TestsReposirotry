@Test public void should_stub_by_delegating_to_real_method() throws Exception {
  Dog dog=mock(Dog.class);
  willCallRealMethod().given(dog).bark();
  Assertions.assertThat(dog.bark()).isEqualTo("woof");
}
