@Test public void doThrowShouldNotAllowSettingNullThrowableClass(){
  exception.expect(MockitoException.class);
  exception.expectMessage("Exception type cannot be null");
  doThrow((Class)null).when(mock).isEmpty();
}
