@Test public void test_stub_only_not_verifiable_in_order(){
  IMethods localMock=mock(IMethods.class,withSettings().stubOnly());
  try {
    inOrder(localMock);
    fail();
  }
 catch (  CannotVerifyStubOnlyMock e) {
  }
}
