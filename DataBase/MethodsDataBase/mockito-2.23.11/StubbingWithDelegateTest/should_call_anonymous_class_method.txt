@Test public void should_call_anonymous_class_method() throws Throwable {
  Foo foo=new Foo(){
    public int bar(){
      return 0;
    }
  }
;
  Foo mock=mock(Foo.class);
  when(mock.bar()).thenAnswer(AdditionalAnswers.delegatesTo(foo));
  mock.bar();
}
