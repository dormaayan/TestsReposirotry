@Test public void testSkipToNextSample() throws Exception {
  for (int i=1; i <= ID3_DATA_1.length + ID3_DATA_2.length; i++) {
    data.setPosition(i);
    feed();
    int id3SampleCount=Math.min(i,ID3_DATA_1.length);
    assertSampleCounts(id3SampleCount,i);
  }
}
