@Test public void openRtmpDataSpec_instantiatesRtmpDataSourceViaReflection() throws IOException {
  DefaultDataSource dataSource=new DefaultDataSource(RuntimeEnvironment.application,"userAgent",false);
  DataSpec dataSpec=new DataSpec(Uri.parse("rtmp://test.com/stream"));
  try {
    dataSource.open(dataSpec);
  }
 catch (  UnsatisfiedLinkError e) {
  }
catch (  UnsupportedOperationException e) {
  }
}
