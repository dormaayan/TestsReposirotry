@Test public void testClearCacheWhenCacheFileIsNotSet(){
  final Checker checker=new Checker();
  checker.clearCache();
  assertNull("If cache file is not set the cache should default to null",Whitebox.getInternalState(checker,"cacheFile"));
}
