@Test public void testIsLocalVariableDefUnexpected(){
  assertFalse("Should return false when passed is not variable def",ScopeUtil.isLocalVariableDef(getNode(TokenTypes.LITERAL_CATCH)));
  assertFalse("Should return false when passed is not variable def",ScopeUtil.isLocalVariableDef(getNode(TokenTypes.COMMA,TokenTypes.PARAMETER_DEF)));
}
