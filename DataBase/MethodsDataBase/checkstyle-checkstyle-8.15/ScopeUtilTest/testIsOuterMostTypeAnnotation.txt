@Test public void testIsOuterMostTypeAnnotation(){
  assertFalse("Should return false when passed is not outer most type",ScopeUtil.isOuterMostType(getNode(TokenTypes.ANNOTATION_DEF,TokenTypes.MODIFIERS)));
}
