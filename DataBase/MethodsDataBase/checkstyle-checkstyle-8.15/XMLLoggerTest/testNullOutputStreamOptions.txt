@Test public void testNullOutputStreamOptions(){
  try {
    final XMLLogger logger=new XMLLogger(outStream,null);
    assertNotNull("Null instance",logger);
    fail("Exception was expected");
  }
 catch (  IllegalArgumentException exception) {
    assertEquals("Invalid error message","Parameter outputStreamOptions can not be null",exception.getMessage());
  }
}
