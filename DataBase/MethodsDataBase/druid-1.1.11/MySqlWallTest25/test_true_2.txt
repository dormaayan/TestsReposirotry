public void test_true_2() throws Exception {
  WallConfig config=new WallConfig();
  config.setUpdateWhereNoneCheck(true);
  Assert.assertTrue(WallUtils.isValidateMySql("update test.dml_health_check set id=? limit ?",config));
}
