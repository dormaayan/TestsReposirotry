@Test public void testDriverClassName() throws SQLException {
  String driverClass=JdbcUtils.getDriverClassName(URL);
  Assert.assertThat("check get driverClassName",driverClass,Is.is(JdbcConstants.KYLIN_DRIVER));
}
