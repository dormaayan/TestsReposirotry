public void test_true_2() throws Exception {
  WallProvider provider=new MySqlWallProvider();
  Assert.assertTrue(provider.checkValid("SELECT USER() FROM X"));
  Assert.assertEquals(1,provider.getTableStats().size());
}
