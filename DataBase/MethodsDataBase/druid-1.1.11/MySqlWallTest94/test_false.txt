public void test_false() throws Exception {
  WallProvider provider=new MySqlWallProvider();
  Assert.assertTrue(provider.checkValid("select * from test having 1=1"));
  Assert.assertEquals(1,provider.getTableStats().size());
}
