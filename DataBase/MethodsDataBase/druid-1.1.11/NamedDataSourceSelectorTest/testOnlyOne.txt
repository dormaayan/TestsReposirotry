@Test public void testOnlyOne(){
  dataSourceMap.remove("foo");
  NamedDataSourceSelector selector=new NamedDataSourceSelector(dataSource);
  for (int i=0; i < 50; i++) {
    assertEquals("bar",((MockDataSource)selector.get()).getName());
  }
}
