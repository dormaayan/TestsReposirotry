@SuppressWarnings("unchecked") @Test public void singleSourceZipperReturnsNull(){
  Single.zipArray(Functions.justFunction(null),Single.just(1)).test().assertFailureAndMessage(NullPointerException.class,"The zipper returned a null value");
}
