@Test public void hasNextThrows(){
  Maybe.just(1).flattenAsFlowable(new Function<Object,Iterable<Integer>>(){
    @Override public Iterable<Integer> apply(    Object v) throws Exception {
      return new CrashingIterable(100,2,100);
    }
  }
).test(2L).assertFailureAndMessage(TestException.class,"hasNext()",0);
}
