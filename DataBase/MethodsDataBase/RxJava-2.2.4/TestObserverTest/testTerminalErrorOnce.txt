@Test public void testTerminalErrorOnce(){
  TestSubscriber<Integer> ts=new TestSubscriber<Integer>();
  ts.onError(new TestException());
  ts.onError(new TestException());
  try {
    ts.assertTerminated();
  }
 catch (  AssertionError ex) {
    return;
  }
  fail("Failed to report multiple onError terminal events!");
}
