@Test public void testTerminalCompletedOnce(){
  TestSubscriber<Integer> ts=new TestSubscriber<Integer>();
  ts.onComplete();
  ts.onComplete();
  try {
    ts.assertTerminated();
  }
 catch (  AssertionError ex) {
    return;
  }
  fail("Failed to report multiple onComplete terminal events!");
}
