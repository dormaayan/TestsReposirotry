@Test public void testNullExpected(){
  TestSubscriber<Integer> ts=new TestSubscriber<Integer>();
  ts.onNext(1);
  try {
    ts.assertValue((Integer)null);
  }
 catch (  AssertionError ex) {
    return;
  }
  fail("Null element check assertion didn't happen!");
}
