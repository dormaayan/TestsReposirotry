@Test public void assertTerminated(){
  TestObserver<Integer> to=TestObserver.create();
  to.assertNotTerminated();
  to.onError(null);
  try {
    to.assertNotTerminated();
    throw new RuntimeException("Should have thrown!");
  }
 catch (  AssertionError ex) {
  }
}
