@Test public void testTerminalOneKind(){
  TestSubscriber<Integer> ts=new TestSubscriber<Integer>();
  ts.onError(new TestException());
  ts.onComplete();
  try {
    ts.assertTerminated();
  }
 catch (  AssertionError ex) {
    return;
  }
  fail("Failed to report multiple kinds of events!");
}
