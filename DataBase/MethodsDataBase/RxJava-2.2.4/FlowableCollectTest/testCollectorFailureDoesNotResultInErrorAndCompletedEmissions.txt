@Test public void testCollectorFailureDoesNotResultInErrorAndCompletedEmissions(){
  final RuntimeException e=new RuntimeException();
  Burst.item(1).create().collect(callableListCreator(),biConsumerThrows(e)).test().assertError(e).assertNotComplete();
}
