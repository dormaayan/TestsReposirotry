@Test public void to(){
  Flowable.range(1,5).parallel().to(new Function<ParallelFlowable<Integer>,Flowable<Integer>>(){
    @Override public Flowable<Integer> apply(    ParallelFlowable<Integer> pf) throws Exception {
      return pf.sequential();
    }
  }
).test().assertResult(1,2,3,4,5);
}
