@Test public void boundaryError(){
  BehaviorSubject.createDefault(1).window(Functions.justCallable(Observable.error(new TestException()))).test().assertValueCount(1).assertNotComplete().assertError(TestException.class);
}
