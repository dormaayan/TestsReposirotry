@Test public void drainFusedFailFast(){
  UnicastSubject<Integer> us=UnicastSubject.create(false);
  TestObserver<Integer> to=us.to(ObserverFusion.<Integer>test(QueueFuseable.ANY,false));
  us.done=true;
  us.drainFused(to);
  to.assertResult();
}
