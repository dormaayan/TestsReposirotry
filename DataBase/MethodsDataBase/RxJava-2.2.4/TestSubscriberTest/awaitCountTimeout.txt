@Test public void awaitCountTimeout(){
  TestSubscriber<Object> ts=Flowable.never().test().awaitCount(1,TestWaitStrategy.SLEEP_1MS,50);
  assertTrue(ts.isTimeout());
  ts.clearTimeout();
  assertFalse(ts.isTimeout());
}
