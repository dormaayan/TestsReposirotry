@Test public void fusedReject(){
  TestObserver<Long> to=ObserverFusion.newTest(QueueFuseable.ASYNC);
  Observable.rangeLong(1,2).subscribe(to);
  ObserverFusion.assertFusion(to,QueueFuseable.NONE).assertResult(1L,2L);
}
