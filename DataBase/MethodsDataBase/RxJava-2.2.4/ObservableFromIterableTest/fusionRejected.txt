@Test public void fusionRejected(){
  TestObserver<Integer> to=ObserverFusion.newTest(QueueFuseable.ASYNC);
  Observable.fromIterable(Arrays.asList(1,2,3)).subscribe(to);
  ObserverFusion.assertFusion(to,QueueFuseable.NONE).assertResult(1,2,3);
}
