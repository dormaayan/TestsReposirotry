@Test public void onSubscribeDispose(){
  CompletableSubject cs=CompletableSubject.create();
  Disposable d=Disposables.empty();
  cs.onSubscribe(d);
  assertFalse(d.isDisposed());
  cs.onComplete();
  d=Disposables.empty();
  cs.onSubscribe(d);
  assertTrue(d.isDisposed());
}
