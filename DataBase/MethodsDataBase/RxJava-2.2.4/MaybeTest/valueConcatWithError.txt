@Test public void valueConcatWithError(){
  Maybe.just(1).concatWith(Maybe.<Integer>error(new RuntimeException("error"))).test().assertValue(1).assertError(RuntimeException.class).assertErrorMessage("error");
}
