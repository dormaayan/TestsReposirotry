@Test public void deferThrows(){
  Maybe.defer(new Callable<Maybe<Integer>>(){
    @Override public Maybe<Integer> call() throws Exception {
      throw new TestException();
    }
  }
).test().assertFailure(TestException.class);
}
