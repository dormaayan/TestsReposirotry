@Test public void normalConditionalBackpressured2(){
  Flowable.fromIterable(Arrays.asList(1,2,3,4,5)).filter(Functions.alwaysTrue()).test(4L).assertSubscribed().assertValues(1,2,3,4).assertNoErrors().assertNotComplete();
}
