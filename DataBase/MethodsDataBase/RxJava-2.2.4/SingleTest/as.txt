@Test public void as(){
  Single.just(1).as(new SingleConverter<Integer,Flowable<Integer>>(){
    @Override public Flowable<Integer> apply(    Single<Integer> v){
      return v.toFlowable();
    }
  }
).test().assertResult(1);
}
