@Test public void testCollectorFailureDoesNotResultInErrorAndCompletedEmissionsObservable(){
  final RuntimeException e=new RuntimeException();
  Burst.item(1).create().collect(callableListCreator(),biConsumerThrows(e)).toObservable().test().assertError(e).assertNotComplete();
}
