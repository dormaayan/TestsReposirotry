@Test public void testNoBackpressureSupport(){
  TestSubscriber<Long> ts=new TestSubscriber<Long>(0L);
  ts.request(100);
  infinite.take(500).subscribe(ts);
  assertEquals(500,ts.values().size());
  ts.assertNoErrors();
}
