@Test public void hasObservers(){
  UnicastProcessor<Integer> us=UnicastProcessor.create();
  assertFalse(us.hasSubscribers());
  TestSubscriber<Integer> ts=us.test();
  assertTrue(us.hasSubscribers());
  ts.cancel();
  assertFalse(us.hasSubscribers());
}
