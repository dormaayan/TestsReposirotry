@Test public void oneByOne(){
  MulticastProcessor<Integer> mp=MulticastProcessor.create(16);
  Flowable.range(1,1000).subscribe(mp);
  mp.rebatchRequests(1).test().assertValueCount(1000).assertNoErrors().assertComplete();
}
