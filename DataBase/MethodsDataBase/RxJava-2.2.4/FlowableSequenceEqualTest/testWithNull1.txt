@Test @Ignore("Null values not allowed") public void testWithNull1(){
  Single<Boolean> single=Flowable.sequenceEqual(Flowable.just((String)null),Flowable.just("one"));
  verifyResult(single,false);
}
