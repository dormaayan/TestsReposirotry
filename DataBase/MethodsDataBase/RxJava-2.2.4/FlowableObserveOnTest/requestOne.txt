@Test public void requestOne() throws Exception {
  TestSubscriber<Integer> ts=Flowable.range(1,5).observeOn(Schedulers.single()).test(1);
  Thread.sleep(100);
  ts.assertSubscribed().assertValue(1).assertNoErrors().assertNotComplete();
}
