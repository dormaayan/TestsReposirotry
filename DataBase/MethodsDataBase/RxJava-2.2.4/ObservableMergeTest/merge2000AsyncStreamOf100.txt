@Test public void merge2000AsyncStreamOf100(){
  TestObserver<Integer> to=new TestObserver<Integer>();
  mergeNAsyncStreamsOfN(2000,100).subscribe(to);
  to.awaitTerminalEvent();
  to.assertNoErrors();
  assertEquals(200000,to.values().size());
}
