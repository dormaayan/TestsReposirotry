@Test public void merge1SyncStreamOf1000000(){
  TestObserver<Integer> to=new TestObserver<Integer>();
  mergeNSyncStreamsOfN(1,1000000).subscribe(to);
  to.awaitTerminalEvent();
  to.assertNoErrors();
  assertEquals(1000000,to.values().size());
}
