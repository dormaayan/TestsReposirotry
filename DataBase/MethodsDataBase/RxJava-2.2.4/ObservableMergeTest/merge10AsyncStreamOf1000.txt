@Test public void merge10AsyncStreamOf1000(){
  TestObserver<Integer> to=new TestObserver<Integer>();
  mergeNAsyncStreamsOfN(10,1000).subscribe(to);
  to.awaitTerminalEvent();
  to.assertNoErrors();
  assertEquals(10000,to.values().size());
}
