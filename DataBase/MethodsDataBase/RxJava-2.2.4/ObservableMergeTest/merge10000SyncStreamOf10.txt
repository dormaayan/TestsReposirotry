@Test public void merge10000SyncStreamOf10(){
  TestObserver<Integer> to=new TestObserver<Integer>();
  mergeNSyncStreamsOfN(10000,10).subscribe(to);
  to.awaitTerminalEvent();
  to.assertNoErrors();
  assertEquals(100000,to.values().size());
}
