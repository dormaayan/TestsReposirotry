@Test @Ignore("Null values are not permitted") public void mergingNullObservable(){
  TestObserver<String> to=new TestObserver<String>();
  Observable.merge(Observable.just("one"),null).subscribe(to);
  to.assertNoErrors();
  to.assertValue("one");
}
