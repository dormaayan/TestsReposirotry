@Test public void merge1AsyncStreamOf1000(){
  TestObserver<Integer> to=new TestObserver<Integer>();
  mergeNAsyncStreamsOfN(1,1000).subscribe(to);
  to.awaitTerminalEvent();
  to.assertNoErrors();
  assertEquals(1000,to.values().size());
}
