@Test public void fused(){
  TestSubscriber<Long> ts=SubscriberFusion.newTest(QueueFuseable.ANY);
  Flowable.rangeLong(1,2).subscribe(ts);
  SubscriberFusion.assertFusion(ts,QueueFuseable.SYNC).assertResult(1L,2L);
}
