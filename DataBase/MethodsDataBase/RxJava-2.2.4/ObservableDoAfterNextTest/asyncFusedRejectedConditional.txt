@Test public void asyncFusedRejectedConditional(){
  TestObserver<Integer> to0=ObserverFusion.newTest(QueueFuseable.ASYNC);
  Observable.range(1,5).doAfterNext(afterNext).filter(Functions.alwaysTrue()).subscribe(to0);
  ObserverFusion.assertFusion(to0,QueueFuseable.NONE).assertResult(1,2,3,4,5);
  assertEquals(Arrays.asList(-1,-2,-3,-4,-5),values);
}
