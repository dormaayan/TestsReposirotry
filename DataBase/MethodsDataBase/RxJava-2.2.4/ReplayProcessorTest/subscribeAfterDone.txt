@Test public void subscribeAfterDone(){
  ReplayProcessor<Integer> rp=ReplayProcessor.create();
  rp.onComplete();
  BooleanSubscription bs=new BooleanSubscription();
  rp.onSubscribe(bs);
  assertTrue(bs.isCancelled());
}
