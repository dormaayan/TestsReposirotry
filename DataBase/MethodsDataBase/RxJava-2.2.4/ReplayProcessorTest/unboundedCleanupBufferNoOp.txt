@Test public void unboundedCleanupBufferNoOp(){
  ReplayProcessor<Integer> source=ReplayProcessor.create(1);
  source.onNext(1);
  source.onNext(2);
  source.cleanupBuffer();
  source.test().assertValuesOnly(1,2);
}
