@Test public void ambLots(){
  List<Maybe<Integer>> ms=new ArrayList<Maybe<Integer>>();
  for (int i=0; i < 32; i++) {
    ms.add(Maybe.<Integer>never());
  }
  ms.add(Maybe.just(1));
  Maybe.amb(ms).test().assertResult(1);
}
