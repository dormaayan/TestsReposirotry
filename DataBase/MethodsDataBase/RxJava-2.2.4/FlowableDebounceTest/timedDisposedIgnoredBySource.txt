@Test public void timedDisposedIgnoredBySource(){
  final TestSubscriber<Integer> ts=new TestSubscriber<Integer>();
  new Flowable<Integer>(){
    @Override protected void subscribeActual(    org.reactivestreams.Subscriber<? super Integer> s){
      s.onSubscribe(new BooleanSubscription());
      ts.cancel();
      s.onNext(1);
      s.onComplete();
    }
  }
.debounce(1,TimeUnit.SECONDS).subscribe(ts);
}
