@Test public void cancelledAndRun(){
  TestObserver<Long> to=new TestObserver<Long>();
  to.onSubscribe(Disposables.empty());
  TimerObserver tm=new TimerObserver(to);
  tm.dispose();
  tm.run();
  to.assertEmpty();
}
