@Test public void testCountZeroItemsFlowable(){
  Flowable<String> flowable=Flowable.empty();
  flowable.count().toFlowable().subscribe(w);
  verify(w).onNext(0L);
  verify(w,never()).onError(any(Throwable.class));
  verify(w,times(1)).onComplete();
}
