@SuppressWarnings({"rawtypes","unchecked"}) @Test public void singleGenericsSignatureTest(){
  A<String,Integer> a=new A<String,Integer>(){
  }
;
  Single.just(a).as((SingleConverter)ConverterTest.<String>testSingleConverterCreator());
}
