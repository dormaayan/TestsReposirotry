@Test public void syncFusedBoundary(){
  TestSubscriber<Integer> ts=SubscriberFusion.newTest(QueueFuseable.SYNC | QueueFuseable.BOUNDARY);
  Flowable.range(1,5).doFinally(this).subscribe(ts);
  SubscriberFusion.assertFusion(ts,QueueFuseable.NONE).assertResult(1,2,3,4,5);
  assertEquals(1,calls);
}
