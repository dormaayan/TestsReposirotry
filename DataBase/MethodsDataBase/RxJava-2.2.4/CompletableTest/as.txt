@Test public void as(){
  Completable.complete().as(new CompletableConverter<Flowable<Integer>>(){
    @Override public Flowable<Integer> apply(    Completable v){
      return v.toFlowable();
    }
  }
).test().assertComplete();
}
