@Test(timeout=5000) public void subscribeObserverNormal(){
  TestObserver<Object> to=new TestObserver<Object>();
  normal.completable.toObservable().subscribe(to);
  to.assertComplete();
  to.assertNoValues();
  to.assertNoErrors();
}
