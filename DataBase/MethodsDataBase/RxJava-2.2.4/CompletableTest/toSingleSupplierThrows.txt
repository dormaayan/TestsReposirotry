@Test(expected=TestException.class) public void toSingleSupplierThrows(){
  normal.completable.toSingle(new Callable<Object>(){
    @Override public Object call(){
      throw new TestException();
    }
  }
).blockingGet();
}
