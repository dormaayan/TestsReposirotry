@Test(timeout=5000) public void asNormal(){
  normal.completable.as(new CompletableConverter<Flowable<Object>>(){
    @Override public Flowable<Object> apply(    Completable c){
      return c.toFlowable();
    }
  }
).test().assertComplete().assertNoValues();
}
