@Test public void fusionRejected(){
  TestSubscriber<Integer> ts=SubscriberFusion.newTest(QueueFuseable.ANY);
  Flowable.just(1,2,3).hide().flatMapIterable(new Function<Integer,Iterable<Integer>>(){
    @Override public Iterable<Integer> apply(    Integer v) throws Exception {
      return Arrays.asList(1,2);
    }
  }
).subscribe(ts);
  SubscriberFusion.assertFusion(ts,QueueFuseable.NONE).assertResult(1,2,1,2,1,2);
}
