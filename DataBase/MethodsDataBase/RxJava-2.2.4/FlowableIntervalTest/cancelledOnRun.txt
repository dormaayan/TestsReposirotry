@Test public void cancelledOnRun(){
  TestSubscriber<Long> ts=new TestSubscriber<Long>();
  IntervalSubscriber is=new IntervalSubscriber(ts);
  ts.onSubscribe(is);
  is.cancel();
  is.run();
  ts.assertEmpty();
}
