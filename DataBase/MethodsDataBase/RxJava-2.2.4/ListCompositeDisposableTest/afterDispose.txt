@Test public void afterDispose(){
  ListCompositeDisposable lcd=new ListCompositeDisposable();
  lcd.dispose();
  Disposable d=Disposables.empty();
  assertFalse(lcd.add(d));
  assertTrue(d.isDisposed());
  d=Disposables.empty();
  assertFalse(lcd.addAll(d));
  assertTrue(d.isDisposed());
}
