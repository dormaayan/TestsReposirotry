@Test public void hasNextThrows(){
  Single.zip(new CrashingMappedIterable<Single<Integer>>(100,20,100,new Function<Integer,Single<Integer>>(){
    @Override public Single<Integer> apply(    Integer v) throws Exception {
      return Single.just(v);
    }
  }
),addString).test().assertFailureAndMessage(TestException.class,"hasNext()");
}
