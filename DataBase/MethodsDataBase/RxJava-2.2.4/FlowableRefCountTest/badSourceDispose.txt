@Test public void badSourceDispose(){
  BadFlowableDispose bf=new BadFlowableDispose();
  try {
    bf.refCount().test().cancel();
    fail("Should have thrown");
  }
 catch (  TestException expected) {
  }
}
