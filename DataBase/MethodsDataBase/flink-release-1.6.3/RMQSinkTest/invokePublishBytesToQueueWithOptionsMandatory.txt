@Test(expected=IllegalStateException.class) public void invokePublishBytesToQueueWithOptionsMandatory() throws Exception {
  RMQSink<String> rmqSink=createRMQSinkWithOptions(true,false);
  rmqSink.invoke(MESSAGE_STR,SinkContextUtil.forTimestamp(0));
}
