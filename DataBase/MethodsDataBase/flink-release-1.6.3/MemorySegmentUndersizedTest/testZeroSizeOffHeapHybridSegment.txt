@Test public void testZeroSizeOffHeapHybridSegment(){
  MemorySegment segment=new HybridMemorySegment(ByteBuffer.allocateDirect(0));
  testZeroSizeBuffer(segment);
  testSegmentWithSizeLargerZero(segment);
}
