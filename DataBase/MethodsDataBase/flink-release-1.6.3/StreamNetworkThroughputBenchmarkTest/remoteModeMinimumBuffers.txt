@Test public void remoteModeMinimumBuffers() throws Exception {
  StreamNetworkThroughputBenchmark env=new StreamNetworkThroughputBenchmark();
  int writers=2;
  int channels=2;
  env.setUp(writers,channels,100,false,writers * channels,writers * channels * TaskManagerOptions.NETWORK_BUFFERS_PER_CHANNEL.defaultValue());
  env.executeBenchmark(10_000);
  env.tearDown();
}
