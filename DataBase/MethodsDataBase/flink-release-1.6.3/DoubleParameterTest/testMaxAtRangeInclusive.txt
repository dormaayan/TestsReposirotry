@Test public void testMaxAtRangeInclusive(){
  parameter.setMaximumValue(0,true);
  parameter.configure(ParameterTool.fromArgs(new String[]{"--test","0"}));
  Assert.assertEquals(new Double(0),parameter.getValue());
}
