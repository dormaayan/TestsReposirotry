@Test public void testGetLongByField() throws Exception {
  Long value=r.nextLong();
  StormTuple<?> tuple=testGetByField(arity,index,value);
  Assert.assertSame(value,tuple.getLongByField(fieldName));
}
