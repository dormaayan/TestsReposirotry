@Test public void sourcesMustBeAccessible(){
  assertThatIllegalStateException().isThrownBy(() -> new SpringApplication(InaccessibleConfiguration.class).run()).withMessageContaining("Cannot load configuration");
}
