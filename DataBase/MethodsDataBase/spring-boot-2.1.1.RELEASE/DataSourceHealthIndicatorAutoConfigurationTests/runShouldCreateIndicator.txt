@Test public void runShouldCreateIndicator(){
  this.contextRunner.run((context) -> {
    context.getBean(DataSourceHealthIndicator.class);
    assertThat(context).hasSingleBean(DataSourceHealthIndicator.class).doesNotHaveBean(ApplicationHealthIndicator.class);
  }
);
}
