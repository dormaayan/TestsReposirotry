@Test public void parseSimpleWhenUnknownUnitShouldThrowException(){
  try {
    DurationStyle.SIMPLE.parse("10mb");
    fail("Did not throw");
  }
 catch (  IllegalArgumentException ex) {
    assertThat(ex.getCause().getMessage()).isEqualTo("Unknown unit 'mb'");
  }
}
