@Test public void handlesNoSuchCommand(){
  int status=this.commandRunner.runAndHandleErrors("missing");
  assertThat(status).isEqualTo(1);
  assertThat(this.calls).containsOnly(Call.ERROR_MESSAGE);
}
