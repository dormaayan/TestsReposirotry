@Test public void invalidJdbcTypeOptUsingExpression(){
  try {
    new ParameterExpression("(expression)+");
    Assert.fail();
  }
 catch (  BuilderException e) {
    Assert.assertTrue(e.getMessage().contains("Parsing error in {(expression)+} in position 12"));
  }
}
