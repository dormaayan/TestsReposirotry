@Test public void invalidOldJdbcTypeFormat(){
  try {
    new ParameterExpression("id:");
    Assert.fail();
  }
 catch (  BuilderException e) {
    Assert.assertTrue(e.getMessage().contains("Parsing error in {id:} in position 3"));
  }
}
