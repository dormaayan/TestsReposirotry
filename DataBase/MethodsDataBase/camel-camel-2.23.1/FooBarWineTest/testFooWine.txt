@Test public void testFooWine() throws Exception {
  MockEndpoint mock=getMockEndpoint("mock:wine");
  mock.expectedMinimumMessageCount(2);
  mock.setAssertPeriod(500);
  assertMockEndpointsSatisfied();
}
