@Test public void shouldExecuteSISMEMBER() throws Exception {
  when(setOperations.isMember(anyString(),any())).thenReturn(true);
  Object result=sendHeaders(RedisConstants.COMMAND,"SISMEMBER",RedisConstants.KEY,"key",RedisConstants.VALUE,"set");
  verify(setOperations).isMember("key","set");
  assertEquals(true,result);
}
