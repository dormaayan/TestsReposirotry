@Test public void testParameterVendorType(){
  Template template=parser.parseTemplate("FOO('p_instance_id' 2 ${header.foo})");
  assertEquals(2,((InParameter)template.getParameterList().get(0)).getSqlType());
}
