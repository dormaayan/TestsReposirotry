@Test public void testSimpleEqOperator() throws Exception {
  exchange.getIn().setBody("Camel");
  assertPredicate(exchange,"[[body]] == 'Tiger'",false);
  assertPredicate(exchange,"[[body]] == 'Camel'",true);
  assertPredicate(exchange,"[[body]] == \"Tiger\"",false);
  assertPredicate(exchange,"[[body]] == \"Camel\"",true);
}
