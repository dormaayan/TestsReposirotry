/** 
 * Tests that only one aggregator thread is created if a RuntimeException in the next method of a custom iterator occurs.
 */
@Test public void testSplitErrorInNext() throws Exception {
  execute("direct:errorInNext");
}
