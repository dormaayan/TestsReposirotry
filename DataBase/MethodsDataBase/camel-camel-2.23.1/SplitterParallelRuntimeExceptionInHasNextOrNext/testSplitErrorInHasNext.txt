/** 
 * Tests that only one aggregator thread is created if a RuntimeException in the hasNext method of a custom iterator occurs.
 */
@Test public void testSplitErrorInHasNext() throws Exception {
  execute("direct:errorInHasNext");
}
