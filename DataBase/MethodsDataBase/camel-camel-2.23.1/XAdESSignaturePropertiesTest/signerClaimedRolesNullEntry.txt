@Test(expected=IllegalArgumentException.class) public void signerClaimedRolesNullEntry() throws Exception {
  new XAdESSignatureProperties().setSignerClaimedRoles(Collections.<String>singletonList(null));
}
