@Test public void shouldLoadSwaggerPetStoreModel() throws Exception {
  SwaggerRestProducerFactory factory=new SwaggerRestProducerFactory();
  factory.loadSwaggerModel(new DefaultCamelContext(),"petstore.json");
}
