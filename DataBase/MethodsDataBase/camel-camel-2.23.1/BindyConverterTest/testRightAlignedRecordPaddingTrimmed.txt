@Test @DirtiesContext public void testRightAlignedRecordPaddingTrimmed() throws Exception {
  AllCombinations data=sendAndRecieveAllCombinations();
  assertThat("Right aligned, padding trimmed",data.field7,Is.is("f7"));
}
