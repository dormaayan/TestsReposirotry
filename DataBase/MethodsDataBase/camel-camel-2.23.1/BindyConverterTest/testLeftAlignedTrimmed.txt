@Test @DirtiesContext public void testLeftAlignedTrimmed() throws Exception {
  AllCombinations data=sendAndRecieveAllCombinations();
  assertThat("Left aligned, padding trimmed",data.field4,Is.is("f4"));
}
