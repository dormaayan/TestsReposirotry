@Test public void testElExpressions() throws Exception {
  assertExpression("${exchange}",exchange);
  assertExpression("${in.headers.foo}","abc");
  assertExpression("${in.body}","<hello id='m123'>world!</hello>");
}
