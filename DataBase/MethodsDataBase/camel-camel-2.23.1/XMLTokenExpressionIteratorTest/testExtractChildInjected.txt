@Test public void testExtractChildInjected() throws Exception {
  String[] result=RESULTS_CHILD_JAVA8;
  if (isJava7OrLower()) {
    result=RESULTS_CHILD;
  }
  invokeAndVerify("//C:child",'i',new ByteArrayInputStream(TEST_BODY),result);
}
