@Test public void testGetEndpointNoScheme() throws Exception {
  DefaultCamelContext ctx=new DefaultCamelContext();
  ctx.disableJMX();
  Endpoint endpoint=ctx.getEndpoint("log");
  assertNotNull(endpoint);
}
