@Test public void testStoreOidOnSaveWithoutId() throws Exception {
  Document document=new Document();
  ObjectId oid=template.requestBody("direct:testStoreOidOnSave",document,ObjectId.class);
  assertNotNull(oid);
}
