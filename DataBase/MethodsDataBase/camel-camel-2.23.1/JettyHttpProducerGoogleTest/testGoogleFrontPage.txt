@Test @Ignore("ignore online tests") public void testGoogleFrontPage() throws Exception {
  if (isPlatform("windows")) {
    return;
  }
  String reply=template.requestBody("direct:start",null,String.class);
  assertNotNull(reply);
}
