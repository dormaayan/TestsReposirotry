@Test public void testSpringBuilderRefProperties() throws Exception {
  getMockEndpoint("{{result}}").expectedMessageCount(1);
  template.sendBody("direct:start","Hello World");
  assertMockEndpointsSatisfied();
}
