@Test public void testNoBlobNameProducerWithOp() throws Exception {
  registerCredentials();
  BlobServiceComponent component=new BlobServiceComponent(context);
  BlobServiceEndpoint endpointWithOp=(BlobServiceEndpoint)component.createEndpoint("azure-blob://camelazure/container?operation=deleteBlob&credentials=#creds");
  try {
    endpointWithOp.createProducer();
    fail();
  }
 catch (  IllegalArgumentException ex) {
    assertEquals("Blob name must be specified.",ex.getMessage());
  }
}
