@Test public void testManually() throws Exception {
  MockEndpoint mock=getMockEndpoint("mock:result");
  mock.setSleepForEmptyTest(100);
  mock.expectedMessageCount(0);
  mock.assertIsSatisfied();
}
