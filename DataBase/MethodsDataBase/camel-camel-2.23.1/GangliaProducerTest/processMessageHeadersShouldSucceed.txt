@Test public void processMessageHeadersShouldSucceed() throws Exception {
  gangliaProducer.process(mockExchange);
  verify(mockPublisher).publish(GROUP_NAME,METRIC_NAME,BODY,TYPE,SLOPE,T_MAX,D_MAX,UNITS);
}
