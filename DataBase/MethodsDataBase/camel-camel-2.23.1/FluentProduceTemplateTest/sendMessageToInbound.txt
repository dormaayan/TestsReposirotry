@Test @InSequence(2) public void sendMessageToInbound(@Uri("direct:inbound") FluentProducerTemplate in,@Uri("mock:outbound") MockEndpoint out) throws InterruptedException {
  out.expectedMessageCount(1);
  out.expectedBodiesReceived("test-processed");
  in.withBody("test").send();
  assertIsSatisfied(2L,TimeUnit.SECONDS,out);
}
