@Test public void testAsyncScannerWithoutCaching() throws Exception {
  testAsyncScanner(TableName.valueOf(name.getMethodName()),5,1,1,1,(b) -> {
    try {
      TimeUnit.MILLISECONDS.sleep(500);
    }
 catch (    InterruptedException ex) {
    }
  }
);
}
