/** 
 * Test a case when the column specified in command line arguments is exclusive for few rows and also a row range filter is specified
 * @throws Exception
 */
@Test public void testRowCounterColumnAndRowRange() throws Exception {
  String[] args=new String[]{TABLE_NAME,"--range=\\x00rov,\\x00rox",COL_FAM + ":" + COL1};
  runRowCount(args,8);
}
