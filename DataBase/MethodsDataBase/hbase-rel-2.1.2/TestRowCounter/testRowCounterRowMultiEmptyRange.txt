/** 
 * Test a case when a range is specified with multiple ranges of start-end keys; one range is filled, another two are not
 * @throws Exception
 */
@Test public void testRowCounterRowMultiEmptyRange() throws Exception {
  String[] args=new String[]{TABLE_NAME,"--range=\\x00row1,\\x00row3;;"};
  runRowCount(args,2);
}
