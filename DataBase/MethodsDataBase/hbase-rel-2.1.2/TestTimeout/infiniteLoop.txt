/** 
 * Enable to check if timeout works. Can't enable as it waits 30seconds and expected doesn't do Exception catching
 */
@Ignore @Test public void infiniteLoop(){
  Thread t=new Thread("HangingThread"){
    public void run(){
synchronized (this) {
        while (true) {
        }
      }
    }
  }
;
  t.start();
  while (true) {
  }
}
