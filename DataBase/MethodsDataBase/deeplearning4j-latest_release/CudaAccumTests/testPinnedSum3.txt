@Test public void testPinnedSum3() throws Exception {
  INDArray array1=Nd4j.linspace(1,100000,100000).reshape(100,1000);
  for (int x=0; x < 100000; x++) {
    assertEquals("Failed on iteration [" + x + "]",x + 1,array1.getFloat(x),0.01f);
  }
}
