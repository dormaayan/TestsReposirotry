@Test public void getTotalRecordsTest() throws Exception {
  assertThat(1,is(getTotalRecords()));
  INDArray load=hsqlLoader.load(hsqlLoader.loadForID("1"));
  assertNotNull(load);
  assertEquals(Nd4j.linspace(1,4,4),load);
}
