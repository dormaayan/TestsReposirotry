/** 
 * Here we just check formatting for octets
 */
@Test public void testFormat1() throws Exception {
  for (int i=0; i < 256; i++) {
    String octet=NetworkOrganizer.toBinaryOctet(i);
    assertEquals(8,octet.length());
    log.trace("i: {}; Octet: {}",i,octet);
  }
}
