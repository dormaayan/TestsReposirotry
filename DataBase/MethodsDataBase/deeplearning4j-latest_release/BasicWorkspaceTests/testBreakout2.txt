@Test public void testBreakout2() throws Exception {
  assertEquals(null,Nd4j.getMemoryManager().getCurrentWorkspace());
  INDArray scoped=outScope2();
  assertEquals(null,scoped);
  assertEquals(null,Nd4j.getMemoryManager().getCurrentWorkspace());
}
