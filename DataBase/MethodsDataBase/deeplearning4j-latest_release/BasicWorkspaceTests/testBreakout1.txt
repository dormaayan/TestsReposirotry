@Test public void testBreakout1() throws Exception {
  assertEquals(null,Nd4j.getMemoryManager().getCurrentWorkspace());
  INDArray scoped=outScope1();
  assertEquals(true,scoped.isAttached());
  assertEquals(null,Nd4j.getMemoryManager().getCurrentWorkspace());
}
