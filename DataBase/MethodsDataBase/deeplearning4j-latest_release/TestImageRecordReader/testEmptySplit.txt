@Test(expected=IllegalArgumentException.class) public void testEmptySplit() throws IOException {
  InputSplit data=new CollectionInputSplit(new ArrayList<URI>());
  new ImageRecordReader().initialize(data,null);
}
