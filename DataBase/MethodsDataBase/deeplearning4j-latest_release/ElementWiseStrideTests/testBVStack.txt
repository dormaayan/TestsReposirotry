@Test public void testBVStack() throws Exception {
  INDArray[] arr=new INDArray[5];
  for (int i=0; i < arr.length; i++) {
    arr[i]=Nd4j.create(new int[]{1,5749},'c');
  }
  Nd4j.vstack(arr);
  Nd4j.create(1);
}
