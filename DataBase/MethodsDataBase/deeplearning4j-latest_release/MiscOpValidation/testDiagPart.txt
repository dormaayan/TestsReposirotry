@Test public void testDiagPart(){
  INDArray i=Nd4j.create(5,5);
  SameDiff sd=SameDiff.create();
  SDVariable var=sd.var("in",i);
  SDVariable diag=sd.diagPart(var);
  INDArray out=sd.execAndEndResult();
  assertEquals(1,out.rank());
}
