@Test public void testLinspace(){
  SameDiff sd=SameDiff.create();
  SDVariable out=sd.linspace("linspace",1,10,10);
  SDVariable loss=out.std(true);
  String err=OpValidation.validate(new TestCase(sd).expected(out,Nd4j.linspace(1,10,10)));
  assertNull(err);
}
