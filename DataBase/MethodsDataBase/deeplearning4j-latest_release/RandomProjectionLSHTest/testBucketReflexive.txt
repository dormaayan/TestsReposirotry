@Test public void testBucketReflexive(){
  rpLSH.makeIndex(inputs);
  int idx=(new Random(12345)).nextInt(100);
  INDArray row=inputs.getRow(idx);
  assertEquals(1.0f,rpLSH.bucket(row).maxNumber().floatValue(),1e-3f);
}
