@Test public void testByteArrayOrder1(){
  val ndarray=Nd4j.create(2).assign(1);
  assertEquals(DataBuffer.Type.FLOAT,ndarray.data().dataType());
  val array=ndarray.data().asBytes();
  assertEquals(8,array.length);
}
