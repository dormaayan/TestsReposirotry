@Test public void testUnstackEdgeCase2(){
  for (int i=0; i < 3; i++) {
    INDArray arr=Nd4j.rand(new long[]{1,1,1});
    List<long[]> shapes=Nd4j.getExecutioner().calculateOutputShape(new Unstack(arr,null,i));
    assertEquals(1,shapes.size());
    assertArrayEquals(new long[]{1,1},shapes.get(0));
  }
}
