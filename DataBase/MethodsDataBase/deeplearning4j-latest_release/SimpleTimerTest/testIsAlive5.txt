@Test public void testIsAlive5() throws Exception {
  SimpleTimer timer=new SimpleTimer(10,TimeUnit.SECONDS);
  timer.triggerEvent();
  timer.triggerEvent();
  Thread.sleep(1100);
  timer.triggerEvent();
  timer.triggerEvent();
  Thread.sleep(1100);
  timer.triggerEvent();
  timer.triggerEvent();
  assertEquals(6,timer.getNumberOfEvents());
  Thread.sleep(9000);
  assertEquals(2,timer.getNumberOfEvents());
}
