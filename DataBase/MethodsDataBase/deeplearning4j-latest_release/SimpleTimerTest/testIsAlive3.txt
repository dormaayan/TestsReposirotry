@Test public void testIsAlive3() throws Exception {
  SimpleTimer timer=new SimpleTimer(2,TimeUnit.SECONDS);
  timer.triggerEvent();
  timer.triggerEvent();
  assertEquals(2,timer.getNumberOfEvents());
}
