@Test public void testReverse2(){
  INDArray in=Nd4j.trueVector(new double[]{1,2,3,4,5,6});
  INDArray axis=Nd4j.trueScalar(0);
  INDArray out=Nd4j.create(new long[]{6});
  DynamicCustomOp op=DynamicCustomOp.builder("reverse").addInputs(in,axis).addOutputs(out).build();
  Nd4j.getExecutioner().exec(op);
  System.out.println(out);
}
