@Test public void testEyeShape(){
  DynamicCustomOp dco=DynamicCustomOp.builder("eye").addIntegerArguments(3,3).build();
  List<long[]> list=Nd4j.getExecutioner().calculateOutputShape(dco);
  assertEquals(1,list.size());
  assertArrayEquals(new long[]{3,3},list.get(0));
}
