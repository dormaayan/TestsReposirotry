@Test public void testGetRequiredMemory3() throws Exception {
  AllocationShape shape=new AllocationShape();
  shape.setOffset(0);
  shape.setLength(10);
  shape.setStride(2);
  shape.setDataType(DataBuffer.Type.FLOAT);
  assertEquals(80,AllocationUtils.getRequiredMemory(shape));
}
