@Test public void testSimpleBaseFormKnownWord(){
  List<Token> tokens=tokenizer.tokenize("お寿司が食べたい。");
  assertTrue(tokens.size() == 6);
  assertEquals("食べ",tokens.get(3).getSurface());
  assertEquals("食べる",tokens.get(3).getBaseForm());
}
