@Test public void testAudoBroadcastAddMatrix(){
  INDArray arr=Nd4j.linspace(1,4,4).reshape(2,2);
  INDArray row=Nd4j.ones(2);
  INDArray assertion=arr.add(1.0);
  INDArray test=arr.add(row);
  assertEquals(assertion,test);
}
