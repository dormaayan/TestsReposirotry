/** 
 * This test fails, but that's ok. It's here only as reminder, that in some cases we can have EWS==1 for better performances.
 * @throws Exception
 */
@Test @Ignore public void testTadEws() throws Exception {
  INDArray array=Nd4j.create(32,5,10);
  assertEquals(1,array.tensorAlongDimension(0,1,2).elementWiseStride());
}
