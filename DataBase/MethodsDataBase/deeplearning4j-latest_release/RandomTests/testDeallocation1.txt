@Ignore @Test public void testDeallocation1() throws Exception {
  while (true) {
    Random random1=Nd4j.getRandomFactory().getNewRandomInstance(119);
    random1.nextInt();
    System.gc();
    Thread.sleep(50);
  }
}
