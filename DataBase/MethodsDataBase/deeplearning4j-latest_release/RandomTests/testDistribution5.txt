@Test public void testDistribution5() throws Exception {
  for (int i=0; i < 100; i++) {
    Nd4j.getRandom().setSeed(120);
    INDArray z1=Nd4j.rand('f',new int[]{1,1000});
    Nd4j.getRandom().setSeed(120);
    INDArray z2=Nd4j.rand('c',new int[]{1,1000});
    assertEquals("Failed on iteration " + i,z1,z2);
  }
}
