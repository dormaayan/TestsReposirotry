@Test public void testConvolution2DLayer() throws Exception {
  buildConvolution2DLayer(conf1,keras1,false);
  buildConvolution2DLayer(conf2,keras2,false);
  buildConvolution2DLayer(conf2,keras2,true);
}
