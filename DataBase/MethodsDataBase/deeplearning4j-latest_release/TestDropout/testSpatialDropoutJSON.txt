@Test public void testSpatialDropoutJSON(){
  MultiLayerConfiguration conf=new NeuralNetConfiguration.Builder().list().layer(new DropoutLayer.Builder(new SpatialDropout(0.5)).build()).build();
  String asJson=conf.toJson();
  MultiLayerConfiguration fromJson=MultiLayerConfiguration.fromJson(asJson);
  assertEquals(conf,fromJson);
}
