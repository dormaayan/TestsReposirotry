@Test public void testSum2d(){
  INDArray arr=Nd4j.ones(10,10);
  INDArray sum0=arr.sum(0);
  assertArrayEquals(sum0.shape(),new long[]{1,10});
  INDArray sum1=arr.sum(1);
  assertArrayEquals(sum1.shape(),new long[]{10,1});
}
