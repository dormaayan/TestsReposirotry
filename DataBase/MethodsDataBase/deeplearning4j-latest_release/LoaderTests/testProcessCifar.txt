@Ignore @Test public void testProcessCifar(){
  int row=32;
  int col=32;
  int channels=3;
  CifarLoader cifar=new CifarLoader(row,col,channels,null,true,true,false);
  DataSet result=cifar.next(1);
  assertEquals(result.getFeatures().length(),32 * 32 * 3,0.0);
}
