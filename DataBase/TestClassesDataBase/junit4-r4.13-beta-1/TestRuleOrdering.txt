public static class TestRuleOrdering {
  @Rule(order=1) public final TestRule a=new LoggingTestRule(ruleLog,"outer");
  @Rule(order=2) public final TestRule z=new LoggingTestRule(ruleLog,"inner");
  @Test public void foo(){
    ruleLog.append(" foo");
  }
}
