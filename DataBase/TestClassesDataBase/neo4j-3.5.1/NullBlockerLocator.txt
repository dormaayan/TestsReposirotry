private static class NullBlockerLocator implements BlockerLocator {
  @Override public Iterable<AdminCommand.Blocker> findBlockers(  String name) throws NoSuchElementException {
    return Collections.emptyList();
  }
}
