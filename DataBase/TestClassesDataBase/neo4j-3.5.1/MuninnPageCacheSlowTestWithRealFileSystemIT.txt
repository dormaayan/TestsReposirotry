@ExtendWith(TestDirectoryExtension.class) class MuninnPageCacheSlowTestWithRealFileSystemIT extends MuninnPageCacheSlowIT {
  @Inject TestDirectory directory;
  @Override protected Fixture<MuninnPageCache> createFixture(){
    return super.createFixture().withFileSystemAbstraction(DefaultFileSystemAbstraction::new).withFileConstructor(directory::file);
  }
}
