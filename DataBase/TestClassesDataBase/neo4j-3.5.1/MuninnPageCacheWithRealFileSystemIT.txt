@ExtendWith(TestDirectoryExtension.class) class MuninnPageCacheWithRealFileSystemIT extends MuninnPageCacheTest {
  @Inject TestDirectory directory;
  @Override protected Fixture<MuninnPageCache> createFixture(){
    return super.createFixture().withFileSystemAbstraction(DefaultFileSystemAbstraction::new).withFileConstructor(pathname -> directory.file(pathname));
  }
}
