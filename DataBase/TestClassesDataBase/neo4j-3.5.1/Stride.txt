/** 
 * Strides predictably: 1, 2, 3, ..., MAX, 1, 2, 3, ... a.s.o
 */
private static class Stride {
  private int stride;
  private final int max=5;
  public int next(){
    return (stride++ % max) + 1;
  }
}
