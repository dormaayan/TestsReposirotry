/** 
 * SQLServerWallTest
 * @author RaymondXiu
 * @version 1.0, 2012-3-18
 * @see
 */
public class TAEWallTest extends TestCase {
  public void test_true() throws Exception {
    Assert.assertTrue(WallUtils.isValidateMySql("select * from t where 1=1 AND status = 1"));
  }
  public void test_false() throws Exception {
    Assert.assertFalse(WallUtils.isValidateMySql("select * from t where status = 1 AND 1=1"));
  }
}
