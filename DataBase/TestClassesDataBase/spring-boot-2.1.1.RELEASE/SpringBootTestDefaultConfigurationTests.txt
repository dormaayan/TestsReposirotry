/** 
 * Tests for  {@link SpringBootTest} (detectDefaultConfigurationClasses).
 * @author Dave Syer
 */
@RunWith(SpringRunner.class) @DirtiesContext public class SpringBootTestDefaultConfigurationTests {
  @Autowired private Config config;
  @Test public void nestedConfigClasses(){
    assertThat(this.config).isNotNull();
  }
@Configuration protected static class Config {
  }
}
