/** 
 * Test  {@link SpyBean} on a test class field can be used to inject new spy instances.
 * @author Phillip Webb
 */
@RunWith(SpringRunner.class) public class SpyBeanOnTestFieldForNewBeanIntegrationTests {
  @SpyBean private SimpleExampleService exampleService;
  @Autowired private ExampleServiceCaller caller;
  @Test public void testSpying(){
    assertThat(this.caller.sayGreeting()).isEqualTo("I say simple");
    verify(this.caller.getService()).greeting();
  }
@Configuration @Import(ExampleServiceCaller.class) static class Config {
  }
}
