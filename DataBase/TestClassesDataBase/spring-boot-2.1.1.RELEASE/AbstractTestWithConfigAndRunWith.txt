/** 
 * Abstract test with nest  {@code @Configuration} and {@code @RunWith} used by{@link TestTypeExcludeFilter}.
 * @author Phillip Webb
 */
@RunWith(SpringRunner.class) public abstract class AbstractTestWithConfigAndRunWith {
@Configuration static class Config {
  }
}
