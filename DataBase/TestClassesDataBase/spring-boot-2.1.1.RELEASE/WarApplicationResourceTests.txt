@RunWith(SpringRunner.class) @SpringBootTest public class WarApplicationResourceTests {
  @Value("${demo.string.value}") private String demoStringValue;
  @Test public void contextLoads(){
    assertThat(this.demoStringValue).isEqualTo("demo");
  }
}
