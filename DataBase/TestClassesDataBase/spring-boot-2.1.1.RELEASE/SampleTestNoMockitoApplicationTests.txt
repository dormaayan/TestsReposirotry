/** 
 * Tests that  {@code ResetMocksTestExecutionListener} and{@code MockitoTestExecutionListener} gracefully degrade when Mockito is not on theclasspath.
 * @author Madhura Bhave
 */
@RunWith(SpringRunner.class) public class SampleTestNoMockitoApplicationTests {
  @Autowired private ApplicationContext context;
  @Test public void contextLoads() throws Exception {
    assertThat(this.context).isNotNull();
  }
}
