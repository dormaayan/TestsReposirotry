/** 
 * Example integration test that uses JMX.
 * @author Stephane Nicoll
 */
@SuppressWarnings("unused") @RunWith(SpringRunner.class) @SpringBootTest(properties="spring.jmx.enabled=true") @DirtiesContext public class SampleJmxTests {
  @Autowired private MBeanServer mBeanServer;
  @Test public void exampleTest(){
  }
}
