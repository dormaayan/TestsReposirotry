/** 
 * Tests for the  {@link WebFluxTest#properties properties} attribute of{@link WebFluxTest @WebFluxTest}.
 * @author Artsiom Yudovin
 */
@RunWith(SpringRunner.class) @WebFluxTest(properties="spring.profiles.active=test") public class WebFluxTestPropertiesIntegrationTests {
  @Autowired private Environment environment;
  @Test public void environmentWithNewProfile(){
    assertThat(this.environment.getActiveProfiles()).containsExactly("test");
  }
}
