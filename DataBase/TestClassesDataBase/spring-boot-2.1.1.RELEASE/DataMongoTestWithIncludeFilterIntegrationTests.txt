/** 
 * Integration test with custom include filter for  {@link DataMongoTest}.
 * @author Michael Simons
 */
@RunWith(SpringRunner.class) @DataMongoTest(includeFilters=@Filter(Service.class)) public class DataMongoTestWithIncludeFilterIntegrationTests {
  @Autowired private ExampleService service;
  @Test public void testService(){
    assertThat(this.service.hasCollection("foobar")).isFalse();
  }
}
