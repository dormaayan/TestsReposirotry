@Configuration static class ExitCodeCommandLineRunConfig {
  @Bean public CommandLineRunner runner(){
    return (args) -> {
      throw new IllegalStateException(new ExitStatusException());
    }
;
  }
}
