/** 
 * Tests for the  {@link DataMongoTest#properties properties} attribute of{@link DataMongoTest @DataMongoTest}.
 * @author Artsiom Yudovin
 */
@RunWith(SpringRunner.class) @DataMongoTest(properties="spring.profiles.active=test") public class DataMongoTestPropertiesIntegrationTests {
  @Autowired private Environment environment;
  @Test public void environmentWithNewProfile(){
    assertThat(this.environment.getActiveProfiles()).containsExactly("test");
  }
}
