/** 
 * Tests for the  {@link DataJdbcTest#properties properties} attribute of{@link DataJdbcTest @DataJdbcTest}.
 * @author Andy Wilkinson
 */
@RunWith(SpringRunner.class) @DataJdbcTest(properties="spring.profiles.active=test") public class DataJdbcTestPropertiesIntegrationTests {
  @Autowired private Environment environment;
  @Test public void environmentWithNewProfile(){
    assertThat(this.environment.getActiveProfiles()).containsExactly("test");
  }
}
