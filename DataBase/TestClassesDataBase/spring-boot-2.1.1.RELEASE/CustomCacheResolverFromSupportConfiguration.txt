@Configuration @EnableCaching static class CustomCacheResolverFromSupportConfiguration extends CachingConfigurerSupport {
  @Override @Bean public CacheResolver cacheResolver(){
    return (context) -> Collections.singleton(mock(Cache.class));
  }
}
