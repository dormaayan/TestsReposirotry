/** 
 * Tests for the  {@link JdbcTest#properties properties} attribute of{@link JdbcTest @JdbcTest}.
 * @author Artsiom Yudovin
 */
@RunWith(SpringRunner.class) @JdbcTest(properties="spring.profiles.active=test") public class JdbcTestPropertiesIntegrationTests {
  @Autowired private Environment environment;
  @Test public void environmentWithNewProfile(){
    assertThat(this.environment.getActiveProfiles()).containsExactly("test");
  }
}
