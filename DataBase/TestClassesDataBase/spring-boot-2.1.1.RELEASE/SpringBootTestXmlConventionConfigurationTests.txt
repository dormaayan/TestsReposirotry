/** 
 * Tests for  {@link SpringBootTest} finding XML config.
 * @author Phillip Webb
 */
@RunWith(SpringRunner.class) @DirtiesContext @SpringBootTest public class SpringBootTestXmlConventionConfigurationTests {
  @Autowired private String foo;
  @Test public void xmlConfigLoaded(){
    assertThat(this.foo).isEqualTo("World");
  }
}
