/** 
 * Tests for  {@link SampleLdapApplication}.
 * @author Phillip Webb
 */
@RunWith(SpringRunner.class) @SpringBootTest public class SampleLdapApplicationTests {
  @ClassRule public static OutputCapture outputCapture=new OutputCapture();
  @Test public void testDefaultSettings(){
    String output=outputCapture.toString();
    assertThat(output).contains("cn=Alice Smith");
  }
}
