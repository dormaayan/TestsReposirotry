/** 
 * Tests for  {@link SpringBootTest} with active profiles. See gh-1469.
 * @author Phillip Webb
 */
@RunWith(SpringRunner.class) @DirtiesContext @SpringBootTest("spring.config.name=enableother") @ActiveProfiles("override") public class SpringBootTestActiveProfileTests {
  @Autowired private ApplicationContext context;
  @Test public void profiles(){
    assertThat(this.context.getEnvironment().getActiveProfiles()).containsExactly("override");
  }
@Configuration protected static class Config {
  }
}
