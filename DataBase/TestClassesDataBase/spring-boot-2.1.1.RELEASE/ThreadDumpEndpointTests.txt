/** 
 * Tests for  {@link ThreadDumpEndpoint}.
 * @author Phillip Webb
 * @author Andy Wilkinson
 */
public class ThreadDumpEndpointTests {
  @Test public void dumpThreads(){
    assertThat(new ThreadDumpEndpoint().threadDump().getThreads().size()).isGreaterThan(0);
  }
}
