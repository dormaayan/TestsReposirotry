public class OpenSslServerContextTest extends SslContextTest {
  @BeforeClass public static void checkOpenSsl(){
    assumeTrue(OpenSsl.isAvailable());
  }
  @Override protected SslContext newServerContext(  File crtFile,  File keyFile,  String pass) throws SSLException {
    Assume.assumeTrue(OpenSsl.isAvailable());
    return new OpenSslServerContext(crtFile,keyFile,pass);
  }
}
