public class LittleEndianUnsafeNoCleanerDirectByteBufTest extends LittleEndianDirectByteBufTest {
  @Before @Override public void init(){
    Assume.assumeTrue("java.nio.DirectByteBuffer.<init>(long, int) not found, skip tests",PlatformDependent.useDirectBufferNoCleaner());
    super.init();
  }
  @Override protected ByteBuf newBuffer(  int length,  int maxCapacity){
    return new UnpooledUnsafeNoCleanerDirectByteBuf(UnpooledByteBufAllocator.DEFAULT,length,maxCapacity);
  }
}
