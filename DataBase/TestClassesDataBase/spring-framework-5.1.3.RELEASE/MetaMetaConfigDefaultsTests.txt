/** 
 * Integration tests for meta-meta-annotation support, relying on default attribute values defined in  {@link ConfigClassesAndProfilesWithCustomDefaultsMetaConfig} andoverrides in  {@link MetaMetaConfig}.
 * @author Sam Brannen
 * @since 4.0.3
 */
@RunWith(SpringJUnit4ClassRunner.class) @MetaMetaConfig public class MetaMetaConfigDefaultsTests {
  @Autowired private String foo;
  @Test public void foo(){
    assertEquals("Production Foo",foo);
  }
}
