/** 
 * Integration tests that verify support for  {@link ApplicationContextInitializer ApplicationContextInitializers} in conjunction with XML configuration filesin the TestContext framework.
 * @author Sam Brannen
 * @since 3.2
 */
@RunWith(SpringJUnit4ClassRunner.class) @ContextConfiguration(initializers={FooBarAliasInitializer.class,DevProfileInitializer.class}) public class MultipleInitializersXmlConfigTests {
  @Autowired private String foo, bar, baz;
  @Test public void activeBeans(){
    assertEquals("foo",foo);
    assertEquals("foo",bar);
    assertEquals("dev profile config",baz);
  }
}
