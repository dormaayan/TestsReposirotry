public class FlowableAutoConnectTest {
  @Test public void autoConnectImmediately(){
    PublishProcessor<Integer> pp=PublishProcessor.create();
    pp.publish().autoConnect(0);
    assertTrue(pp.hasSubscribers());
  }
}
