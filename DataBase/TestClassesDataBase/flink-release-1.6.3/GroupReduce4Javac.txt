@ForwardedFields("f0->*") private static class GroupReduce4Javac implements GroupReduceFunction<Tuple2<Long,Long>,Long> {
  @SuppressWarnings("unchecked") @Override public void reduce(  Iterable<Tuple2<Long,Long>> values,  Collector<Long> out) throws Exception {
    Long id=0L;
    @SuppressWarnings("rawtypes") Iterator it=values.iterator();
    if (it.hasNext()) {
      id=((Tuple2<Long,Long>)it.next()).f0;
    }
 else {
      System.out.println("hello world");
    }
    out.collect(id);
  }
}
