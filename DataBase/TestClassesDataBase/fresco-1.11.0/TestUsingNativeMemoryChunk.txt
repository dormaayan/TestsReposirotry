/** 
 * Base class for tests that indirectly use NativeMemoryChunk. <p>These need to stub out the native code called from NMC's static initializer.
 */
public class TestUsingNativeMemoryChunk {
  @BeforeClass public static void allowNativeStaticInitializers(){
    SoLoader.setInTestMode();
  }
}
