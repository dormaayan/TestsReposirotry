public class TestMapredParquetInputFormat {
  @Test public void testDefaultConstructor(){
    new MapredParquetInputFormat();
  }
  @SuppressWarnings("unchecked") @Test public void testConstructorWithParquetInputFormat(){
    new MapredParquetInputFormat((ParquetInputFormat<ArrayWritable>)mock(ParquetInputFormat.class));
  }
}
