@Data public static class CheckModelsListener extends BaseTrainingListener {
  private Set<Class<?>> modelClasses=new HashSet<>();
  @Override public void iterationDone(  Model model,  int iteration,  int epoch){
    modelClasses.add(model.getClass());
  }
}
