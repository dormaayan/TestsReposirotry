public class ReuseExecutorTest extends BaseExecutorTest {
  @Test public void dummy(){
  }
  @Override @Test public void shouldFetchPostWithBlogWithCompositeKey() throws Exception {
    super.shouldFetchPostWithBlogWithCompositeKey();
  }
  @Override protected Executor createExecutor(  Transaction transaction){
    return new ReuseExecutor(config,transaction);
  }
}
