@Config(shadows={ShadowLog.class},manifest="src/test/AndroidManifest.xml",sdk=21) @RunWith(RobolectricTestRunner.class) public abstract class FacebookTestCase {
  @Before public void setUp(){
    ShadowLog.stream=System.out;
    MockitoAnnotations.initMocks(this);
  }
}
