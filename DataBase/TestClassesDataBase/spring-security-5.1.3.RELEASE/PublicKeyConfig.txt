@EnableWebFlux @EnableWebFluxSecurity static class PublicKeyConfig {
  @Bean SecurityWebFilterChain springSecurity(  ServerHttpSecurity http) throws Exception {
    http.authorizeExchange().anyExchange().hasAuthority("SCOPE_message:read").and().oauth2ResourceServer().jwt().publicKey(publicKey());
    return http.build();
  }
}
