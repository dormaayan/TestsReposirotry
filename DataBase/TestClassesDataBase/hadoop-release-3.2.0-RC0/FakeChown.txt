/** 
 * Faked Chown class for  {@link testChownUserAndGroupValidity()}. The test only covers argument parsing, so override to skip processing.
 */
private static class FakeChown extends FsShellPermissions.Chown {
  public static String NAME="chown";
  @Override protected void processArgument(  PathData item){
  }
}
