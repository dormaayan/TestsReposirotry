/** 
 * Faked Chgrp class for  {@link testChgrpGroupValidity()}. The test only covers argument parsing, so override to skip processing.
 */
private static class FakeChgrp extends FsShellPermissions.Chgrp {
  public static String NAME="chgrp";
  @Override protected void processArgument(  PathData item){
  }
}
