/** 
 * The Balancer ensures that it disperses RPCs to the NameNode in order to avoid NN's RPC queue saturation.
 */
public class TestBalancerRPCDelay {
  @Test(timeout=100000) public void testBalancerRPCDelay() throws Exception {
    new TestBalancer().testBalancerRPCDelay();
  }
}
