/** 
 * Base class for queue collection tests.
 * @author Jared Levy
 */
@GwtCompatible @Ignore public class AbstractQueueTester<E> extends AbstractCollectionTester<E> {
  protected final Queue<E> getQueue(){
    return (Queue<E>)collection;
  }
}
