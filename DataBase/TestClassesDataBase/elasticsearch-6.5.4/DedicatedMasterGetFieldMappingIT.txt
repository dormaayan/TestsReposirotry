@ClusterScope(scope=Scope.TEST,numDataNodes=0) public class DedicatedMasterGetFieldMappingIT extends SimpleGetFieldMappingsIT {
  @Override protected boolean forbidPrivateIndexSettings(){
    return false;
  }
  @Before public void before1() throws Exception {
    Settings settings=Settings.builder().put(Node.NODE_DATA_SETTING.getKey(),false).build();
    internalCluster().startNodes(settings,Settings.EMPTY);
  }
}
