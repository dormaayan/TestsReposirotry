/** 
 * This interface fulfills both TopLevelItem and BuildableItem interface, this allows it for being present in a view as well as the build queue!
 */
private interface BuildableTopLevelItem extends TopLevelItem, BuildableItem {
}
