class OverrideRun extends InvisibleAction implements EnvironmentContributingAction {
  private boolean wasCalled=false;
  @Override public void buildEnvironment(  Run<?,?> run,  EnvVars env){
    wasCalled=true;
  }
  boolean wasNewMethodCalled(){
    return wasCalled;
  }
}
