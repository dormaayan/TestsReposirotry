/** 
 * Runs the TestReplicationKillRS test and selects the RS to kill in the slave cluster Do not add other tests in this class.
 */
@Category({ReplicationTests.class,LargeTests.class}) public class TestReplicationKillSlaveRS extends TestReplicationKillRS {
  @ClassRule public static final HBaseClassTestRule CLASS_RULE=HBaseClassTestRule.forClass(TestReplicationKillSlaveRS.class);
  @Test public void killOneSlaveRS() throws Exception {
    loadTableAndKillRS(utility2);
  }
}
