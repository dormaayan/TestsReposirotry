/** 
 * Enables compression and runs the TestWALReplay tests.
 */
@Category({RegionServerTests.class,MediumTests.class}) public class TestWALReplayCompressed extends TestWALReplay {
  @ClassRule public static final HBaseClassTestRule CLASS_RULE=HBaseClassTestRule.forClass(TestWALReplayCompressed.class);
  @BeforeClass public static void setUpBeforeClass() throws Exception {
    Configuration conf=AbstractTestWALReplay.TEST_UTIL.getConfiguration();
    conf.setBoolean(HConstants.ENABLE_WAL_COMPRESSION,true);
    TestWALReplay.setUpBeforeClass();
  }
}
