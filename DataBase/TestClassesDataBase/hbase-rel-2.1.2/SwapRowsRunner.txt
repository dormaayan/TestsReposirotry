class SwapRowsRunner implements Runnable {
  @Override public void run(){
    try {
      swapRows(table);
    }
 catch (    Throwable e) {
      failures.incrementAndGet();
      e.printStackTrace();
    }
  }
}
