@RunWith(PaxExam.class) public class CamelMvelTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelMvelTest.class);
  @Test public void test() throws Exception {
    testLanguage(COMPONENT);
  }
}
