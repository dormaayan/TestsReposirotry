@RunWith(PaxExam.class) public class CamelSchematronTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelSchematronTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
