@RunWith(PaxExam.class) public class CamelGitTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelGitTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
