@RunWith(PaxExam.class) public class CamelCryptoCmsTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelCryptoCmsTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
