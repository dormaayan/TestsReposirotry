/** 
 * Integration test listing images on Docker Platform
 */
public class DockerProducerTestIT extends DockerITTestSupport {
  @Test public void testDocker() throws Exception {
    template.sendBody("direct:in","");
    MockEndpoint mock=getMockEndpoint("mock:result");
    mock.expectedMinimumMessageCount(1);
    assertMockEndpointsSatisfied(60,TimeUnit.SECONDS);
  }
  @Override protected RouteBuilder createRouteBuilder() throws Exception {
    return new RouteBuilder(){
      public void configure(){
        from("direct:in").to("docker://imagelist?maxTotalConnections=10").log("${body}").to("mock:result");
      }
    }
;
  }
}
