@RunWith(PaxExam.class) public class CamelMinaTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelMinaTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
