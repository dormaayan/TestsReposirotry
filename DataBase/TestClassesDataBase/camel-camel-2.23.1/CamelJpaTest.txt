@RunWith(PaxExam.class) public class CamelJpaTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelJpaTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
