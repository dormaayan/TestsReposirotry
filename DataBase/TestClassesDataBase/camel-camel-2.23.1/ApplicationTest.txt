@RunWith(SpringRunner.class) @SpringBootTest(webEnvironment=WebEnvironment.RANDOM_PORT) public class ApplicationTest {
  @Autowired private TestRestTemplate restTemplate;
  @Autowired private CamelContext camelContext;
  @Test public void geocoderAddressTest(){
    ResponseEntity<GeocodingResult[]> response=restTemplate.exchange("/camel/geocoder?address=Paris",HttpMethod.GET,null,new ParameterizedTypeReference<GeocodingResult[]>(){
    }
);
    assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);
    GeocodingResult[] res=response.getBody();
    assertNotNull(res);
  }
}
