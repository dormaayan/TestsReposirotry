public abstract class AbstractJsonTestSupport extends CamelTestSupport {
  private static final String XOM_WARNING="XOM must be in classpath for camel-xmljson unit tests to execute; " + "please enable the appropriate build profile.";
  @BeforeClass public static void checkXomInClasspath(){
    try {
      Class.forName("nu.xom.Serializer");
    }
 catch (    Throwable t) {
      System.out.println(XOM_WARNING);
      Assume.assumeNoException(XOM_WARNING,t);
    }
  }
}
