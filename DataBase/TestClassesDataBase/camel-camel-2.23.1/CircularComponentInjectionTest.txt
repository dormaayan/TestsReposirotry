@ContextConfiguration public class CircularComponentInjectionTest extends SpringRunWithTestSupport {
  @EndpointInject(ref="seda") protected Endpoint sedaEndpoint;
  @EndpointInject(ref="log") protected Endpoint logEndpoint;
  @DirtiesContext @Test public void test(){
    Assert.assertNotNull(sedaEndpoint);
    Assert.assertNotNull(logEndpoint);
  }
}
