/** 
 * As the  {@link RunWith} annotation is flagged to be {@link Inherited} wemake use of this class as the base class of those tests where we need {@link SpringJUnit4ClassRunner} as the test runner but at the same timerequire the useful testing methods provided by  {@link TestSupport}.
 */
@RunWith(SpringJUnit4ClassRunner.class) public class SpringRunWithTestSupport extends TestSupport {
}
