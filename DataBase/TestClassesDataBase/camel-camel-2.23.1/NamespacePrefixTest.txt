/** 
 * @version 
 */
@ContextConfiguration public class NamespacePrefixTest extends SpringRunWithTestSupport {
  protected String body="Hello";
  @Autowired private ProducerTemplate template;
  @EndpointInject(uri="mock:results") private MockEndpoint endpoint;
  @Test public void testAssertThatInjectionWorks() throws Exception {
    assertNotNull("Bean should be injected",template);
    assertNotNull("endpoint should be injected",endpoint);
  }
}
