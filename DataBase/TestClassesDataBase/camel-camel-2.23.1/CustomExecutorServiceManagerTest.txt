/** 
 * @version 
 */
@ContextConfiguration public class CustomExecutorServiceManagerTest extends SpringRunWithTestSupport {
  @Autowired protected CamelContext context;
  @Test public void testCustomExecutorService() throws Exception {
    assertIsInstanceOf(CustomExecutorServiceManager.class,context.getExecutorServiceManager());
  }
}
