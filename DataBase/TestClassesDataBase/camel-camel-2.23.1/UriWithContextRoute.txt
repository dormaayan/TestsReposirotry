@ContextName("first") class UriWithContextRoute extends RouteBuilder {
  @Inject @Uri(value="direct:inbound",context="first") Endpoint inbound;
  @Inject @Mock(value="mock:outbound",context="first") MockEndpoint outbound;
  @Override public void configure(){
    from(inbound).to(outbound);
  }
}
