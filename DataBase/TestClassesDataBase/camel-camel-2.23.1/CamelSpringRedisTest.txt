@RunWith(PaxExam.class) @Ignore("CAMEL-10363 camel-spring-redis - Cannot install in Karaf") public class CamelSpringRedisTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelSpringRedisTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
