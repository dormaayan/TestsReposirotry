/** 
 * @version 
 */
public class Mina2SslContextParametersTcpTest extends BaseMina2Test {
  @Test public void testMinaRoute() throws Exception {
    MockEndpoint endpoint=getMockEndpoint("mock:result");
    Object body="Hello there!";
    endpoint.expectedBodiesReceived(body);
    template.sendBodyAndHeader("mina2:tcp://localhost:" + getPort() + "?sync=false&minaLogger=true&sslContextParameters=#sslContextParameters",body,"cheese",123);
    assertMockEndpointsSatisfied();
  }
  @Override protected boolean isUseSslContext(){
    return true;
  }
  protected RouteBuilder createRouteBuilder(){
    return new RouteBuilder(){
      public void configure(){
        fromF("mina2:tcp://localhost:%s?sync=false&minaLogger=true&sslContextParameters=#sslContextParameters",getPort()).to("log:before?showAll=true").to("mock:result").to("log:after?showAll=true");
      }
    }
;
  }
}
