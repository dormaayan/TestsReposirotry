@RunWith(PaxExam.class) @Ignore("Need OSGi bundles of hadoop, and it does not run well in OSGi neither") public class CamelHbaseTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelHbaseTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
