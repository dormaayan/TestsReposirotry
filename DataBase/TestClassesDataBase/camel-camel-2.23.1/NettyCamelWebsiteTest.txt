@Ignore("For manual testing") public class NettyCamelWebsiteTest extends CamelTestSupport {
  @Test public void testCamel() throws Exception {
    getMockEndpoint("mock:test").expectedMessageCount(1);
    template.sendBody("direct:in",null);
    assertMockEndpointsSatisfied();
  }
  @Override protected RouteBuilder createRouteBuilder() throws Exception {
    return new RouteBuilder(){
      @Override public void configure() throws Exception {
        from("direct:in").setHeader(Exchange.HTTP_METHOD,constant("GET")).to("netty4-http:http://camel.apache.org/").to("log:test").to("mock:test");
      }
    }
;
  }
}
