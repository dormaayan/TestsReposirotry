/** 
 * @version 
 */
@ContextConfiguration public class SpringTraceTest extends SpringRunWithTestSupport {
  @Autowired protected ProducerTemplate camelTemplate;
  @Test public void testTracing() throws Exception {
    camelTemplate.sendBody("Hello");
    camelTemplate.sendBody(1234);
  }
}
