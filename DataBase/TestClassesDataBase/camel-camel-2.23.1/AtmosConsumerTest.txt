public class AtmosConsumerTest extends CamelTestSupport {
  @EndpointInject(uri="atmos:foo/get?remotePath=/path&fullTokenId=fakeToken&secretKey=fakeSecret&uri=https://fake/uri") private AtmosEndpoint atmosEndpoint;
  @Override protected RouteBuilder createRouteBuilder() throws Exception {
    return new RouteBuilder(){
      @Override public void configure() throws Exception {
        from(atmosEndpoint).to("mock:test");
      }
    }
;
  }
  @Test public void shouldCreateGetConsumer() throws Exception {
    Consumer consumer=atmosEndpoint.createConsumer(null);
    Assert.assertTrue(consumer instanceof AtmosScheduledPollGetConsumer);
    assertEquals("foo",atmosEndpoint.getConfiguration().getName());
  }
}
