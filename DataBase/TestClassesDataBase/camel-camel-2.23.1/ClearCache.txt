private class ClearCache implements Runnable {
  @Override public void run(){
    try {
      Thread.sleep(200);
      clearCachedSchema();
    }
 catch (    Exception e) {
      throw new RuntimeException(e);
    }
  }
}
