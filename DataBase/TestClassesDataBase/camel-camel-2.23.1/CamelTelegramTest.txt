@RunWith(PaxExam.class) public class CamelTelegramTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelTelegramTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
