@RunWith(PaxExam.class) public class CamelAzureTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelAzureTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT,"azure-blob");
    testComponent(COMPONENT,"azure-queue");
  }
}
