@RunWith(SpringRunner.class) @DirtiesContext @SpringBootApplication @SpringBootTest(classes={GroupConditionTest4.TestConfiguration.class},properties={"test.group.enabled=true","test.group.my-class.enabled=true"}) public class GroupConditionTest4 extends GroupConditionTestBase {
  @Autowired ApplicationContext context;
  @Test public void testCondition(){
    Assert.assertEquals(1,context.getBeansOfType(MyClass.class).size());
  }
}
