@RunWith(PaxExam.class) public class CamelServiceTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelServiceTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
