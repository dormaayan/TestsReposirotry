@Ignore("Manual test as you need to start a Kestrel broker") public class KestrelProducerTest extends CamelTestSupport {
  @EndpointInject(uri="kestrel://cameltest1") ProducerTemplate producerTemplate;
  @Test public void testKestrelProducer() throws Exception {
    for (int k=0; k < 100; ++k) {
      producerTemplate.sendBody("test body " + k);
    }
  }
}
