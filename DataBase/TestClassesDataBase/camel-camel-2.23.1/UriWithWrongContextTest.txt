@RunWith(Arquillian.class) public class UriWithWrongContextTest {
  @ClassRule public static TestRule exception=ExpectedDeploymentException.none().expect(RuntimeException.class).expectMessage(containsString("Error adding routes of type [" + UriWithWrongContextRoute.class.getName() + "] to Camel context [first]")).expectMessage(containsString("Error injecting endpoint annotated with @org.apache.camel.cdi.Uri")).expectMessage(containsString("No Camel context with name [second] is deployed!"));
  @Deployment public static Archive<?> deployment(){
    return ShrinkWrap.create(JavaArchive.class).addPackage(CdiCamelExtension.class.getPackage()).addClasses(FirstCamelContextBean.class,UriWithWrongContextRoute.class).addAsManifestResource(EmptyAsset.INSTANCE,"beans.xml");
  }
  @Test public void test(){
  }
}
