@RunWith(PaxExam.class) public class CamelMasterTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelMasterTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
