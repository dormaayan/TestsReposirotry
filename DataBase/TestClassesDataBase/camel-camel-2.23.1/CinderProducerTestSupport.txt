@RunWith(MockitoJUnitRunner.class) public class CinderProducerTestSupport extends AbstractProducerTestSupport {
  @Mock protected CinderEndpoint endpoint;
  @Mock protected BlockStorageService blockStorageService;
  @Before public void setUpComputeService(){
    when(client.blockStorage()).thenReturn(blockStorageService);
  }
}
