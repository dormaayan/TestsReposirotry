/** 
 * Spring placeholder DSL test
 */
public class SpringOptionalPropertiesDslTokenizeTest extends SpringTestSupport {
  @Override protected AbstractXmlApplicationContext createApplicationContext(){
    return new ClassPathXmlApplicationContext("org/apache/camel/component/properties/SpringOptionalPropertiesDslTokenizeTest.xml");
  }
  @Test public void testPlaceholderDslTest() throws Exception {
    getMockEndpoint("mock:a").expectedMessageCount(2);
    getMockEndpoint("mock:a").expectedBodiesReceived("a,b","c,d");
    template.sendBody("direct:start","a,b,c,d");
    assertMockEndpointsSatisfied();
  }
}
