/** 
 * Testing that the ssl configuration is not created if a subproperty of "camel.ssl.config." (note the last dot) is not present.
 */
@RunWith(SpringRunner.class) @SpringBootApplication @DirtiesContext @ContextConfiguration(classes={CamelSSLAutoConfiguration.class,CamelAutoConfiguration.class}) @SpringBootTest(properties={"camel.ssl.configxxx=true"}) public class CamelSSLNoConfigTest {
  @Autowired private ApplicationContext applicationContext;
  @Test(expected=NoSuchBeanDefinitionException.class) public void checkSSLPropertiesNotPresent(){
    applicationContext.getBean(GlobalSSLContextParametersSupplier.class);
  }
}
