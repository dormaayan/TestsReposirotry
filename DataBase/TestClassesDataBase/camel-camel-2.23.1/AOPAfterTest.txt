/** 
 * @version 
 */
public class AOPAfterTest extends ContextTestSupport {
  @Test public void testAOPAfter() throws Exception {
    getMockEndpoint("mock:after").expectedBodiesReceived("Bye World");
    MockEndpoint mock=getMockEndpoint("mock:result");
    mock.expectedBodiesReceived("Bye World");
    String out=template.requestBody("direct:start","Hello World",String.class);
    assertEquals("Bye World",out);
    assertMockEndpointsSatisfied();
  }
  @Override protected RouteBuilder createRouteBuilder() throws Exception {
    return new RouteBuilder(){
      @SuppressWarnings("deprecation") @Override public void configure() throws Exception {
        from("direct:start").aop().after("mock:after").transform(constant("Bye World")).to("mock:result");
      }
    }
;
  }
}
