@RunWith(PaxExam.class) public class CamelHttpTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelHttpTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
