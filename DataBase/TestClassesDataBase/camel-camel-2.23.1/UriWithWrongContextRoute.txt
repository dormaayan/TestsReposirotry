@ContextName("first") class UriWithWrongContextRoute extends RouteBuilder {
  @Inject @Uri(value="direct:inbound",context="second") Endpoint inbound;
  @Override public void configure(){
    from(inbound).to("mock:outbound");
  }
}
