@RunWith(MockitoJUnitRunner.class) public class NeutronProducerTestSupport extends AbstractProducerTestSupport {
  @Mock protected NeutronEndpoint endpoint;
  @Mock protected NetworkingService networkingService;
  @Before public void setUpComputeService(){
    when(client.networking()).thenReturn(networkingService);
  }
}
