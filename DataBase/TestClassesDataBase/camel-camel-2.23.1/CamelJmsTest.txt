@RunWith(PaxExam.class) public class CamelJmsTest extends BaseKarafTest {
  public static final String COMPONENT=extractName(CamelJmsTest.class);
  @Test public void test() throws Exception {
    testComponent(COMPONENT);
  }
}
