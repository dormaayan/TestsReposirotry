@RunWith(Arquillian.class) public class UnsatisfiedContextForEndpointInjectTest {
  @ClassRule public static TestRule exception=ExpectedDeploymentException.none().expect(RuntimeException.class).expectMessage(containsString("Error adding routes of type [" + EndpointInjectWrongContextRoute.class.getName() + "] to Camel context")).expectMessage(containsString("No Camel context with name [foo] is deployed!"));
  @Deployment public static Archive<?> deployment(){
    return ShrinkWrap.create(JavaArchive.class).addPackage(CdiCamelExtension.class.getPackage()).addClass(EndpointInjectWrongContextRoute.class).addAsManifestResource(EmptyAsset.INSTANCE,"beans.xml");
  }
  @Test public void test(){
  }
}
