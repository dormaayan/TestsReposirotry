@ShutdownTimeout(value=11,timeUnit=TimeUnit.MILLISECONDS) public class CamelSpringRunnerShutdownTimeoutTest extends CamelSpringRunnerPlainTest {
  @Test @Override public void testShutdownTimeout() throws Exception {
    assertEquals(11,camelContext.getShutdownStrategy().getTimeout());
    assertEquals(TimeUnit.MILLISECONDS,camelContext.getShutdownStrategy().getTimeUnit());
  }
}
