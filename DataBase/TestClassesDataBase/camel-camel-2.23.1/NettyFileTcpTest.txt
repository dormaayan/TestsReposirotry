/** 
 * @version 
 */
public class NettyFileTcpTest extends BaseNettyTest {
  @Test public void testMinaRoute() throws Exception {
    MockEndpoint endpoint=getMockEndpoint("mock:results");
    endpoint.expectedMessageCount(1);
    endpoint.message(0).body().startsWith("Hello World");
    assertMockEndpointsSatisfied();
  }
  protected RouteBuilder createRouteBuilder(){
    return new RouteBuilder(){
      public void configure(){
        from("netty4:tcp://localhost:{{port}}?sync=false&textline=true").to("mock:results");
        from("file:src/test/data?noop=true&fileName=message1.txt").to("netty4:tcp://localhost:{{port}}?sync=false&textline=true");
      }
    }
;
  }
}
