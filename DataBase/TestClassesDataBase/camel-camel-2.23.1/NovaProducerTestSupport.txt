@RunWith(MockitoJUnitRunner.class) public class NovaProducerTestSupport extends AbstractProducerTestSupport {
  @Mock protected NovaEndpoint endpoint;
  @Mock protected ComputeService computeService;
  @Before public void setUpComputeService(){
    when(client.compute()).thenReturn(computeService);
  }
}
