@RunWith(SpringJUnit4ClassRunner.class) @ContextConfiguration public class SecureRandomParametersFactoryBeanTest {
  @Resource SecureRandomParameters srp;
  @Test public void testKeyStoreParameters(){
    assertEquals("algorithm",srp.getAlgorithm());
    assertEquals("provider",srp.getProvider());
  }
}
