@RunWith(CamelCdiRunner.class) public class CustomContextTest {
  @Test public void testCustomContext(  CamelContext context){
    assertEquals("Management strategy is incorrect!",context.getManagementStrategy().getClass(),DefaultManagementStrategy.class);
  }
@Default @ContextName("camel-test-cdi") @ApplicationScoped static class CustomCamelContext extends DefaultCamelContext {
    @PostConstruct void customize(){
      disableJMX();
    }
  }
}
