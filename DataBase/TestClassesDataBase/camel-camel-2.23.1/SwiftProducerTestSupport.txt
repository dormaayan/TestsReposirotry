@RunWith(MockitoJUnitRunner.class) public class SwiftProducerTestSupport extends AbstractProducerTestSupport {
  @Mock protected SwiftEndpoint endpoint;
  @Mock protected ObjectStorageService objectStorageService;
  @Before public void setUpComputeService(){
    when(client.objectStorage()).thenReturn(objectStorageService);
  }
}
